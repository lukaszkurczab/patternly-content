import { createHash } from "node:crypto";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { buildManifest, ROOT } from "./lib/model.mjs";

const TRACK = "microsoft-azure-administrator-associate-az-104";
const NODE = "azure_resource_deployment_virtual_machines_and_resilience";
const CREATED_AT = "2026-08-16T00:00:00Z";
const curriculumPath = join(ROOT, "config/curricula", `${TRACK}.json`);
const anchorsPath = join(ROOT, "evidence/curriculum", `${TRACK}-source-anchors.json`);

const S = (sourceId, title, url, authority, boundary, transfer) => ({
  sourceId, provider: "Microsoft", sourceType: "direct_first_party_product_documentation", title, url,
  checkedDate: "2026-08-16", sourceVolatility: "high",
  versionContext: "current Microsoft Learn documentation checked for AZ104-N05 authoring",
  resolutionState: "resolved_exact_first_party", authoritativeFor: [authority],
  propertyAuthorityBasis: "manual_slot_property_review_2026_08_16", boundary, transfer,
});

const sourceSpecs = [
  S("arm-management-overview", "What is Azure Resource Manager?", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/overview", "arm-control-plane-resource-model", "ARM manages resources through providers, scopes, resource IDs, and declarative control-plane operations.", "Re-evaluate provider registration, scope, API version, identity, and dependencies when the same declaration behaves differently."),
  S("arm-template-syntax", "Understand the structure and syntax of ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/syntax", "arm-template-json-structure", "An ARM template declares parameters, variables, resources, outputs, and supported expressions in a fixed JSON schema.", "Use schema-aware tooling and resource reference documentation instead of treating exported JSON as an opaque script."),
  S("arm-deployment-modes", "Azure Resource Manager deployment modes", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/deployment-modes", "arm-incremental-complete-semantics", "Incremental deployment reapplies declared properties without deleting omitted resources; complete-mode deletion behavior is scope- and resource-sensitive.", "Choose the deployment operation from the desired end state and inspect deletions with what-if before changing mode."),
  S("arm-template-dependencies", "Define the order for deploying resources in ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/resource-dependency", "arm-resource-dependency-order", "ARM deploys independent resources in parallel and honors explicit or implicit dependencies only where one resource needs another.", "Add a dependency for real provisioning order, not merely to make source JSON appear sequential."),
  S("arm-template-parameters", "Parameters in ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/parameters", "arm-parameter-contract", "Parameters expose deployment-time values and can enforce type, allowed-value, length, and numeric constraints.", "Keep environment values in parameter files and secrets in protected references rather than hard-coding them."),
  S("arm-template-functions", "ARM template functions", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/template-functions", "arm-template-expression-evaluation", "ARM functions calculate values and resource identifiers during deployment within documented expression and scope rules.", "Use resourceId-family functions for scope-correct IDs and inspect expression inputs when an evaluated value is wrong."),
  S("arm-template-copy", "Resource iteration in ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/copy-resources", "arm-resource-copy-loop", "A resource copy loop creates a bounded set of resource instances whose names and properties are derived from copyIndex.", "Keep loop identity stable so a count or ordering change does not unintentionally remap persistent resources."),
  S("arm-template-condition", "Conditional deployment in ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/conditional-resource-deployment", "arm-resource-condition", "A condition controls whether a declared resource is deployed, but references and dependency expressions still require careful evaluation.", "Test both condition branches and guard runtime references that would evaluate against an absent resource."),
  S("arm-template-outputs", "Outputs in ARM templates", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/outputs", "arm-template-output-contract", "Outputs return deployment results such as resource IDs or properties and can be conditional or iterated within documented limits.", "Expose stable identifiers needed by downstream automation without returning secrets."),
  S("arm-export-template", "Export an Azure Resource Manager template", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/export-template-portal", "arm-export-limitations", "Export reconstructs a useful template from current resources but may omit runtime settings or produce parameters and expressions needing review.", "Treat export as a starting point, then validate, parameterize, remove read-only properties, and test redeployment."),
  S("bicep-overview", "What is Bicep?", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/overview", "bicep-declarative-model", "Bicep is a declarative Azure resource language compiled to ARM JSON rather than a general-purpose imperative language.", "Model desired resources and their relationships; use deployment scripts only for work that cannot be expressed declaratively."),
  S("bicep-resources", "Resource declaration in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/resource-declaration", "bicep-resource-declaration", "A Bicep resource declaration combines a symbolic name, type and API version, resource name, and deployable properties.", "Use symbolic references to obtain IDs and properties so dependency inference remains accurate."),
  S("bicep-existing", "Reference existing resources in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/existing-resource", "bicep-existing-resource", "The existing keyword references a resource without redeploying it and requires the correct name and scope.", "Use existing for ownership boundaries; a not-found result usually means the name or scope is wrong."),
  S("bicep-modules", "Modules in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/modules", "bicep-module-contract", "A module deploys another Bicep file with explicit parameter inputs, scope, deployment name, and output access.", "Keep module contracts narrow and version reusable registry modules deliberately."),
  S("bicep-loops", "Iterative loops in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/loops", "bicep-iteration", "Bicep loops create repeated resources, modules, variables, properties, or outputs from an array, range, or object.", "Use stable item keys and filter expressions so collection changes do not unexpectedly replace stateful resources."),
  S("bicep-condition", "Conditional deployments in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/conditional-resource-deployment", "bicep-conditional-deployment", "An if expression on a resource or module determines whether that declaration is deployed.", "Guard dereferences of conditional resources and test the false path, not only successful creation."),
  S("bicep-scopes", "Deployment scopes in Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/deploy-to-resource-group", "bicep-deployment-scope", "targetScope defines the file's deployment scope while modules can deliberately deploy to other allowed scopes.", "Choose the scope command and permissions that match targetScope and use modules for cross-scope composition."),
  S("bicep-decompile", "Decompiling ARM template JSON to Bicep", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/bicep/decompile", "bicep-decompilation", "Decompilation is best-effort and can require manual correction even when the source JSON deployed successfully.", "Build and lint the result, resolve warnings, refactor names and modules, and test before adopting it as canonical source."),
  S("arm-what-if", "ARM template deployment what-if operation", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/templates/deploy-what-if", "arm-deployment-what-if", "What-if predicts create, delete, modify, deploy, ignore, and no-change effects without applying the deployment.", "Review noisy property re-evaluations and permissions, then use the preview as evidence rather than a guarantee of runtime success."),
  S("arm-deployment-errors", "Troubleshoot common Azure deployment errors", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/troubleshooting/common-deployment-errors", "arm-deployment-error-diagnosis", "Deployment error codes and inner details distinguish syntax, validation, policy, authorization, quota, dependency, and runtime failures.", "Start with the failing operation and code, correct the underlying constraint, then safely redeploy the idempotent declaration."),
  S("vm-overview", "Virtual machines in Azure", "https://learn.microsoft.com/en-us/azure/virtual-machines/overview", "azure-vm-composition", "An Azure VM depends on compute, image, disk, NIC, network, identity, availability, and management choices made together.", "Validate regional capability, quotas, image generation, disk and NIC limits before selecting a size or location."),
  S("vm-sizes", "Virtual machine sizes overview", "https://learn.microsoft.com/en-us/azure/virtual-machines/sizes/overview", "azure-vm-size-selection", "VM size families differ in vCPU, memory, disk, network, accelerator, architecture, and regional availability.", "Select from measured workload constraints and verify subscription quota and cluster availability at deployment time."),
  S("vm-generation-2", "Support for generation 2 VMs on Azure", "https://learn.microsoft.com/en-us/azure/virtual-machines/generation-2", "azure-vm-generation", "Generation 2 enables UEFI-based capabilities but requires a compatible image and has conversion and feature boundaries.", "Choose generation from image and security requirements before deployment because it is not an in-place toggle."),
  S("vm-boot-diagnostics", "Azure VM boot diagnostics", "https://learn.microsoft.com/en-us/azure/virtual-machines/boot-diagnostics", "azure-vm-boot-diagnostics", "Boot diagnostics captures serial console output and screenshots that help diagnose guest startup failures.", "Enable managed boot diagnostics before an incident and correlate boot evidence with platform and guest health."),
  S("vm-resize", "Resize a virtual machine", "https://learn.microsoft.com/en-us/azure/virtual-machines/sizes/resize-vm", "azure-vm-resize", "A resize can require deallocation when the target size is unavailable on the current cluster and is constrained by region, quota, disks, NICs, and features.", "Check target capability and quota, plan restart or deallocation, then verify guest and storage performance afterward."),
  S("managed-disks-overview", "Introduction to Azure managed disks", "https://learn.microsoft.com/en-us/azure/virtual-machines/managed-disks-overview", "azure-managed-disk-model", "Managed disks are independent Azure resources with a selected SKU, size, performance, encryption, and attachment state.", "Manage disk lifecycle separately from the VM and confirm delete options before removing compute resources."),
  S("vm-disk-types", "Select a disk type for Azure IaaS VMs", "https://learn.microsoft.com/en-us/azure/virtual-machines/disks-types", "azure-managed-disk-type-selection", "Ultra, Premium SSD v2, Premium SSD, Standard SSD, and Standard HDD have different latency, IOPS, throughput, size, and support constraints.", "Choose from measured I/O and recovery needs, then verify VM size, region, zone, caching, and redundancy support."),
  S("vm-disk-caching", "Disk caching overview", "https://learn.microsoft.com/en-us/azure/virtual-machines/premium-storage-performance", "azure-vm-disk-caching", "Host caching can improve read performance but changes durability and consistency behavior and is limited by workload and disk type.", "Match None, ReadOnly, or ReadWrite to the application's write semantics and benchmark end-to-end limits."),
  S("vm-encryption-at-host", "Encryption at host", "https://learn.microsoft.com/en-us/azure/virtual-machines/linux/disks-enable-host-based-encryption-cli", "azure-vm-encryption-at-host", "Encryption at host encrypts temporary disks and host-side caches and must be supported and enabled for the VM or scale set.", "Confirm regional and size support and follow required deallocation or creation-time steps before changing the property."),
  S("managed-disk-snapshots", "Create a snapshot of a virtual hard disk", "https://learn.microsoft.com/en-us/azure/virtual-machines/snapshot-copy-managed-disk", "azure-managed-disk-snapshot", "A managed snapshot is a read-only point-in-time disk resource used to create another managed disk.", "Use application-consistent coordination when required and restore by creating and attaching or swapping a disk."),
  S("vm-availability", "Availability options for Azure Virtual Machines", "https://learn.microsoft.com/en-us/azure/virtual-machines/availability", "azure-vm-availability-design", "Availability zones and sets protect against different failure boundaries and require multiple correctly distributed VM instances.", "Select the redundancy boundary before deployment and pair compute placement with redundant networking and data services."),
  S("vm-availability-sets", "Availability sets overview", "https://learn.microsoft.com/en-us/azure/virtual-machines/availability-set-overview", "azure-vm-availability-set", "An availability set distributes VMs across fault and update domains within one datacenter-scale regional boundary.", "Use it for nonzonal multi-VM designs and remember that placing a lone VM in a set creates no application redundancy."),
  S("vm-availability-zones", "Availability zones overview", "https://learn.microsoft.com/en-us/azure/reliability/availability-zones-overview", "azure-availability-zone", "Availability zones are separate datacenter groups within a region and zonal resources remain pinned to a selected zone.", "Deploy multiple instances across zones and design zone-redundant dependencies to survive a zone loss."),
  S("vmss-orchestration", "Orchestration modes for Virtual Machine Scale Sets", "https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-orchestration-modes", "vmss-orchestration-mode", "Flexible and Uniform orchestration differ in instance resource model, supported features, and lifecycle behavior; the mode is chosen at creation.", "Choose the mode from workload heterogeneity, VM API needs, availability, image and update requirements before deployment."),
  S("vmss-autoscale", "Automatically scale a Virtual Machine Scale Set", "https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-autoscale-overview", "vmss-autoscale-rule", "Autoscale evaluates metric or schedule profiles within minimum, maximum, default, aggregation, threshold, duration, and cooldown settings.", "Design complementary scale-out and scale-in rules with hysteresis and verify metric availability and time grain."),
  S("vmss-upgrades", "Upgrade policies for Virtual Machine Scale Sets", "https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-upgrade-policy", "vmss-upgrade-policy", "Manual, rolling, and automatic policies determine how scale set model changes reach instances and what health evidence protects availability.", "Choose the policy with the application's health signal, maintenance control, and acceptable rollout risk."),
  S("vmss-health", "Application Health extension for Virtual Machine Scale Sets", "https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-health-extension", "vmss-application-health", "The Application Health extension reports instance application health from an HTTP, HTTPS, or TCP endpoint for rolling upgrades and repair decisions.", "Probe an endpoint that represents real readiness and allow enough grace time for application startup."),
  S("vmss-scale-in", "Use scale-in policies with Virtual Machine Scale Sets", "https://learn.microsoft.com/en-us/azure/virtual-machine-scale-sets/virtual-machine-scale-sets-scale-in-policy", "vmss-scale-in-policy", "Scale-in first preserves zone and fault-domain balance, then applies Default, NewestVM, or OldestVM selection behavior.", "Protect irreplaceable instances and use lifecycle hooks or draining before capacity reduction."),
  S("azure-move-resources", "Move resources to a new resource group or subscription", "https://learn.microsoft.com/en-us/azure/azure-resource-manager/management/move-resource-group-and-subscription", "azure-resource-move", "A move changes resource IDs and requires supported resource types, destination validation, dependencies, permissions, and temporary write locks.", "Inventory ID consumers and move dependent resources together, then update automation and access assignments after the move."),
  S("vm-move-region", "Move Azure VMs across regions", "https://learn.microsoft.com/en-us/azure/resource-mover/tutorial-move-region-virtual-machines", "azure-vm-region-move", "Azure Resource Mover prepares, initiates, commits, and cleans up cross-region VM resources rather than changing the location property in place.", "Plan dependency mapping, test the target, control cutover downtime, commit deliberately, and remove source resources only after validation."),
  S("vm-states-billing", "States and billing status of Azure Virtual Machines", "https://learn.microsoft.com/en-us/azure/virtual-machines/states-billing", "azure-vm-power-state-billing", "Stopped and deallocated are different power states; compute billing normally stops only after allocation is released while disks and other resources remain billable.", "Use deallocation for compute release and inspect attached resource costs instead of equating a guest shutdown with zero cost."),
  S("vm-extensions", "Azure virtual machine extensions and features", "https://learn.microsoft.com/en-us/azure/virtual-machines/extensions/overview", "azure-vm-extension-model", "VM extensions use the Azure VM agent to run post-deployment configuration packages with publisher, type, version, settings, and protected settings.", "Keep secrets in protected settings, allow required egress, and inspect extension instance status separately from VM power state."),
  S("vm-run-command", "Run Command overview", "https://learn.microsoft.com/en-us/azure/virtual-machines/run-command-overview", "azure-vm-run-command", "Run Command uses the VM agent to execute scripts when normal guest access is unavailable, subject to agent and outbound connectivity health.", "Use it as an audited recovery channel, make scripts idempotent, and do not treat it as an interactive session."),
  S("vm-extension-troubleshoot", "Troubleshooting Azure Windows VM extension failures", "https://learn.microsoft.com/en-us/azure/virtual-machines/extensions/troubleshoot", "azure-vm-extension-troubleshooting", "Extension failures are diagnosed from provisioning state, substatus, VM agent health, handler logs, egress, permissions, and operation overlap.", "Fix the first failing dependency, avoid deleting evidence prematurely, then retry or reinstall the specific extension safely."),
  S("vm-redeploy", "Redeploy a virtual machine to a new Azure node", "https://learn.microsoft.com/en-us/troubleshoot/azure/virtual-machines/windows/redeploy-to-new-node-windows", "azure-vm-redeploy", "Redeploy moves a VM to a new host while retaining configuration and disks, resets the temporary disk, and causes downtime.", "Use it for suspected host issues after preserving ephemeral data and confirming the problem is not guest configuration."),
];

const sourceById = new Map(sourceSpecs.map((source) => [source.sourceId, source]));
const W = (text, why) => ({ text, why });
const pools = {
  B01: [W("Add a sleep command between every resource creation.", "Imperative delays do not express ARM dependency or desired state."), W("Assume JSON file order determines deployment order.", "ARM deploys independent resources in parallel; source order is not an ordering contract."), W("Use a display name where a fully scoped resource ID is required.", "A display name does not identify provider, type, parent path, subscription, or resource group."), W("Switch to complete mode to repair an authorization error.", "Deployment mode does not grant missing control-plane permissions."), W("Register a provider in a different subscription.", "Provider registration is maintained per subscription used by the deployment."), W("Delete deployment history to make the template idempotent.", "History retention does not change resource desired-state semantics."), W("Hard-code the current subscription ID into every reusable module.", "Scope-aware resource ID functions preserve reuse across subscriptions."), W("Remove every dependsOn entry, including real provisioning dependencies.", "A dependency is required when one resource must exist before another can be provisioned."), W("Treat a successful script exit code as proof of ARM convergence.", "Imperative execution success does not compare or enforce the complete declared state."), W("Assign a data-plane role to fix a resource-provider registration failure.", "Data-plane authorization does not register control-plane resource providers."), W("Rename the resource group to change a resource's region.", "Resource-group metadata and the resource location are independent."), W("Use tags to declare parent-child resource ownership.", "Tags are metadata and do not create ARM parent-child identity or dependency." )],
  B02: [W("Put environment-specific values directly in every resource property.", "Hard-coded environment values defeat parameter reuse."), W("Return an administrator password in outputs.", "Deployment outputs must not expose secrets."), W("Use copyIndex outside the copy expression that defines it.", "copyIndex is valid only in the corresponding iteration context."), W("Reference an optional resource unconditionally when its condition is false.", "An evaluated reference to an absent conditional resource can fail."), W("Assume export produces a production-ready source template.", "Exported templates require review, parameterization, and removal of read-only values."), W("Use a string parameter where the resource schema requires an array.", "ARM validates parameter and resource property types."), W("Set an API version by copying the portal's current date without checking support.", "The provider resource type defines supported API versions and properties."), W("Create duplicate resource names in one scope inside a copy loop.", "Each loop instance must resolve to a unique resource identity."), W("Use variables for values that operators must supply at deployment time.", "Parameters, not variables, form the deployment-time input contract."), W("Add dependsOn to an output to change resource order.", "Outputs do not impose provisioning order on resources."), W("Edit a generated resource ID by concatenating slash-delimited strings.", "resourceId-family functions correctly construct scoped IDs."), W("Expect incremental deployment to reset every omitted property to its default.", "Incremental mode can preserve omitted resources while resource-property behavior depends on the provider contract.")],
  B03: [W("Use an existing declaration to create a missing resource.", "existing only references a resource and does not deploy it."), W("Set targetScope to subscription but run a resource-group deployment command.", "The deployment command must match the Bicep target scope."), W("Pass every parent-file variable implicitly into a module.", "Modules receive only declared parameters."), W("Assume decompiled Bicep is semantically reviewed because it compiles.", "Decompilation is best-effort and still needs human review and testing."), W("Use dependsOn whenever one declaration appears below another.", "Bicep infers dependencies from symbolic references; file order is irrelevant."), W("Dereference a conditional resource without guarding the false branch.", "The reference can be evaluated when the resource is absent."), W("Use a module output before the module deployment completes without a symbolic reference.", "A symbolic module output reference supplies the dependency and value."), W("Change an existing resource's scope by changing only its symbolic name.", "The existing declaration requires the real name and scope."), W("Create stateful resources from array indexes that can reorder.", "Index-based identities can remap resources when collection order changes."), W("Put a secret in a normal string output for a downstream module.", "Outputs are visible deployment data and should not disclose secrets."), W("Use Bicep as an imperative shell-script language.", "Bicep declares Azure desired state and is not a general-purpose command language."), W("Assume module registry references update automatically without a version change.", "Pinned module versions should be advanced deliberately.")],
  B04: [W("Retry the same deployment indefinitely without reading the error code.", "Retries do not correct deterministic validation, policy, quota, or authorization failures."), W("Use what-if with a principal that cannot deploy the target resources.", "What-if requires deployment and target resource permissions."), W("Treat a what-if Modify result as proof that the resource will be replaced.", "Modify describes predicted property change, not necessarily replacement."), W("Delete the entire resource group to clear one failed deployment record.", "Deployment history and deployed resources have separate lifecycles."), W("Increase template timeout to fix an invalid parameter type.", "Parameter validation fails independently of operation duration."), W("Change deployment scope without changing target IDs or permissions.", "Scope changes affect identity, IDs, policies, and required permissions."), W("Ignore the nested inner error returned by the failing operation.", "Inner details commonly identify the exact resource and constraint."), W("Use complete mode as the default response to deployment drift.", "Complete mode can delete omitted resources and requires deliberate preview."), W("Assume validation catches every runtime provider failure.", "Some failures occur only during provider-side deployment."), W("Delete a dependency before retrying the resource that requires it.", "Removing a required dependency makes the target operation less likely to succeed."), W("Run a subscription deployment command for a resource-group-scoped template.", "The command and target scope must agree."), W("Interpret what-if Ignore as a guaranteed no-op.", "Ignore can result from expansion limits or unsupported analysis and requires review.")],
  B05: [W("Select the largest VM size without checking quota.", "Size selection must account for family quota and current regional capacity."), W("Attach a Generation 1-only image to a Generation 2 VM.", "Image and VM generation must be compatible."), W("Add more NICs than the selected size supports.", "VM sizes enforce NIC count limits."), W("Use a public IP when the workload requires private-only administration.", "A public IP expands exposure and is not required for private management paths."), W("Disable boot diagnostics before investigating a boot failure.", "Boot diagnostics provides serial and screenshot evidence for startup issues."), W("Resize to a family unsupported by the attached accelerator.", "Specialized devices and features constrain compatible target sizes."), W("Assume every size is available in every zone of a region.", "Size availability varies by region, zone, subscription, and cluster capacity."), W("Store the Linux private key in VM custom data.", "Private credentials must not be embedded in retrievable deployment data."), W("Change a VM from Generation 1 to Generation 2 in place.", "VM generation is not an in-place mutable property."), W("Use the temporary disk for durable application state.", "Temporary disks can be lost during maintenance, redeploy, or deallocation."), W("Treat a guest shutdown as resource deallocation.", "Guest shutdown normally leaves the VM allocated and compute-billed."), W("Assume an image includes every required extension and runtime secret.", "Image content and post-deployment configuration have separate lifecycle and security concerns.")],
  B06: [W("Enable ReadWrite host caching for a database log that requires durable ordered writes.", "Write-back caching can violate workload durability expectations."), W("Choose Standard HDD for a latency-sensitive transactional workload.", "Standard HDD targets low-cost, latency-tolerant access."), W("Assume disk SKU performance exceeds the VM's aggregate storage limits.", "Effective throughput is bounded by both disk and VM limits."), W("Delete the VM before checking each disk's delete option.", "Disk lifecycle behavior is controlled by delete settings and must be verified first."), W("Attach one managed disk to multiple VMs without a supported shared-disk configuration.", "Normal managed disks are single-writer attachments unless shared-disk requirements are configured."), W("Change caching on an unsupported disk type and expect Azure to ignore it.", "Unsupported caching combinations are rejected rather than silently normalized."), W("Treat a crash-consistent snapshot as automatically application-consistent.", "Application consistency requires workload coordination or supported backup mechanisms."), W("Enable encryption at host on an unsupported VM size.", "The VM size and region must support encryption at host."), W("Use an OS disk snapshot as a writable disk directly.", "A snapshot is read-only; create a managed disk from it first."), W("Assume expanding a disk also expands the guest partition and file system.", "Guest partition and filesystem growth is a separate step."), W("Detach an in-use data disk without quiescing the guest workload.", "Guest I/O must be stopped or coordinated to avoid corruption."), W("Choose a disk solely by capacity and ignore IOPS and throughput.", "Disk performance tiers and VM caps are part of the workload fit.")],
  B07: [W("Place one VM in an availability set and claim application redundancy.", "A single instance has no peer for fault or maintenance continuity."), W("Assume an availability set spans availability zones.", "Availability sets distribute within a datacenter-scale regional boundary, not across zones."), W("Deploy all redundant VMs into the same zone.", "Same-zone placement does not survive loss of that zone."), W("Use zones without checking whether the region and VM size support them.", "Zone and SKU availability is region-specific."), W("Make only compute zonal while every dependency remains single-zone.", "End-to-end resilience is limited by the least resilient dependency."), W("Move an existing VM into an availability set by editing one property.", "Availability placement is selected at deployment and generally requires recreation or migration planning."), W("Treat fault domains as customer-selected physical racks.", "Azure assigns fault domains; customers select the availability construct."), W("Assume zone numbers represent the same physical location across subscriptions.", "Logical zone mappings can differ across subscriptions."), W("Use update domains to protect against an entire datacenter loss.", "Update domains coordinate planned maintenance, not zonal disaster isolation."), W("Place a zonal public IP in a different zone without checking compatibility.", "Zonal dependencies must be designed with compatible placement."), W("Rely on an SLA without deploying the architecture it requires.", "Availability commitments depend on qualifying configuration and multiple instances."), W("Use proximity placement groups as a replacement for redundancy.", "Proximity optimizes latency and can constrain placement; it does not create fault isolation.")],
  B08: [W("Choose orchestration mode after the scale set is created.", "Orchestration mode is a creation-time decision."), W("Use one scale-out rule with no scale-in rule and call capacity self-balancing.", "Autoscale needs complementary rules or profiles to reduce capacity safely."), W("Set minimum capacity above maximum capacity.", "Autoscale capacity bounds must be internally valid."), W("Use an application health endpoint that returns healthy before the app is ready.", "False health signals can expose or preserve nonfunctional instances."), W("Apply a model change under Manual policy and expect every instance to update automatically.", "Manual policy requires explicit instance upgrade actions."), W("Configure identical scale-out and scale-in thresholds with no margin.", "Missing hysteresis can cause oscillation."), W("Protect every instance from scale-in and expect autoscale to reduce capacity.", "Scale-in protection prevents the required deletions."), W("Use CPU on one VM as the metric for the entire scale set without the intended aggregation.", "Autoscale decisions depend on resource scope, aggregation, time grain, and statistic."), W("Delete instances without draining stateful work.", "Scale-in needs workload-aware draining or lifecycle handling."), W("Use Rolling upgrades without any valid health signal for a fragile application.", "Rolling safety depends on meaningful health evidence and configured batch limits."), W("Assume Uniform and Flexible expose identical instance resource APIs.", "Their instance models and feature support differ."), W("Set cooldown to zero for a workload with slow startup.", "Autoscale can reevaluate before new capacity becomes effective and over-scale."), W("Use a load balancer probe that checks only TCP reachability when application readiness is required.", "Transport reachability may not represent application health."), W("Expect default scale-in to ignore zone balance.", "Scale-in prioritizes preserving zone and fault-domain balance before instance age policy." )],
  B09: [W("Edit a resource's location property to move the VM to another region.", "Azure resource location is not changed by an in-place property edit."), W("Move only the VM object and ignore NICs, disks, and dependent IDs.", "Moves require supported dependent resources and consumer updates."), W("Shut down the guest OS and assume compute charges stop.", "Stopped allocated VMs normally continue compute billing."), W("Store extension secrets in public settings.", "Sensitive values belong in protected settings."), W("Run Command when the VM agent is unavailable and expect an interactive console.", "Run Command depends on the agent and is not an interactive connection."), W("Delete extension logs before diagnosing a failed handler.", "Handler and agent logs are primary failure evidence."), W("Redeploy a VM without preserving data on the temporary disk.", "Redeploy resets the temporary disk."), W("Change resource IDs in consumers only after an unvalidated subscription move.", "ID dependencies should be inventoried and updated as part of the move plan."), W("Resize a running VM without checking whether deallocation is required.", "Target cluster availability can require deallocation and downtime."), W("Run two conflicting extension operations concurrently.", "The VM agent serializes or conflicts extension operations and can leave provisioning pending."), W("Commit a cross-region move before testing the target VM.", "Commit finalizes the target decision and should follow validation."), W("Use redeploy to fix a deterministic guest firewall rule.", "Redeploy changes the host, not guest configuration stored on the OS disk."), W("Remove the source resources before the regional cutover is validated.", "Premature cleanup destroys the rollback path."), W("Assume moving a resource automatically recreates every RBAC assignment at the new scope.", "Scope-dependent assignments and external references require explicit verification." )],
};

const combinations = (length) => {
  const result = [];
  for (let a = 0; a < length; a += 1) for (let b = a + 1; b < length; b += 1) for (let c = b + 1; c < length; c += 1) result.push([a, b, c]);
  return result;
};
const tuple = (source, prompt, correct, reason) => ({ source, prompt, correct, reason });

const blocks = {
  "AZ104-N05-B01": [
    tuple("arm-management-overview", "A template deploys Microsoft.Compute resources in a new subscription and returns NoRegisteredProviderFound. What should be done?", "Register the Microsoft.Compute resource provider in that subscription, wait for Registered status, and redeploy.", "Provider registration enables the subscription to use that provider's resource types and locations."),
    tuple("arm-template-functions", "A role assignment template needs the full ID of a storage account in the current resource group. Which value should it use?", "Use resourceId('Microsoft.Storage/storageAccounts', storageAccountName).", "resourceId constructs the provider-qualified, scope-correct Azure resource identifier."),
    tuple("arm-management-overview", "A deployment identity can write VMs but cannot create Microsoft.Resources/deployments. Why can the deployment still fail?", "It also needs permission for the deployment resource at the selected scope.", "ARM records and executes a deployment resource in addition to writing each declared target resource."),
    tuple("arm-management-overview", "A declarative deployment is run twice with unchanged inputs. What behavior should be expected?", "The second run converges on the same desired resource state instead of creating duplicate identities.", "Stable resource identities and declarative PUT semantics make a correctly designed deployment idempotent."),
    tuple("arm-deployment-modes", "An incremental template omits an existing virtual network. What is the expected result?", "The virtual network remains because incremental mode does not delete resources omitted from the template.", "Incremental mode applies declared resources while retaining other resources at the deployment scope."),
    tuple("arm-management-overview", "A resource group is created in West Europe and a VM inside it is created in North Europe. Is this valid?", "Yes; the resource group's location stores group metadata and does not require every resource to use that region.", "Resource group metadata location and individual resource locations are distinct ARM properties."),
    tuple("arm-management-overview", "A team needs repeatable desired-state deployment with dependency tracking and change history. Which approach best fits?", "Use an ARM template or Bicep declaration deployed through Azure Resource Manager.", "ARM declarations describe the end state and let the platform order, track, and repeat control-plane operations."),
    tuple("arm-management-overview", "Which sequence prepares and deploys a resource type in a subscription for the first time?", "Confirm the target scope and permissions, register the required provider, validate regional/API support, deploy the declaration, then inspect deployment operations.", "Provider readiness, authorization, schema support, and deployment evidence are separate prerequisites."),
    tuple("arm-template-dependencies", "A web app configuration needs the hostname produced by a newly declared database resource. Which deployment sequence should the template express?", "Declare the database, reference its symbolic/resource value from the app configuration to create a dependency, then let ARM deploy the app after the database is ready.", "A value reference supplies both the needed value and provisioning order."),
    tuple("arm-management-overview", "A deployment fails only in one subscription with MissingSubscriptionRegistration. What effective state should be checked first?", "Check the target provider's registration state in the failing subscription.", "Provider registration is subscription-specific even when the same identity and template work elsewhere."),
    tuple("arm-template-dependencies", "Two resources deploy in parallel, but one provider requires the other resource to exist first. What is missing?", "Add an explicit or reference-derived dependency from the consuming resource to the prerequisite.", "ARM parallelizes independent declarations and waits only for modeled dependencies."),
    tuple("arm-template-functions", "A child resource deployment returns InvalidResourceReference after a resource-group name was concatenated manually. What should be corrected?", "Build the referenced ID with the appropriate resourceId function and the actual parent/type path.", "Scope-aware ID functions prevent malformed provider and parent resource paths."),
    tuple("arm-management-overview", "Which Azure CLI operation explicitly registers the Microsoft.Compute provider?", "Run `az provider register --namespace Microsoft.Compute` in the target subscription.", "The provider register operation changes the subscription's provider registration state."),
    tuple("arm-management-overview", "What is the boundary between a resource group's location and a resource's location?", "The group location stores ARM deployment metadata; each regional resource's location determines where that service instance runs.", "The two locations serve different control-plane and workload purposes."),
    tuple("arm-deployment-modes", "What is the boundary between incremental and complete deployment behavior?", "Incremental retains omitted resources, whereas complete mode can delete resources absent from the template at its supported deployment scope.", "The selected mode changes reconciliation of omissions and therefore deletion risk."),
    tuple("arm-template-dependencies", "What is the boundary between implicit and explicit ARM dependencies?", "A reference to another declared resource can create an implicit dependency; dependsOn states order when no suitable value reference exists.", "Both affect provisioning order, but explicit dependencies should represent real coupling rather than file layout."),
  ],
  "AZ104-N05-B02": [
    tuple("arm-template-parameters", "The same template must deploy different VM sizes in test and production. Where should the size be defined?", "Declare a string parameter for the VM size and supply environment-specific parameter values.", "Parameters form the deployment-time input contract without duplicating resource declarations."),
    tuple("arm-template-parameters", "A template accepts an administrator credential. Which parameter type protects it from normal deployment-history display?", "Use a secureString or secureObject parameter and source the value through a protected secret path.", "Secure parameter types suppress ordinary logging and display of sensitive input."),
    tuple("arm-template-condition", "A public IP must be created only when `deployPublicIp` is true. How should this be modeled?", "Place a condition expression on the public IP resource and guard any references to it.", "Conditional deployment includes or excludes the resource from the desired declaration."),
    tuple("arm-template-copy", "Ten consistently configured data disks need unique names. Which ARM construct is appropriate?", "Use a resource copy loop with count 10 and derive each name from copyIndex.", "A copy loop expands one declaration into a bounded set of uniquely identified resources."),
    tuple("arm-template-parameters", "Which sequence turns a fixed-location template into a reusable two-environment template?", "Identify varying values, declare typed parameters with safe defaults or constraints, replace fixed properties with parameter expressions, create environment parameter files, then validate both deployments.", "Reusable templates separate stable resource structure from environment inputs."),
    tuple("arm-template-condition", "Which sequence safely adds an optional diagnostics resource?", "Add a Boolean parameter, condition the diagnostics declaration, guard references and dependent outputs, validate true and false branches, then preview both with what-if.", "Every evaluated expression must remain valid when the conditional resource is absent."),
    tuple("arm-template-copy", "Which sequence converts three repeated NIC declarations into one loop?", "Create an input array or count, define one copy block, derive stable unique names and properties per item, update consumers to the resulting IDs, then validate unchanged identities.", "The loop must preserve resource identity and downstream references while removing repetition."),
    tuple("arm-export-template", "Which sequence turns an exported resource-group template into maintainable source?", "Export it, remove read-only and environment-derived properties, repair dependencies, parameterize inputs, verify API versions, validate and what-if, then redeploy to a test scope.", "Export reconstructs configuration but does not replace source review and deployment testing."),
    tuple("arm-template-parameters", "Deployment validation reports that `zones` expects an array but the parameter file supplies a string. What should be fixed?", "Change the parameter contract/value to an array that matches the target resource schema.", "ARM validates JSON types before the provider accepts the resource operation."),
    tuple("arm-template-condition", "A template fails when an optional resource is disabled because an output still calls reference() on it. What is the cause?", "The output expression evaluates a resource that the false condition did not deploy.", "Conditional resources require conditional or safe access in references and outputs."),
    tuple("arm-template-copy", "A loop deployment reports duplicate resource names. What should be inspected?", "Inspect the name expression and ensure each iteration incorporates a stable unique item value or copyIndex.", "All resources of the same type and scope must resolve to distinct identities."),
    tuple("arm-export-template", "An exported template redeployment fails on read-only provisioning properties. What is the correct response?", "Remove provider-returned read-only properties and keep only supported deployable properties from the resource schema.", "Export can include current-state fields that are not accepted in create or update requests."),
    tuple("arm-template-functions", "Which ARM expression constructs a subnet ID from a virtual network and subnet name in the current resource group?", "Use `resourceId('Microsoft.Network/virtualNetworks/subnets', vnetName, subnetName)`.", "The nested resourceId form includes the provider type and both parent and child names."),
    tuple("arm-template-functions", "Which expression returns the current resource group's location?", "Use `resourceGroup().location`.", "The resourceGroup function exposes metadata for the active resource-group deployment scope."),
    tuple("arm-template-outputs", "How should a template return the deployed VM's resource ID to a pipeline?", "Declare an output whose value uses the VM resource's symbolic ID or resourceId expression.", "Outputs are the supported deployment result contract for downstream automation."),
    tuple("arm-template-parameters", "Which Azure CLI command pattern deploys `main.json` with a production parameter file at resource-group scope?", "Use `az deployment group create --resource-group <rg> --template-file main.json --parameters @prod.parameters.json`.", "The group deployment command applies a local ARM template and parameter file to one resource group."),
    tuple("arm-export-template", "Which Azure CLI operation exports the current resource-group template?", "Use `az group export --name <resource-group>` and then review the generated JSON.", "Resource-group export retrieves a reconstructed ARM template for resources in that group."),
    tuple("arm-template-syntax", "Which Azure CLI operation performs preflight validation of a resource-group template?", "Use `az deployment group validate` with the same template and parameters intended for deployment.", "Validate checks template and preflight constraints without creating the deployment resources."),
    tuple("arm-template-parameters", "What is the boundary between an ARM parameter and a variable?", "A parameter is supplied by the deployer; a variable is calculated inside the template from stable values and expressions.", "Parameters expose configuration, while variables reduce repeated internal expressions."),
    tuple("arm-export-template", "What is the boundary between an exported template and canonical infrastructure source?", "An export approximates current deployed configuration; canonical source is intentionally parameterized, reviewed, versioned, and proven redeployable.", "Export is recovery or discovery input, not automatic source-of-truth quality."),
  ],
  "AZ104-N05-B03": [
    tuple("bicep-existing", "A Bicep file needs a subnet ID from a VNet managed by another team without redeploying the VNet. What should it declare?", "Declare the VNet and subnet with `existing` at their real scope and reference the subnet resource ID.", "Existing declarations provide typed references to resources owned outside the current deployment."),
    tuple("bicep-modules", "Networking must be reusable across several applications with a narrow input/output contract. Which Bicep construct fits?", "Move networking declarations into a module with explicit parameters and outputs.", "A module encapsulates a deployable Bicep file behind a defined deployment contract."),
    tuple("bicep-loops", "A Bicep file receives an array of subnet objects. How should it create one subnet per object?", "Use a resource loop over the array and derive each subnet name and properties from the current item.", "Bicep iteration maps structured collection elements to repeated resource declarations."),
    tuple("bicep-condition", "A bastion host is required only in production. How should the Bicep declaration express this?", "Apply `if (environment == 'prod')` to the bastion resource or its module and guard consumers.", "A conditional declaration keeps one source file while varying the desired resources by input."),
    tuple("bicep-modules", "Which sequence extracts a storage account into a Bicep module?", "Define module parameters and outputs, move the resource declaration, call the module with a unique deployment name and correct scope, update consumers to module outputs, then build and what-if.", "The parent must explicitly pass inputs and consume stable outputs from the nested deployment."),
    tuple("bicep-existing", "Which sequence references an existing Key Vault in another resource group?", "Declare the other resource group as scope, declare the vault with `existing` at that scope, verify its name, then use its typed ID or properties without declaring it for creation.", "Both name and scope identify the existing resource."),
    tuple("bicep-loops", "Which sequence replaces repeated module calls with a Bicep module loop?", "Create a stable configuration array, loop the module over each item with a unique deployment name, pass item-specific parameters and scope, consume indexed outputs deliberately, then preview identity changes.", "Stable item identity prevents collection edits from unintentionally remapping deployments and resources."),
    tuple("bicep-decompile", "Which sequence migrates a JSON ARM template to maintainable Bicep?", "Run decompile, build and lint the output, resolve diagnostics, compare generated ARM behavior, refactor symbols/modules, then what-if and test deploy before replacing the source.", "Decompilation preserves as much structure as possible but cannot supply design review."),
    tuple("bicep-existing", "An existing resource reference returns NotFound although the resource exists. What should be checked?", "Check the exact resource name, parent path, subscription, resource group, and scope on the existing declaration.", "Existing does no discovery; it resolves the identity supplied by the declaration."),
    tuple("bicep-scopes", "A Bicep file declares `targetScope = 'subscription'` but is deployed with `az deployment group create`. Why does it fail?", "The command selects resource-group scope, which does not match the file's subscription target scope.", "Bicep compilation and ARM deployment enforce the declared deployment scope."),
    tuple("bicep-condition", "Bicep warns that a conditional resource may be null when its property is accessed. What is required?", "Guard the access with the same condition or use the null-forgiving operator only when the logic truly guarantees existence.", "The false branch creates no resource, so unconditional dereference is unsafe."),
    tuple("bicep-modules", "A parent file cannot read a value calculated inside a module. What is missing?", "Declare the value as a module output and reference `moduleSymbol.outputs.<name>` from the parent.", "Module internals are encapsulated; outputs are the explicit return contract."),
    tuple("bicep-decompile", "Which command converts `azuredeploy.json` to a Bicep starting point?", "Run `az bicep decompile --file azuredeploy.json` and review the generated Bicep.", "The decompile command performs best-effort ARM JSON to Bicep conversion."),
    tuple("bicep-overview", "Which command compiles `main.bicep` to ARM JSON for inspection?", "Run `az bicep build --file main.bicep`.", "Bicep build emits the ARM template representation used by deployment."),
    tuple("bicep-modules", "Which Bicep syntax invokes a local module file?", "Declare `module <symbol> './module.bicep' = { name: ... params: { ... } }`.", "A module declaration creates a nested deployment from another Bicep file."),
    tuple("bicep-existing", "Which syntax marks a storage account as referenced but not deployed?", "Use `resource stg 'Microsoft.Storage/storageAccounts@<api-version>' existing = { name: storageName }`.", "The existing keyword changes the declaration from resource deployment to typed lookup."),
    tuple("bicep-loops", "Which Bicep construct creates resources from every item in `subnets`?", "Use an array loop such as `[for subnet in subnets: { ... }]` on the resource body.", "The for-expression expands the declaration once per collection item."),
    tuple("bicep-scopes", "Which command deploys a subscription-scope Bicep file?", "Use `az deployment sub create --location <region> --template-file main.bicep` with required parameters.", "Subscription deployments require the subscription command and a deployment metadata location."),
    tuple("bicep-existing", "What is the boundary between a normal Bicep resource and an existing resource?", "A normal declaration creates or updates desired state; an existing declaration only references a resource already managed elsewhere.", "Using existing preserves the external ownership boundary but still requires read access and correct identity."),
    tuple("bicep-modules", "What is the boundary between a Bicep module and a copied resource declaration?", "A module is a nested deployment with parameters, outputs, name, and scope; a resource loop repeats one declaration within the current file and scope context.", "Choose modules for reusable contracts and loops for repeated instances of the same pattern."),
  ],
  "AZ104-N05-B04": [
    tuple("arm-what-if", "A production template change may remove resources under complete mode. What should be run before approval?", "Run what-if at the exact deployment scope with the production parameters and review every predicted Delete and Modify result.", "What-if previews reconciliation effects without applying the deployment."),
    tuple("arm-deployment-errors", "A deployment fails with RequestDisallowedByPolicy. What is the correct next action?", "Inspect the policy assignment and definition identified in the error, then change the resource declaration or obtain an approved policy exemption.", "Azure Policy denies the control-plane request before retrying the same noncompliant payload can succeed."),
    tuple("arm-deployment-errors", "A VM deployment reports OperationNotAllowed because the regional vCPU quota is exhausted. What should be changed?", "Request the appropriate VM-family quota increase or deploy a supported size/region within available quota.", "Quota is a subscription and region constraint independent of template syntax."),
    tuple("arm-what-if", "A pipeline requires human confirmation after previewing a resource-group deployment. Which deployment behavior fits?", "Use the deployment create operation with `--confirm-with-what-if` and review the predicted changes before confirmation.", "The option combines a what-if preview with an explicit continue decision."),
    tuple("arm-deployment-errors", "Which sequence diagnoses a failed ARM deployment?", "Open deployment operations, find the first failed resource, read its error code and inner details, verify scope/policy/quota/dependencies, correct the cause, validate and what-if, then redeploy.", "The failing operation contains the most specific evidence needed for a safe correction."),
    tuple("arm-what-if", "Which sequence safely rolls out a high-risk template update?", "Use the intended identity and parameters to validate, run what-if at the real scope, investigate deletes and replacements, obtain approval, deploy, then verify deployment operations and resource health.", "Preflight, preview, approval, execution, and post-deployment verification are distinct controls."),
    tuple("arm-deployment-errors", "Which sequence responds to a transient dependency provisioning failure?", "Confirm the dependency's effective state and error, correct or wait for that prerequisite, validate the dependent declaration, then rerun the idempotent deployment and verify both operations.", "A safe redeployment follows evidence that the prerequisite is now healthy."),
    tuple("arm-deployment-errors", "Validation fails with InvalidTemplate before any deployment history entry appears. What does this indicate?", "The template structure or expression is invalid and must be corrected before provider operations can start.", "Syntax and template-validation errors occur before resource deployment execution."),
    tuple("arm-deployment-errors", "The deployment identity is Contributor on the VM resource group but a template also creates a role assignment. Why does authorization fail?", "Contributor cannot create role assignments; the identity needs Microsoft.Authorization/roleAssignments/write at the assignment scope.", "Role assignment operations require authorization permissions beyond ordinary resource contribution."),
    tuple("arm-deployment-errors", "A child resource fails with ParentResourceNotFound while the parent is declared in the same template. What should be inspected?", "Inspect the child name/parent identity and ensure a symbolic or explicit dependency makes the parent available first.", "A wrong parent path or missing dependency can make the provider evaluate the child before its required parent exists."),
    tuple("arm-deployment-errors", "A deployment repeatedly reports AnotherOperationInProgress for the same VM. What should be done?", "Inspect the active operation, wait for or resolve it, then retry one idempotent deployment instead of launching concurrent changes.", "Many resource providers serialize conflicting operations on the same resource."),
    tuple("arm-what-if", "What-if marks deeply nested resources Ignore after expansion limits are reached. How should this be interpreted?", "Treat those resources as not fully analyzed and review or split the deployment before approval.", "Ignore can reflect analysis limits and is not proof of no change."),
    tuple("arm-what-if", "Which Azure CLI command previews a resource-group deployment without applying it?", "Run `az deployment group what-if --resource-group <rg> --template-file main.bicep --parameters ...`.", "The group what-if command predicts changes at resource-group scope."),
    tuple("arm-deployment-errors", "Which Azure CLI command lists operations for a failed resource-group deployment?", "Run `az deployment operation group list --resource-group <rg> --name <deployment-name>`.", "Deployment operations expose each resource operation and its provisioning error."),
    tuple("arm-what-if", "What is the boundary between validate and what-if?", "Validate checks whether a deployment request is structurally and preflight-valid; what-if additionally predicts changes relative to current resources.", "Neither applies the deployment, and neither guarantees every provider runtime operation will succeed."),
    tuple("arm-deployment-errors", "What is the boundary between a validation error and a deployment error?", "Validation errors occur before resource changes; deployment errors arise while providers execute operations and appear in deployment operation evidence.", "The stage determines where to look and whether partial resource changes may already exist."),
  ],
  "AZ104-N05-B05": [
    tuple("vm-sizes", "A memory-bound database needs high memory per vCPU but no GPU. Which sizing approach is appropriate?", "Choose a memory-optimized VM family after validating required RAM, vCPU, disk/network limits, region availability, and quota.", "VM families are designed for workload ratios and must also satisfy platform limits."),
    tuple("vm-sizes", "A machine-learning inference workload requires an NVIDIA GPU. What must drive the VM choice?", "Select a supported GPU VM family and image/driver combination in a region and zone with quota and capacity.", "Accelerated workloads require a size exposing the correct GPU hardware and compatible software stack."),
    tuple("vm-generation-2", "A security design requires UEFI Secure Boot and vTPM through Trusted Launch. Which image characteristic is required?", "Use a compatible Generation 2 image and VM size that supports the required Trusted Launch features.", "UEFI-based security capabilities depend on Generation 2 virtual hardware and compatible images."),
    tuple("vm-overview", "A VM must have no direct internet exposure but administrators connect through Azure Bastion. Should it receive a public IP?", "No; attach its NIC to the private subnet without a public IP and use Bastion for the management path.", "Bastion provides browser-based administrative connectivity without assigning a public IP to the target VM."),
    tuple("vm-boot-diagnostics", "A new VM reaches a failed boot state after an image update. Which built-in evidence should be enabled?", "Use boot diagnostics to inspect serial log output and the console screenshot.", "Boot diagnostics observes guest startup before ordinary network access may be available."),
    tuple("vm-overview", "A Linux VM requires key-based administration. How should the administrator credential be configured?", "Supply the administrator's SSH public key and protect the corresponding private key outside Azure deployment data.", "The VM stores the public key; possession of the private key authenticates the administrator."),
    tuple("vm-sizes", "A workload needs four NICs, but the proposed size supports only two. What should be done?", "Choose a size that supports at least four NICs and still meets performance, region, and quota requirements.", "Maximum NIC count is a VM-size capability enforced at deployment."),
    tuple("vm-overview", "An application requires durable data across redeployments. Where should that data be stored?", "Store it on managed data disks or an external durable data service, not the temporary disk.", "The temporary disk can be reset during host movement and lifecycle operations."),
    tuple("vm-resize", "A running VM needs more memory, and the target size is available only on another cluster. What should be planned?", "Deallocate the VM, resize it to the compatible target size, then start and verify it.", "Deallocation releases the current cluster placement so Azure can allocate capacity that supports the target size."),
    tuple("vm-overview", "Which sequence creates a private Linux VM with managed identity?", "Create or select the VNet/subnet and NSG, create the NIC without public IP, choose image/size/zone and SSH key, enable managed identity and boot diagnostics, deploy, assign least-privilege roles, then verify private access.", "Networking, compute, identity, diagnostics, and authorization are separate VM dependencies."),
    tuple("vm-generation-2", "Which sequence selects a VM generation for a custom image?", "Inspect the source image partition/boot model, choose a compatible Gen1 or Gen2 image definition, validate required security features and size support, then create a matching VM and test boot.", "Image generation and VM virtual hardware must agree from initial creation."),
    tuple("vm-resize", "Which sequence performs a production VM resize?", "Check target size capabilities, regional availability and family quota, record downtime needs, stop/deallocate if required, resize, start, then verify guest, NIC, disk, and performance behavior.", "Resize success depends on capacity and feature compatibility, not vCPU count alone."),
    tuple("vm-sizes", "A resize fails with a quota error even though the target size exists in the region. What effective state should be checked?", "Check available quota for the target VM family in that region and request an increase if needed.", "Regional SKU availability does not imply the subscription has sufficient family quota."),
    tuple("vm-resize", "The portal does not list the desired target size for a running VM, but the region supports it. What is a likely reason?", "The current cluster cannot provide that size; deallocation may expose additional compatible regional sizes.", "A running VM's resize list is constrained by the hardware cluster currently hosting it."),
    tuple("vm-boot-diagnostics", "A VM has no SSH connectivity and its boot screenshot shows an operating-system recovery prompt. Where is the primary fault domain?", "Investigate guest boot and OS disk state rather than changing the NSG first.", "Boot diagnostics shows the guest did not reach normal operating-system readiness."),
    tuple("vm-overview", "Which Azure CLI command pattern creates a Linux VM with an SSH key while avoiding password authentication?", "Use `az vm create ... --admin-username <user> --ssh-key-values <public-key>` or `--generate-ssh-keys` for the authorized key path.", "Azure CLI can place an SSH public key into the guest administrator account during VM creation."),
    tuple("vm-generation-2", "What is the boundary between Generation 1 and Generation 2 VMs?", "Generation 1 uses legacy BIOS-style virtual hardware; Generation 2 uses UEFI and supports capabilities such as Secure Boot when the image and size support them.", "The generation is an image and creation choice rather than an in-place performance tier."),
    tuple("vm-states-billing", "What is the boundary between Stopped and Stopped (deallocated)?", "Stopped can remain allocated and compute-billed; deallocated releases the compute allocation, while disks, IPs, and other resources can still incur charges.", "Guest power state and Azure allocation state have different billing and placement consequences."),
    tuple("vm-overview", "What is the boundary between an OS image and a VM extension?", "An image supplies the base bootable operating-system state; an extension applies managed post-deployment configuration through the VM agent.", "Images favor immutable baseline reuse, while extensions have an independent install and update lifecycle."),
    tuple("vm-boot-diagnostics", "What is the boundary between boot diagnostics and application monitoring?", "Boot diagnostics captures early guest console evidence; application monitoring observes workload behavior after the OS and agents are running.", "They answer different failure-stage questions and are complementary."),
  ],
  "AZ104-N05-B06": [
    tuple("vm-disk-types", "A latency-sensitive database needs independently configurable high IOPS and throughput on supported VMs. Which disk should be evaluated?", "Evaluate Ultra Disk or Premium SSD v2, then verify region, zone, VM size, redundancy, and workload support.", "Those disk offerings expose provisioned performance controls beyond capacity-based default tiers."),
    tuple("vm-disk-types", "A lightly used archive-processing VM needs low-cost durable disks and tolerates high latency. Which tier is most aligned?", "Use Standard HDD if its availability and performance envelope meet the workload.", "Standard HDD optimizes cost for latency-tolerant, infrequently accessed I/O."),
    tuple("vm-disk-caching", "A read-heavy data disk contains immutable reference data. Which host-cache mode can improve reads without caching writes?", "Use ReadOnly host caching when the disk type and workload support it.", "ReadOnly caching serves repeated reads from the host while sending writes to durable storage."),
    tuple("vm-encryption-at-host", "A compliance requirement includes encryption of temporary disks and host-side disk caches. Which VM feature addresses this?", "Enable encryption at host on a supported VM size and region.", "Encryption at host extends platform encryption to host caches and temporary disk data."),
    tuple("managed-disks-overview", "Which sequence attaches a new empty data disk to a Linux VM?", "Create or select a managed disk, attach it at an unused LUN with an appropriate cache setting, identify it in the guest, partition/format it, mount it, and configure persistent mounting by stable identifier.", "Azure attachment exposes the block device; guest storage initialization makes it usable."),
    tuple("managed-disks-overview", "Which sequence safely detaches a data disk?", "Stop application writes, unmount or offline the volume in the guest, verify no handles or I/O remain, detach the managed disk in Azure, then retain or delete it according to policy.", "Guest quiescence prevents filesystem corruption before control-plane detachment."),
    tuple("managed-disk-snapshots", "Which sequence restores a VM data disk from a snapshot?", "Create a managed disk from the selected snapshot in a compatible location/zone, stop or quiesce the workload, attach or swap the disk, mount it in the guest, then validate data and application state.", "Snapshots are read-only sources from which writable managed disks are created."),
    tuple("managed-disks-overview", "Which sequence expands a managed data disk and its filesystem?", "Confirm backup and size limits, increase the managed disk size, rescan it in the guest, extend the partition or volume, grow the filesystem, then verify capacity and performance tier.", "Azure disk size and guest filesystem size are separate layers."),
    tuple("vm-disk-caching", "Which sequence changes a database data disk from ReadWrite to None safely?", "Review workload guidance, quiesce I/O, apply the cache change with any required VM restart/deallocation, start the workload, then verify consistency and benchmark effective performance.", "Caching changes affect both I/O semantics and end-to-end performance."),
    tuple("vm-encryption-at-host", "Which sequence enables encryption at host on an existing supported VM?", "Verify feature, region and size support, deallocate the VM if required, enable the encryption-at-host property, start the VM, and verify the effective model and disk state.", "The property is enforced at compute-host placement and can require reallocation."),
    tuple("managed-disks-overview", "A detached disk is missing after its VM was deleted. What should be inspected?", "Inspect the disk's delete-with-VM setting and the deletion operation evidence.", "Managed disks are independent resources, but delete options can intentionally couple their lifecycle to the VM."),
    tuple("vm-disk-caching", "A disk benchmark is below the disk SKU's advertised maximum. What other limit must be checked?", "Check the VM size's aggregate uncached and cached storage IOPS/throughput limits and workload queue depth.", "Effective storage performance is capped by both the disk and the VM path."),
    tuple("vm-disk-types", "A Premium SSD v2 disk cannot be created in the selected zone. What should be checked?", "Check regional and zonal support, selected redundancy, and VM/disk compatibility, then choose a supported placement or tier.", "Disk capabilities and placement support vary by region and zone."),
    tuple("vm-encryption-at-host", "Encryption-at-host enablement is rejected for a VM size. What is the likely cause?", "The selected size or regional deployment does not support encryption at host, or the VM has not followed the required deallocation path.", "Azure validates compute capability and lifecycle state before applying host encryption."),
    tuple("managed-disks-overview", "Which Azure CLI command family attaches an existing managed disk to a VM?", "Use `az vm disk attach` with the VM and managed disk identifiers plus the intended LUN/cache settings.", "The disk attach operation adds the managed disk to the VM storage profile."),
    tuple("managed-disk-snapshots", "Which Azure CLI command family creates a managed snapshot from a disk?", "Use `az snapshot create --source <disk-id>` with the destination resource group and snapshot name.", "A snapshot resource captures the selected managed disk's point-in-time state."),
    tuple("vm-disk-types", "What is the boundary between a disk performance tier and VM storage limits?", "The disk tier caps that disk's IOPS and throughput; the VM size caps aggregate traffic that can pass through its storage path.", "Provisioning a faster disk cannot exceed the compute size's end-to-end limit."),
    tuple("managed-disk-snapshots", "What is the boundary between a managed disk and its snapshot?", "A managed disk is attachable writable block storage; a snapshot is a read-only point-in-time resource used to create disks or copies.", "Restoration materializes a new writable disk rather than attaching the snapshot itself."),
  ],
  "AZ104-N05-B07": [
    tuple("vm-availability-sets", "Two VMs must tolerate a single host or rack-like failure in a region without using zones. Which placement fits?", "Place both VMs in the same availability set so Azure distributes them across fault domains.", "Fault domains separate instances across underlying power and network failure boundaries within the set."),
    tuple("vm-availability-sets", "An application must remain available during planned host maintenance. What availability-set property provides staggered maintenance grouping?", "Deploy multiple instances across update domains in an availability set.", "Azure processes planned maintenance by update domain so another instance can remain available."),
    tuple("vm-availability-zones", "A regional web tier must survive loss of one datacenter group. Which compute placement is required?", "Deploy at least two instances across different availability zones and load-balance them.", "Separate zones provide isolated datacenter groups within the region."),
    tuple("vm-availability-zones", "A stateful service needs low-latency synchronous replicas but must survive a zone failure. What design is appropriate?", "Place replicas in multiple zones of the same supported region and validate the application's cross-zone latency and quorum design.", "Zonal distribution provides isolation, while the application must remain correct across cross-zone links."),
    tuple("vm-availability", "A single VM has premium disks but the workload requires continuity during VM failure. What should be changed?", "Deploy multiple application instances using a supported availability construct and redundant dependencies.", "Durable storage does not make one compute instance highly available."),
    tuple("vm-availability-zones", "A zonal VM depends on a public endpoint that must remain available after any zone loss. What should be selected?", "Use a zone-redundant supported frontend such as a Standard Load Balancer or other service configuration, rather than a single-zone dependency.", "End-to-end availability requires the traffic path to survive the same failure boundary as compute."),
    tuple("vm-availability", "A legacy workload cannot use zones and must document the failure boundary of its redundancy design. What should be confirmed before deployment?", "Confirm that a supported region and VM configuration allow multiple instances in one availability set, and document that the set does not protect against an entire zone or regional loss.", "An availability set provides fault and update-domain distribution within its regional datacenter-scale boundary, not zone or region isolation."),
    tuple("vm-availability-zones", "Three replicas must be intentionally pinned one per zone. What placement model should be used?", "Create each zonal VM in a different supported zone and configure the application/load balancer across all three.", "Zonal resources remain assigned to the selected logical zone."),
    tuple("vm-availability-sets", "Which sequence deploys a two-VM availability-set web tier?", "Create the availability set, deploy both compatible VMs into it, attach them to redundant networking/load balancing, deploy the application, then verify fault/update-domain distribution and health.", "The availability set must be selected when the VMs are created and application traffic must use both instances."),
    tuple("vm-availability-zones", "Which sequence deploys a zone-resilient VM tier?", "Verify regional zone and SKU support, choose multiple zones, deploy at least one instance per zone with zone-resilient disks/networking, configure load balancing and health probes, then test loss of an instance/zone path.", "Compute placement alone is insufficient without compatible dependencies and health-based routing."),
    tuple("vm-availability", "Which sequence chooses between an availability set and zones?", "Identify the required failure boundary, check region/SKU/dependency support, evaluate latency and cost, choose zones for datacenter isolation or a set for nonzonal fault/update domains, then deploy multiple instances and test failover.", "The architecture follows the required isolation boundary and actual service support."),
    tuple("vm-availability-sets", "A second VM was created outside the existing availability set and cannot be added through an edit. Why?", "Availability-set membership is a placement decision made when the VM is created, so the VM must be recreated or migrated through a supported process.", "Azure must place the VM according to the set's fault/update-domain model at allocation time."),
    tuple("vm-availability-zones", "A zonal deployment works in one subscription but zone 1 lacks the same SKU in another. What should be checked?", "Check the subscription's logical-zone mapping, SKU availability, quota, and capacity rather than assuming zone labels are physically equivalent.", "Logical zone numbers need not map to the same datacenter group across subscriptions."),
    tuple("vm-availability", "All web VMs are healthy, but the single-zone database is unavailable during a zone incident. What is the architecture flaw?", "The data tier is a single-zone dependency and limits end-to-end availability.", "A multi-zone compute tier cannot exceed the resilience of its critical downstream services."),
    tuple("vm-availability-sets", "Which Azure CLI option places a new VM in an existing availability set?", "Use `az vm create --availability-set <set-name-or-id>` when creating the VM.", "Availability-set placement is supplied during VM creation."),
    tuple("vm-availability-sets", "What is the boundary between a fault domain and an update domain?", "A fault domain groups shared physical failure exposure; an update domain groups instances that may undergo planned maintenance together.", "One models unplanned infrastructure failure, while the other sequences planned platform updates."),
    tuple("vm-availability-zones", "What is the boundary between a zonal resource and a zone-redundant resource?", "A zonal resource is pinned to one zone; a zone-redundant service replicates or distributes across zones under the service's platform contract.", "Zonal placement gives location control, while zone redundancy provides service-managed multi-zone continuity."),
    tuple("vm-availability", "What is the boundary between an availability set and an availability zone design?", "A set distributes VMs across fault/update domains within a datacenter-scale boundary; zones distribute resources across isolated datacenter groups within a region.", "Zones protect against a broader failure boundary when the workload and dependencies support them."),
  ],
  "AZ104-N05-B08": [
    tuple("vmss-orchestration", "A scale set needs a mix of VM sizes and standard VM resource APIs. Which orchestration mode should be evaluated?", "Use Flexible orchestration if all required workload features are supported.", "Flexible mode manages standard Azure VM resources and supports heterogeneous instance configurations."),
    tuple("vmss-orchestration", "A stateless workload needs a large pool of identical instances from one scale set model. Which mode is aligned?", "Use Uniform orchestration when its identical-instance model and feature set meet the workload.", "Uniform mode optimizes management of homogeneous scale set-specific VM instances."),
    tuple("vmss-autoscale", "Capacity must rise when average CPU exceeds 70% for 10 minutes and later fall below 30%. What should be configured?", "Create separate scale-out and scale-in autoscale rules with appropriate aggregation, durations, steps, bounds, and cooldowns.", "Complementary thresholds with hysteresis adjust capacity while limiting oscillation."),
    tuple("vmss-autoscale", "A batch workload must run ten instances every weekday at 08:00 regardless of CPU. Which autoscale capability fits?", "Create a recurring schedule profile that sets the required capacity for that time window.", "Autoscale profiles can select capacity by schedule independently of metric thresholds."),
    tuple("vmss-upgrades", "Application owners must approve exactly when a new scale set model reaches existing instances. Which policy fits?", "Use Manual upgrade policy and explicitly upgrade selected instances after validation.", "Manual policy separates model update from instance rollout."),
    tuple("vmss-health", "Rolling upgrades must stop when the application is not ready even though the OS responds. What health signal should be used?", "Configure Application Health to probe an endpoint that represents real application readiness.", "Upgrade safety requires application-level health rather than only VM power or agent state."),
    tuple("vmss-orchestration", "Which sequence creates a scale set with the right orchestration model?", "Define workload homogeneity, instance API, availability and feature needs, choose Flexible or Uniform before creation, select image/network/zones, deploy a small capacity, then verify instance lifecycle and scaling.", "Orchestration mode cannot be switched after creation, so selection precedes deployment."),
    tuple("vmss-autoscale", "Which sequence configures CPU autoscale?", "Set minimum/maximum/default capacity, choose the scale set metric and aggregation, add sustained scale-out and lower scale-in thresholds with actions and cooldowns, save the setting, then load-test and inspect autoscale history.", "Bounds, signal evaluation, actions, and stabilization all determine effective behavior."),
    tuple("vmss-autoscale", "Which sequence adds a scheduled overnight scale-in without overriding daytime metric scaling incorrectly?", "Create time-bounded profiles with explicit capacity bounds and metric rules as needed, check profile precedence and timezone, simulate transition times, then monitor resulting capacity.", "Autoscale selects an active profile before evaluating that profile's rules."),
    tuple("vmss-upgrades", "Which sequence performs a controlled rolling model update?", "Publish the new model, configure rolling batch and pause settings, provide a valid health signal and startup grace, begin rollout, monitor each batch, and stop or roll back on unhealthy instances.", "Rolling policy limits simultaneous disruption using health-gated batches."),
    tuple("vmss-upgrades", "Which sequence updates instances under Manual policy?", "Update the scale set model, identify out-of-date instances, upgrade a canary instance, validate it, then explicitly upgrade remaining instances in controlled groups.", "Manual policy leaves existing instances on their prior model until an upgrade action is requested."),
    tuple("vmss-health", "Which sequence configures the Application Health extension?", "Choose an HTTP/HTTPS/TCP readiness endpoint, deploy the extension with protocol/port/path and grace settings, verify per-instance health, then enable repair or rolling behavior that consumes the signal.", "The platform must receive a trustworthy per-instance health state before using it for lifecycle decisions."),
    tuple("vmss-scale-in", "Which sequence protects a stateful instance during temporary scale-in?", "Mark that instance protected from scale-in, drain or migrate its state, remove protection when safe, then allow autoscale and verify which instance is selected.", "Instance protection defers deletion while workload state is made disposable."),
    tuple("vmss-autoscale", "Which sequence prevents autoscale flapping for a slow-starting application?", "Separate out/in thresholds, require sustained evaluation, set a cooldown longer than effective startup, use meaningful aggregation, and load-test transitions before production.", "Hysteresis and cooldown let new capacity affect the measured signal before another action."),
    tuple("vmss-autoscale", "Autoscale never triggers although portal CPU charts look high. What should be inspected?", "Inspect the autoscale setting's metric namespace/resource, aggregation, statistic, time grain, threshold duration, active profile, and evaluation history.", "A chart can visualize a different aggregation or scope than the autoscale rule evaluates."),
    tuple("vmss-upgrades", "A scale set model changed, but instances still show the old configuration under Manual policy. Why?", "The model update does not modify existing instances until they receive an explicit upgrade action.", "Manual policy intentionally decouples desired model publication from instance application."),
    tuple("vmss-health", "Rolling upgrade pauses because every new instance reports unhealthy during its first minute. What should be corrected?", "Use an adequate health-extension grace period and make the endpoint report healthy only after initialization completes.", "Startup time must be represented in health gating so normal initialization is not treated as rollout failure."),
    tuple("vmss-scale-in", "Autoscale requests scale-in but capacity cannot fall because all instances are protected. What is the cause?", "Scale-in protection makes every instance ineligible for deletion.", "Autoscale cannot satisfy a lower capacity target without a removable instance."),
    tuple("vmss-autoscale", "Which Azure CLI command family creates an autoscale setting for a VM scale set?", "Use `az monitor autoscale create` for the setting and `az monitor autoscale rule create` for its scale-out and scale-in rules.", "Azure Monitor autoscale resources hold capacity profiles and metric rules for the scale set."),
    tuple("vmss-upgrades", "Which Azure CLI operation upgrades selected Uniform scale set instances to the latest model?", "Use `az vmss update-instances --instance-ids ...` after updating the scale set model.", "The instance update operation applies the current model to instances under manual control."),
    tuple("vmss-orchestration", "What is the boundary between Flexible and Uniform orchestration?", "Flexible manages standard VM resources and can support heterogeneous placement; Uniform manages scale-set-specific instances from a homogeneous model with a different feature surface.", "The resource model and feature compatibility drive the creation-time selection."),
    tuple("vmss-upgrades", "What is the boundary between a scale set model update and an instance upgrade?", "The model update changes desired configuration; the instance upgrade applies that model to already provisioned VMs according to the selected policy.", "Publishing desired state and rolling it out are separate lifecycle events."),
  ],
  "AZ104-N05-B09": [
    tuple("azure-move-resources", "A VM move is blocked by a resource lock on its current scope. What should be done before retrying the move?", "Record the inherited lock, obtain owner approval, remove or adjust the lock at its defining scope, then revalidate the supported dependency set before moving.", "A management lock can block the move even when resource types and dependencies are otherwise supported; protection changes require explicit approval and post-move restoration."),
    tuple("azure-move-resources", "A VM must move to another subscription in the same tenant. What additional risk must be planned?", "Plan for changed resource IDs, destination quotas/provider registration, permissions, dependency support, and consumers that cache the old IDs.", "Subscription is part of every resource ID and destination control-plane state."),
    tuple("vm-move-region", "A VM must relocate from East US to West US. Can an ARM resource move change its location in place?", "No; use Azure Resource Mover or a supported replication/recreation process to prepare and cut over resources in the target region.", "A cross-region move creates target-region resources rather than editing the immutable location."),
    tuple("vm-redeploy", "A VM is unresponsive because of suspected host failure, but its managed disks are healthy. Which recovery action should be considered?", "Redeploy the VM to a new Azure host after preserving any temporary-disk data and accepting downtime.", "Redeploy retains managed disks and configuration while changing the underlying compute node."),
    tuple("azure-move-resources", "Which sequence moves a VM dependency set to another resource group?", "Inventory the VM, NICs, disks and referenced resources, validate move support and destination, obtain source/destination permissions, stop conflicting changes, submit one move, then update IDs, automation and access checks.", "Dependency validation and post-move reference repair prevent an apparently successful move from breaking operations."),
    tuple("vm-move-region", "Which sequence moves a VM with Azure Resource Mover?", "Add and validate dependencies, prepare target resources, initiate the move, test the target and plan downtime, commit after approval, then discard or clean source artifacts only after verification.", "Prepare, initiate, commit, and cleanup are explicit stages with a testable cutover boundary."),
    tuple("vm-extensions", "Which sequence deploys an extension that requires a secret?", "Verify VM agent/egress, select publisher/type/version, place normal configuration in settings and the secret in protectedSettings, deploy, inspect instance status and handler logs, then rotate the secret through an updated protected configuration.", "Extension packages and secrets have different visibility and lifecycle requirements."),
    tuple("vm-redeploy", "The guest OS is unresponsive, Azure reports the VM allocated, and no host fault is indicated. What is the first lifecycle action?", "Restart the VM through Azure and inspect boot diagnostics; reserve redeploy for evidence of an underlying host problem after restart troubleshooting.", "A restart tests guest and platform recovery without changing host placement; redeploy is a separate recovery action for suspected host faults and causes temporary-disk loss."),
    tuple("vm-extensions", "An extension must be updated after a handler configuration change. What maintenance sequence is appropriate?", "Review current instance status and logs, apply the new version or settings with secrets in protectedSettings, wait for provisioning success, then validate the guest change before removing the old configuration.", "Extension updates are asynchronous and should be verified through instance status and handler logs before declaring the guest configuration healthy."),
    tuple("vm-extensions", "An extension remains in Transitioning and the VM cannot reach Azure endpoints. What should be fixed?", "Restore the VM agent's required outbound connectivity and inspect agent/handler logs before retrying the extension.", "Extensions depend on a healthy agent and access to required package and control endpoints."),
    tuple("vm-run-command", "Run Command fails immediately on a running VM. Which dependency should be inspected?", "Inspect Azure VM agent provisioning/health and its outbound connectivity.", "Run Command is delivered and executed through the VM agent channel."),
    tuple("azure-move-resources", "A subscription move validation reports a dependent public IP was omitted. What is the correction?", "Include the supported dependent resource in the same move request or redesign the dependency before moving.", "ARM blocks moves that would leave an unsupported dependency graph."),
    tuple("vm-resize", "A resize succeeds but storage throughput remains unchanged. Why can this happen?", "The attached disk tiers or workload pattern may remain the bottleneck even though the VM storage cap increased.", "Effective performance is limited by the slowest component across VM, disk, cache, and application I/O."),
    tuple("vm-redeploy", "A VM works after redeploy, but files formerly on the temporary disk are gone. Is this expected?", "Yes; redeploy changes host placement and resets ephemeral temporary storage.", "Temporary disk data is not durable across host-moving lifecycle operations."),
    tuple("vm-run-command", "Which Azure CLI command executes a named Run Command operation on a VM?", "Use `az vm run-command invoke` with the VM, resource group, command ID, and any script or parameters.", "The invoke operation submits a script action through the VM agent."),
    tuple("vm-redeploy", "Which Azure CLI operation redeploys a VM to a new host?", "Use `az vm redeploy --resource-group <rg> --name <vm>` after acknowledging downtime and temporary-disk loss.", "The redeploy operation requests new host placement for the existing VM resource."),
    tuple("azure-move-resources", "What is the boundary between a resource-group/subscription move and a regional move?", "An ARM move changes resource scope and IDs while supported resources remain in their region; a regional move prepares new resources in another location and cuts workloads over.", "The mechanisms, downtime, dependency handling, and rollback boundaries differ."),
    tuple("vm-run-command", "What is the boundary between Run Command and a VM extension?", "Run Command performs an on-demand script action through the agent; an extension is a versioned handler resource with persistent provisioning state and configuration lifecycle.", "Use Run Command for bounded recovery/administration and extensions for managed post-deployment capabilities."),
  ],
};

const curriculum = JSON.parse(await readFile(curriculumPath, "utf8"));
const slotOrdinal = (slot) => Number(slot.slotId.match(/-(\d+)$/)?.[1] ?? Number.NaN);
const operationToMix = {
  decision_selection: "scenarioDecision",
  configuration_sequence: "configurationSequence",
  decision_diagnosis: "troubleshootingEffectiveState",
  tool_selection: "toolOrIacCommand",
  boundary_analysis: "boundaryOrContrast",
};

const seenPrompts = new Set();
const seenWrongTriples = new Set();
const allCards = [];
const customWrongTriples = new Map([
  ["The guest OS is unresponsive, Azure reports the VM allocated, and no host fault is indicated. What is the first lifecycle action?", [
    W("Redeploy immediately to a new host without checking boot diagnostics.", "Redeploy changes host placement and is not the first action when no host fault is indicated."),
    W("Delete the VM and its managed disks before testing a restart.", "Deletion destroys recoverable state and is not an appropriate first response to guest unresponsiveness."),
    W("Resize the VM to a different SKU as a substitute for restart troubleshooting.", "Changing size does not test guest recovery and can introduce quota, feature, or availability constraints."),
  ]],
  ["An extension must be updated after a handler configuration change. What maintenance sequence is appropriate?", [
    W("Resize the VM before applying the extension configuration.", "A VM resize does not update an extension handler or its settings."),
    W("Move the resource group and treat the extension as updated.", "Changing resource-group scope does not apply extension configuration or handler versions."),
    W("Edit an NSG or route table without checking VM Agent and extension status.", "Network changes alone do not perform the extension update and can obscure the actual handler state."),
  ]],
  ["An extension remains in Transitioning and the VM cannot reach Azure endpoints. What should be fixed?", [
    W("Redeploy the VM immediately without inspecting the extension or agent state.", "Redeploy changes host placement but does not diagnose missing agent egress or handler failure."),
    W("Change the VM SKU to a larger size.", "A larger SKU does not restore VM Agent connectivity or correct extension provisioning."),
    W("Move the VM to another resource group.", "Resource-group scope has no effect on extension handler execution or required outbound endpoints."),
  ]],
  ["A VM move is blocked by a resource lock on its current scope. What should be done before retrying the move?", [W("Grant Owner and retry without changing the lock.", "Owner does not bypass an applicable management lock."), W("Delete the resource group to clear the lock.", "Deleting the group is destructive and is not a safe way to remove the lock."), W("Move only the VM and leave the locked dependency set behind.", "A move still requires the supported dependency graph and the lock state to be resolved first.")]],
  ["A VM must move to another subscription in the same tenant. What additional risk must be planned?", [W("Assume resource IDs and cached consumer references remain unchanged.", "Subscription changes alter resource IDs and can break consumers."), W("Ignore destination quotas and provider registration.", "The destination subscription must have capacity and registered providers."), W("Move the VM without checking dependent resources.", "Supported dependencies and permissions must be evaluated together.")]],
  ["A VM must relocate from East US to West US. Can an ARM resource move change its location in place?", [W("Edit the location field on the existing VM.", "Azure resource location is not changed by an in-place property edit."), W("Move only the VM object and retain regional dependencies in East US.", "A regional move requires preparing supported dependencies in the target region."), W("Rename the resource group to change the region.", "Resource-group naming does not move resources between regions.")]],
  ["A VM is unresponsive because of suspected host failure, but its managed disks are healthy. Which recovery action should be considered?", [W("Delete the managed disks before investigating the host.", "Managed disks should be retained for recovery."), W("Run a guest script without a functioning VM channel.", "A host-level recovery action is needed when the VM is unresponsive."), W("Change the resource group and expect a new host.", "Scope changes do not relocate the VM to another host.")]],
  ["Which sequence moves a VM dependency set to another resource group?", [W("Move the VM first and discover dependencies afterward.", "Dependencies must be validated before the move."), W("Change the VM's region as part of the resource-group move.", "A resource-group move keeps supported resources in their region."), W("Delete the source resource group after submitting an unvalidated request.", "The source must remain available until the move is verified.")]],
  ["Which sequence moves a VM with Azure Resource Mover?", [W("Edit the VM location and skip target preparation.", "Resource Mover creates and prepares target resources rather than editing location."), W("Commit immediately before validating dependencies and target health.", "Commit is the cutover boundary after preparation and testing."), W("Delete source resources before the target is verified.", "Cleanup follows successful validation, not preparation.")]],
  ["Which sequence deploys an extension that requires a secret?", [W("Put the secret in normal settings visible to readers.", "Secrets belong in protected settings."), W("Skip VM Agent and outbound connectivity checks.", "The agent and required egress are prerequisites for extension execution."), W("Declare the secret in deployment outputs for the handler.", "Outputs can expose sensitive values and are not the extension secret channel.")]],
  ["A subscription move validation reports a dependent public IP was omitted. What is the correction?", [W("Proceed and repair the public IP after deleting the source.", "The dependency graph must be valid before the move."), W("Change the public IP's region instead of including it.", "A regional change does not satisfy the omitted dependency."), W("Move only the VM and let ARM recreate the public IP implicitly.", "ARM does not silently recreate omitted dependent resources.")]],
  ["Run Command fails immediately on a running VM. Which dependency should be inspected?", [W("Redeploy the VM without checking the VM Agent.", "Run Command is delivered through the VM Agent, so agent health must be inspected first."), W("Resize the VM to a larger SKU.", "VM size does not repair an unhealthy agent or its command channel."), W("Move the VM to another resource group.", "Resource-group scope does not provide the guest execution channel required by Run Command.")]],
  ["A resize succeeds but storage throughput remains unchanged. Why can this happen?", [W("Assume the VM size removes all disk and workload limits.", "Disk tier, cache, workload pattern, or VM caps can remain bottlenecks."), W("Delete and recreate the data disk to increase throughput.", "Disk recreation is not a general performance control and risks data loss."), W("Move the VM to another resource group to change disk performance.", "Resource-group scope does not change disk IOPS or throughput limits.")]],
  ["A VM works after redeploy, but files formerly on the temporary disk are gone. Is this expected?", [W("Restore temporary-disk files from the managed OS disk automatically.", "Temporary-disk data is not copied to durable disks by redeploy."), W("Move the resource group to recover the files.", "Resource scope changes cannot restore ephemeral temporary-disk data."), W("Increase the VM size to make the temporary disk durable.", "Temporary storage remains ephemeral regardless of VM size.")]],
  ["Which Azure CLI command executes a named Run Command operation on a VM?", [W("Use `az vm redeploy` to execute the named script.", "Redeploy changes host placement and does not submit a Run Command script."), W("Use `az vm extension set` for every on-demand command.", "Extensions are persistent resources; Run Command uses its invoke operation."), W("Use `az group move` with the command ID.", "Resource moves do not execute guest scripts.")]],
  ["Which Azure CLI operation redeploys a VM to a new host?", [W("Use `az vm run-command invoke` and expect host reassignment.", "Run Command executes a guest script but does not request new host placement."), W("Use `az vm resize` to force redeployment without downtime.", "Resize is a sizing operation and may require deallocation; it is not redeploy."), W("Use `az group move` to another resource group.", "Resource-group moves do not change the VM's underlying host.")]],
  ["What is the boundary between a resource-group/subscription move and a regional move?", [W("Treat both as an in-place location edit with unchanged IDs.", "Scope moves and regional moves have different IDs and mechanisms."), W("Use a resource-group move to change a resource's region.", "Resource-group/subscription moves retain region for supported resources."), W("Delete the source before testing either target.", "Both workflows require validation before destructive cleanup.")]],
  ["What is the boundary between Run Command and a VM extension?", [W("Use an extension for one-off recovery scripts only.", "Run Command is the on-demand script channel."), W("Use Run Command as a persistent versioned post-deployment handler.", "Extensions provide persistent handler lifecycle and provisioning state."), W("Use either without a healthy VM Agent or required egress.", "Both mechanisms depend on the VM Agent channel and connectivity.")]],
]);
for (const [blockId, cards] of Object.entries(blocks)) {
  const plan = curriculum.blockPlans.find((entry) => entry.blockId === blockId);
  const slots = curriculum.slots.filter((slot) => slot.blockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  if (!plan || slots.length !== cards.length) throw new Error(`${blockId}: planned ${slots.length}, authored ${cards.length}.`);
  const actualMix = {};
  slots.forEach((slot) => { actualMix[operationToMix[slot.learningOperation]] = (actualMix[operationToMix[slot.learningOperation]] ?? 0) + 1; });
  if (JSON.stringify(actualMix) !== JSON.stringify(plan.questionMix)) throw new Error(`${blockId}: question mix does not match canonical slots.`);
  const pool = pools[`B${blockId.slice(-2)}`];
  const triples = combinations(pool.length);
  cards.forEach((card, index) => {
    if (!sourceById.has(card.source)) throw new Error(`${blockId}-${index + 1}: unknown source ${card.source}.`);
    if (seenPrompts.has(card.prompt)) throw new Error(`Duplicate prompt: ${card.prompt}`);
    seenPrompts.add(card.prompt);
    card.wrongs = customWrongTriples.get(card.prompt) ?? triples[index].map((wrongIndex) => pool[wrongIndex]);
    const tripleKey = card.wrongs.map((wrong) => wrong.text).sort().join(" || ");
    if (seenWrongTriples.has(tripleKey)) throw new Error(`Repeated distractor triple: ${tripleKey}`);
    seenWrongTriples.add(tripleKey);
    if (new Set(card.wrongs.map((wrong) => wrong.text)).size !== 3 || card.wrongs.some((wrong) => !wrong.text || !wrong.why)) throw new Error(`${blockId}-${index + 1}: invalid distractors.`);
    if (!card.prompt || !card.correct || !card.reason) throw new Error(`${blockId}-${index + 1}: incomplete card.`);
    allCards.push(card);
  });
}
if (allCards.length !== 168 || Object.keys(blocks).length !== 9) throw new Error(`Expected 168 cards in 9 blocks, received ${allCards.length}.`);

if (process.argv.includes("--check")) {
  console.log(`AZ104-N05 slot-order, mix, source, prompt, and distractor QA passed for ${allCards.length} cases across ${Object.keys(blocks).length} blocks.`);
  process.exit(0);
}

const registrySources = sourceSpecs.map(({ boundary, transfer, ...source }) => source);
const sourceRecords = new Map(curriculum.sourceRecords.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = sourceRecords.get(source.sourceId);
  sourceRecords.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
curriculum.sourceRecords = [...sourceRecords.values()].sort((a, b) => a.sourceId.localeCompare(b.sourceId));

for (const [blockId, cards] of Object.entries(blocks)) {
  const slots = curriculum.slots.filter((slot) => slot.blockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  slots.forEach((slot, index) => {
    const requirement = slot.sourceRequirements.directFirstPartyDocumentation[0];
    requirement.testedProperties = [`${blockId.toLowerCase()}-${slot.learningOperation}-${String(index + 1).padStart(2, "0")}`];
    requirement.sourceRefs = [cards[index].source];
    requirement.resolutionState = "resolved_exact_direct";
  });
  const target = curriculum.targetPlans.find((entry) => entry.blockId === blockId);
  if (!target) throw new Error(`${blockId} target plan is missing.`);
  target.sourceRefs = [...new Set(cards.map((card) => card.source))].sort();
}

const activeSourceIds = new Set();
for (const slot of curriculum.slots) {
  for (const sourceRef of slot.sourceRequirements.officialObjective?.sourceRefs ?? []) activeSourceIds.add(sourceRef);
  for (const requirement of slot.sourceRequirements.directFirstPartyDocumentation ?? []) for (const sourceRef of requirement.sourceRefs ?? []) activeSourceIds.add(sourceRef);
}
const collectBlueprintSources = (value) => {
  if (Array.isArray(value)) return value.forEach(collectBlueprintSources);
  if (!value || typeof value !== "object") return;
  for (const [key, child] of Object.entries(value)) key === "sourceRefs" && Array.isArray(child) ? child.forEach((sourceRef) => activeSourceIds.add(sourceRef)) : collectBlueprintSources(child);
};
collectBlueprintSources(curriculum.examSimulationBlueprint);
curriculum.sourceRecords = curriculum.sourceRecords.filter((source) => activeSourceIds.has(source.sourceId));
const { promotionProvenance, contentFingerprint, ...fingerprintPayload } = curriculum;
curriculum.contentFingerprint = createHash("sha256").update(JSON.stringify(fingerprintPayload)).digest("hex");
await writeFile(curriculumPath, `${JSON.stringify(curriculum, null, 2)}\n`);

const anchorRegistry = JSON.parse(await readFile(anchorsPath, "utf8"));
const anchorSources = new Map(anchorRegistry.anchors.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = anchorSources.get(source.sourceId);
  anchorSources.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
const retainedSourceIds = new Set(curriculum.sourceRecords.map((source) => source.sourceId));
anchorRegistry.anchors = [...anchorSources.values()].filter((source) => retainedSourceIds.has(source.sourceId)).sort((a, b) => a.sourceId.localeCompare(b.sourceId));
await writeFile(anchorsPath, `${JSON.stringify(anchorRegistry, null, 2)}\n`);

const { manifest, model } = await buildManifest(ROOT);
const manifestTrack = manifest.tracks.find((entry) => entry.trackId === TRACK);
const canonicalTrack = model.curricula.get(TRACK);
const normalizedById = new Map(canonicalTrack.normalized.slots.map((slot) => [slot.slotId, slot]));
const optionIds = ["A", "B", "C", "D"];

for (const [blockId, cards] of Object.entries(blocks)) {
  const batchId = `az104-2026-08-16-${blockId}`;
  const provenance = { authoringMethod: "manual", approvalStatus: "unapproved", author: "Codex", createdAt: CREATED_AT, contentBatchId: batchId };
  const slots = manifestTrack.slots.filter((slot) => slot.learningBlockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  const items = slots.map((slot, index) => {
    const card = cards[index];
    const normalized = normalizedById.get(slot.slotId);
    if (!slot.authoringAdmitted || !slot.sourceBinding) throw new Error(`${slot.slotId} is not authoring admitted.`);
    const spec = sourceById.get(card.source);
    const rawOptions = [{ text: card.correct, correct: true, why: card.reason }, ...card.wrongs.map((wrong) => ({ ...wrong, correct: false }))];
    const shift = index % 4;
    const arranged = [...rawOptions.slice(shift), ...rawOptions.slice(0, shift)];
    const answer = arranged.findIndex((option) => option.correct);
    if (!slot.sourceBinding.sourceRefs.includes(card.source)) throw new Error(`${slot.slotId} does not bind ${card.source}.`);
    return {
      itemId: `az104-${blockId}-${String(index + 1).padStart(3, "0")}`,
      slotId: slot.slotId,
      nodeId: NODE,
      learningBlockId: blockId,
      taxonomy: { examDomainId: normalized.raw.primarySimulationDomainId, competencyAreaId: NODE, topicId: blockId, skillAtomId: normalized.raw.directSkillOrDecisionAtomId },
      prompt: card.prompt,
      constraints: [spec.boundary, spec.transfer],
      interaction: { type: "choice", selectionMode: "single", options: arranged.map((option, optionIndex) => ({ optionId: optionIds[optionIndex], text: option.text })), acceptedOptionIds: [optionIds[answer]] },
      scoringContract: { type: "choice", resultSemantics: "exact_selected_set_with_partial_v1", selectionMode: "single" },
      feedback: {
        Reason: card.reason,
        Details: { mechanismOrProperty: card.reason, scenarioApplication: card.correct, errorCorrection: arranged.filter((option) => !option.correct).map((option) => option.why).join(" "), boundaryOrTradeoff: spec.boundary, transfer: spec.transfer, url: spec.url },
        wrongOptionExplanationsByOptionId: Object.fromEntries(arranged.map((option, optionIndex) => [optionIds[optionIndex], option.why]).filter((_, optionIndex) => optionIndex !== answer)),
        omittedCorrectElementExplanationsByOptionId: {},
      },
      modeEligibility: slot.modeEligibility,
      sourceBinding: slot.sourceBinding,
      authoringProvenance: provenance,
    };
  });
  const payload = { schemaVersion: "certification-manual-source-v2", batchId, trackId: TRACK, familyId: "certification", contentVersion: "microsoft-azure-administrator-associate-az-104-authoring-v2026.08.15", taxonomyVersion: "2026.08.15", nodeId: NODE, learningBlockId: blockId, slotIds: items.map((item) => item.slotId), items, authoringProvenance: provenance };
  const output = join(ROOT, "manual/source", TRACK, NODE, `${blockId}.json`);
  await mkdir(join(output, ".."), { recursive: true });
  await writeFile(output, `${JSON.stringify(payload, null, 2)}\n`);
  console.log(`Authored ${items.length} reviewed items at ${output}.`);
}
