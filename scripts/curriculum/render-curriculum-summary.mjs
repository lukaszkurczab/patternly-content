import { mkdir, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { ROOT } from "./curricula.mjs";
import { EVIDENCE_VERSION, auditCurricula } from "./audit-curricula.mjs";

const result = await auditCurricula();
const lines = ["# Curriculum coverage specification", "", `Machine-readable source: \`config/curricula/*.json\`. Catalogue fingerprint: \`${result.catalogue}\`.`, "", "## Release tracks", "", "| Track | Family | Nodes | Blocks | Targets | Slots/target | Existing | Authoring | Free | Premium | State |", "| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |"];
for (const entry of result.trackVolumes) { const certification = entry.familyId === "certification"; lines.push(`| ${entry.trackId} | ${entry.familyId} | ${entry.nodes.length} | ${entry.nodes.reduce((sum, node) => sum + node.blockCount, 0)} | ${certification ? result.curricula.find((item) => item.trackId === entry.trackId).targetPlans.length : "—"} | ${entry.targetItemCount} | ${entry.existingVerifiedItemCount} | ${entry.authoringItemCount} | ${entry.freeTargetItemCount} | ${entry.premiumTargetItemCount} | ${certification ? "direct slots; authoring not admitted" : "current family contract"} |`); }
lines.push("", "## Certification direct-slot reconciliation", "");
for (const entry of result.certificationSummary) lines.push(`- ${entry.trackId}: ${entry.slots} slots, ${entry.targets} targets, ${entry.blocks} blocks, ${entry.nodes} nodes, ${entry.sources} sources; Free/Premium ${entry.freeSlots}/${entry.premiumSlots}; ${entry.status}.`);
lines.push("", "## Design direct-slot provenance, mode, graph, and admission", "");
for (const entry of result.designSummary) lines.push(`- ${entry.trackId}: ${entry.slots} slots; exact/direct verified sources ${entry.provenance.exactDirectVerifiedSourceIds.length}; inventory slot sources ${entry.provenance.inventorySlotSourceIds.length}; editorial-method slot sources ${entry.provenance.editorialMethodSourceIds.length}; modes ${entry.mode.status}; graph edges ${entry.graph.relationshipEdgeCount} (${entry.graph.anchoredRelationshipCount} atom-anchored); runtime ${entry.admission.runtimeAdmission}, publishing ${entry.admission.publishingAdmission}.`);
lines.push("", "## Authoring boundary", "", "All Certification batches are future work: no questions were authored and runtime, package, publishing, and learner admission remain unavailable. Before authoring, establish the real track/taxonomy/source/release contract; bind each question to a canonical slot and coherent content version; then complete feedback, factual review, editorial approval, and immutable publication evidence.", "", "## First safe future batch", "");
const first = result.authoringHandoff.firstSafeBatch; lines.push(`- \`${first.batchId}\`: ${first.plannedItemCount} exact slots in ${first.blockIds.map((id) => `\`${id}\``).join(", ")}; ${first.executionStatus}. ${first.reviewBoundary}`);
lines.push("", "## Atomic authoring backlog", "");
for (const batch of result.authoringHandoff.authoringBatches) lines.push(`- \`${batch.batchId}\`: ${batch.plannedItemCount} slots; blocks ${batch.blockIds.map((id) => `\`${id}\``).join(", ")}; ${batch.batchSizeStatus}; ${batch.executionStatus}.`);
lines.push("", "## Audit", "", `Audit: ${result.defects.status}. ${result.defects.completionBoundary}`, "");
await mkdir(join(ROOT, "reports"), { recursive: true }); await writeFile(join(ROOT, "reports", "curriculum-summary.md"), `${lines.join("\n")}\n`); await writeFile(join(ROOT, "evidence", "curriculum", EVIDENCE_VERSION, "final-audit.md"), `${lines.join("\n")}\n`); process.stdout.write("Rendered reports/curriculum-summary.md.\n");
