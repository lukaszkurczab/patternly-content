# Algorithms activation ingress

Place only manual `ContentActivationRecord` JSON here after a human editorial approval exists for every exact item fingerprint. The publisher creates neither approval nor activation records.
