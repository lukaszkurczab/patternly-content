name: Content publishing architecture

on:
  push:
  pull_request:

jobs:
  architecture:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm ci
      - run: npm test
      - run: npm run authoring:validate
      - run: npm run audit:aws-workbook-source
      - run: npm run audit:gcp:authoring
      - run: npm run audit:ai901
      - run: npm run validate:backend-system-design
      - run: npm run validate:frontend-bank
      - run: npm run validate:object-oriented-design
      - run: npm run generate:seven-bank-candidate-readiness
      - run: git diff --exit-code -- evidence/readiness/seven-bank-candidate-readiness.json
      - name: Validate Coding Interview bundled Free node
        run: npm run validate:bundled-free-node -- --input artifacts/bundled-free-nodes/coding-interview-dsa-problem-solving/coding-interview-dsa-problem-solving-free-node-0001/package.json
      - run: node scripts/publishing/ci-release-gate.mjs
