import { createHash } from "node:crypto";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { buildManifest, ROOT } from "./lib/model.mjs";

const TRACK = "microsoft-azure-administrator-associate-az-104";
const NODE = "azure_virtual_networking_addressing_peering_routing_and_dns";
const CREATED_AT = "2026-08-16T00:00:00Z";
const curriculumPath = join(ROOT, "config/curricula", `${TRACK}.json`);
const anchorsPath = join(ROOT, "evidence/curriculum", `${TRACK}-source-anchors.json`);

const S = (sourceId, title, url, authority, boundary, transfer) => ({
  sourceId,
  provider: "Microsoft",
  sourceType: "direct_first_party_product_documentation",
  title,
  url,
  checkedDate: "2026-08-16",
  sourceVolatility: "high",
  versionContext: "current Microsoft Learn documentation checked for AZ104-N07 authoring",
  resolutionState: "resolved_exact_first_party",
  authoritativeFor: [authority],
  propertyAuthorityBasis: "manual_slot_property_review_2026_08_16",
  boundary,
  transfer,
});

const sourceSpecs = [
  S("vnet-overview", "What is Azure Virtual Network?", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-networks-overview", "vnet-address-space-and-subnet-model", "A VNet is a regional private network boundary whose subnets divide one or more nonoverlapping address spaces.", "Validate address overlap, subnet capacity, routing, DNS, and security independently before connecting networks."),
  S("vnet-manage-subnets", "Add, change, or delete a virtual network subnet", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-manage-subnet", "subnet-lifecycle-and-delegation", "Subnet prefixes must fit the VNet address space, must not overlap sibling subnets, and can be constrained by deployed resources or delegation.", "Inventory addresses, delegations, policies, and attached resources before changing a production subnet."),
  S("subnet-delegation-overview", "Subnet delegation in Azure Virtual Network", "https://learn.microsoft.com/en-us/azure/virtual-network/subnet-delegation-overview", "service-specific-subnet-delegation-model", "Each delegated Azure service defines whether it supports a shared or dedicated subnet and which policies it adds; delegation itself does not affect existing workloads.", "Check the selected service's documented deployment model, address minimum, private-endpoint compatibility, and network intent policies before delegating a populated subnet."),
  S("vnet-cli-reference", "az network vnet", "https://learn.microsoft.com/en-us/cli/azure/network/vnet?view=azure-cli-latest", "azure-cli-vnet-create-syntax", "The Azure CLI vnet create command accepts explicit VNet address prefixes plus an initial subnet name and subnet prefixes.", "Validate current Azure CLI syntax and region or subscription context before automating network creation."),
  S("vnet-ip-addresses", "IP addresses in Azure", "https://learn.microsoft.com/en-us/azure/virtual-network/ip-services/public-ip-addresses", "azure-public-ip-behavior", "Public IP resources provide internet-routable addressing with SKU, allocation, zone, and association semantics distinct from a NIC private IP.", "Choose an address resource from exposure, availability-zone, lifecycle, and outbound requirements rather than assuming every VM needs one."),
  S("nic-ip-config", "Configure IP addresses for an Azure network interface", "https://learn.microsoft.com/en-us/azure/virtual-network/ip-services/virtual-network-network-interface-addresses", "nic-private-ip-configuration", "Azure assigns private addresses through NIC IP configurations; static means Azure reserves the chosen subnet address for that configuration.", "Keep the address inside the subnet and coordinate guest configuration with Azure DHCP rather than assigning an untracked guest-only address."),
  S("public-ip-create", "Create a public IP address", "https://learn.microsoft.com/en-us/azure/virtual-network/ip-services/create-public-ip-portal", "public-ip-resource-configuration", "A Standard public IP is a separate resource whose allocation, zone, DNS label, and association are configured explicitly.", "Create and validate the public IP resource before associating it to a supported frontend or NIC."),
  S("public-ip-cli-reference", "az network public-ip", "https://learn.microsoft.com/en-us/cli/azure/network/public-ip?view=azure-cli-latest", "azure-cli-public-ip-create-syntax", "The Azure CLI public-ip create command documents the Standard SKU and availability-zone arguments used to create a public IP resource.", "Check region and SKU zone support before treating a zone list as a portable deployment setting."),
  S("nat-gateway-overview", "What is Azure NAT Gateway?", "https://learn.microsoft.com/en-us/azure/nat-gateway/nat-overview", "nat-gateway-outbound-connectivity", "NAT Gateway supplies explicit subnet-level outbound SNAT through associated public IP resources without permitting unsolicited inbound connections.", "Verify SKU compatibility, subnet association, public IP capacity, UDR precedence, and destination behavior before changing outbound design."),
  S("bastion-overview", "What is Azure Bastion?", "https://learn.microsoft.com/en-us/azure/bastion/bastion-overview", "bastion-private-vm-administration", "Azure Bastion provides managed RDP and SSH connectivity to VM private IPs through a dedicated AzureBastionSubnet without requiring public IPs on target VMs.", "Plan the supported SKU and subnet size, protect the Bastion entry path, and keep target VM NSG access scoped to the managed administration design."),
  S("vnet-peering", "Virtual network peering", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-peering-overview", "vnet-peering-and-gateway-transit", "Peering connects nonoverlapping VNets privately, is nontransitive, and exposes independent traffic-forwarding and gateway-transit controls on each direction.", "Check both peering objects, effective routes, forwarded-traffic flags, and gateway transit before diagnosing application policy."),
  S("vnet-peering-manage", "Create, change, or delete a virtual network peering", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-manage-peering", "peering-configuration-sequence", "A complete peering has local and remote configuration whose permissions, address spaces, and state must agree.", "Create or update both directions deliberately and verify Connected state after address-space or gateway changes."),
  S("vnet-peering-cli-reference", "az network vnet peering", "https://learn.microsoft.com/en-us/cli/azure/network/vnet/peering?view=azure-cli-latest", "azure-cli-peering-gateway-flags", "The Azure CLI peering create and update commands document allow-gateway-transit, use-remote-gateways, forwarded-traffic, and VNet-access flags.", "Apply provider and consumer gateway settings to the correct peering directions and verify the gateway exists before testing propagated routes."),
  S("vnet-routing", "Virtual network traffic routing", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-networks-udr-overview", "azure-route-selection-and-udr", "Azure selects routes by longest prefix, then uses documented preference when equal prefixes compete; UDRs can override many system routes.", "Inspect effective routes at the NIC and preserve symmetric return paths whenever an appliance changes forwarding."),
  S("route-table-create", "Create, change, or delete a route table", "https://learn.microsoft.com/en-us/azure/virtual-network/manage-route-table", "route-table-lifecycle", "A route affects workloads only after the route table is associated with their subnet and contains a valid prefix and next-hop configuration.", "Deploy route entries, associate the intended subnet, and verify effective routes before declaring the path changed."),
  S("route-table-route-cli-reference", "az network route-table route", "https://learn.microsoft.com/en-us/cli/azure/network/route-table/route?view=azure-cli-latest", "azure-cli-route-table-route-list-syntax", "The Azure CLI route-table route list command returns the administrator-defined routes contained in a named route table.", "Distinguish configured route entries from the combined effective routes applied at a workload NIC."),
  S("nva-routing", "Route network traffic with a route table", "https://learn.microsoft.com/en-us/azure/virtual-network/tutorial-create-route-table", "network-virtual-appliance-routing", "Routing through an NVA requires a VirtualAppliance next hop, appliance IP forwarding, guest forwarding, and a viable return route.", "Test both directions and appliance policy; a correct UDR alone does not make the VM forward packets."),
  S("hub-spoke", "Hub-spoke network topology in Azure", "https://learn.microsoft.com/en-us/azure/architecture/networking/architecture/hub-spoke", "hub-spoke-boundary", "Hub-spoke centralizes shared connectivity and services while spokes isolate workloads; peering itself does not create transitive spoke-to-spoke routing.", "Model routing, inspection, DNS, gateway transit, and ownership as explicit hub services rather than inferred peering behavior."),
  S("vpn-gateway", "What is Azure VPN Gateway?", "https://learn.microsoft.com/en-us/azure/vpn-gateway/vpn-gateway-about-vpngateways", "vpn-gateway-connectivity", "VPN Gateway provides encrypted cross-premises or VNet connectivity and requires a dedicated GatewaySubnet sized for the selected configuration.", "Plan prefixes, gateway SKU, active-active behavior, BGP, local network definitions, and on-premises devices as one path."),
  S("expressroute-overview", "ExpressRoute overview", "https://learn.microsoft.com/en-us/azure/expressroute/expressroute-introduction", "expressroute-connectivity-boundary", "ExpressRoute uses a provider circuit and private peering rather than an internet VPN, but does not automatically provide encrypted transport.", "Select private connectivity from bandwidth, availability, encryption, routing, provider, and lead-time requirements."),
  S("dns-overview", "What is Azure DNS?", "https://learn.microsoft.com/en-us/azure/dns/dns-overview", "azure-dns-public-hosting", "Azure DNS hosts authoritative public zones but is not a registrar; internet resolvers reach it only after parent-zone delegation.", "Separate registration, delegation, record content, TTL, and client caching when changing a public name."),
  S("dns-delegate", "Delegate an Azure DNS domain", "https://learn.microsoft.com/en-us/azure/dns/dns-delegate-domain-azure-dns", "dns-parent-delegation", "Delegation requires NS records at the registrar or parent that match the name servers assigned to the Azure DNS zone.", "Query the parent delegation and authoritative server directly before changing records that are not yet discoverable."),
  S("dns-records", "Manage DNS records and record sets in Azure DNS", "https://learn.microsoft.com/en-us/azure/dns/dns-operations-recordsets-portal", "dns-record-sets-and-aliases", "A record set groups records of one type and name with a TTL; the zone apex has special constraints and can use supported alias records.", "Choose record type from target semantics and account for TTL and resolver caches during cutover."),
  S("dns-a-cli-reference", "az network dns record-set a", "https://learn.microsoft.com/en-us/cli/azure/network/dns/record-set/a?view=azure-cli-latest", "azure-cli-public-dns-a-record-syntax", "The Azure CLI public DNS A record command group documents add-record with zone, record-set name, and IPv4 address arguments.", "Confirm public versus private DNS command group and the intended owner name before changing an authoritative record set."),
  S("dns-zone-cli-reference", "az network dns zone", "https://learn.microsoft.com/en-us/cli/azure/network/dns/zone?view=azure-cli-latest", "azure-cli-public-dns-zone-show-syntax", "The Azure CLI DNS zone show command returns the public zone resource including its assigned nameServers collection.", "Use the exact assigned server set from the intended zone and subscription when updating parent delegation."),
  S("dns-private-overview", "What is Azure Private DNS?", "https://learn.microsoft.com/en-us/azure/dns/private-dns-overview", "private-dns-zone-resolution", "Private DNS zones resolve only through linked VNets or a deliberate resolver path and are not published to the internet.", "Link every client VNet that should resolve the zone and design hybrid forwarding separately."),
  S("dns-private-links", "Virtual network links in Azure Private DNS", "https://learn.microsoft.com/en-us/azure/dns/private-dns-virtual-network-links", "private-dns-vnet-links", "A VNet link enables resolution and can optionally enable autoregistration; a VNet can register into only one private zone for a given DNS suffix model.", "Use resolution-only links broadly and enable registration only where automatic VM record ownership is intentional."),
  S("private-dns-link-cli-reference", "az network private-dns link vnet", "https://learn.microsoft.com/en-us/cli/azure/network/private-dns/link/vnet?view=azure-cli-latest", "azure-cli-private-dns-vnet-link-syntax", "The Azure CLI Private DNS VNet link create command documents zone, virtual-network, and registration-enabled arguments.", "Select the correct private zone and use registration only where the VNet should own automatic VM records."),
  S("dns-private-resolver", "What is Azure DNS Private Resolver?", "https://learn.microsoft.com/en-us/azure/dns/dns-private-resolver-overview", "hybrid-dns-private-resolver", "Private Resolver provides managed inbound and outbound endpoints plus forwarding rulesets for hybrid DNS without self-managed DNS VMs.", "Place endpoints in correctly delegated subnets, link rulesets to querying VNets, and avoid forwarding loops."),
  S("network-watcher-ip-flow", "IP flow verify overview", "https://learn.microsoft.com/en-us/azure/network-watcher/ip-flow-verify-overview", "ip-flow-verify", "IP flow verify evaluates whether a specified packet is allowed or denied by effective NSG rules for a VM NIC.", "Use the actual protocol, direction, local and remote addresses, and ports; then inspect the named winning rule."),
  S("network-watcher-next-hop", "Next hop overview", "https://learn.microsoft.com/en-us/azure/network-watcher/next-hop-overview", "network-watcher-next-hop", "Next hop reports the effective route and next-hop type selected for a VM-to-destination path.", "Use it to prove routing selection, then test security and application reachability as separate layers."),
  S("network-watcher-ip-flow-cli-reference", "az network watcher test-ip-flow", "https://learn.microsoft.com/en-us/cli/azure/network/watcher?view=azure-cli-latest#az-network-watcher-test-ip-flow", "azure-cli-ip-flow-verify-syntax", "The Azure CLI Network Watcher test-ip-flow command documents direction, protocol, local and remote address, and port inputs for effective NSG evaluation.", "Use the actual packet tuple and preserve the returned rule evidence before changing policy."),
  S("network-watcher-next-hop-cli-reference", "az network watcher show-next-hop", "https://learn.microsoft.com/en-us/cli/azure/network/watcher?view=azure-cli-latest#az-network-watcher-show-next-hop", "azure-cli-next-hop-syntax", "The Azure CLI Network Watcher show-next-hop command documents the source VM and source and destination IP inputs used for route diagnosis.", "Run the command against the actual source NIC path and compare its output with the full effective route table."),
  S("network-watcher-connection", "Connection troubleshoot overview", "https://learn.microsoft.com/en-us/azure/network-watcher/connection-troubleshoot-overview", "connection-troubleshoot", "Connection troubleshoot performs a point-in-time reachability test and reports path, latency, and failure information between supported endpoints.", "Pair the result with effective routes, NSGs, DNS, and endpoint health instead of treating one successful route as full application success."),
  S("effective-routes", "View effective routes for a virtual machine network interface", "https://learn.microsoft.com/en-us/azure/virtual-network/diagnose-network-routing-problem", "effective-route-diagnosis", "Effective routes combine system, peering, gateway-propagated, and user-defined routes at a NIC after provisioning completes.", "Inspect the actual NIC and destination prefix; a route table definition alone is not proof of the selected path."),
];

const sourceById = new Map(sourceSpecs.map((source) => [source.sourceId, source]));
const P = (value) => { const split = value.indexOf("|"); return { text: value.slice(0, split), why: value.slice(split + 1) }; };
const Q = (source, prompt, correct, reason, ...wrongs) => ({ source, prompt, correct, reason, wrongs: wrongs.map(P) });

const blocks = {
  "AZ104-N07-B01": [
    Q("vnet-overview", "A new VNet must contain application and database subnets, each needing 50 usable addresses, and must leave an equally sized subnet for growth. Which address plan is valid?", "Use a /24 VNet and allocate three nonoverlapping /26 subnets, leaving the remaining /26 available.", "A /24 divides into four /26 ranges; each Azure subnet retains enough usable addresses after platform reservations.", "Use a /26 VNet and define three /26 subnets inside it.|A /26 address space contains only one /26 and cannot contain three peers.", "Use a /25 VNet with three /26 subnets.|A /25 has room for only two nonoverlapping /26 ranges.", "Use three overlapping /26 subnets because network security groups isolate them.|Security policy cannot make overlapping prefixes valid in one VNet."),
    Q("vnet-manage-subnets", "A platform team needs a subnet that can grow from 20 to 180 private endpoints without changing its prefix. Which initial size is appropriate?", "Allocate at least a /24 subnet after accounting for Azure-reserved addresses and expected platform consumption.", "A /24 provides 256 total addresses and avoids an immediately undersized prefix for the stated growth.", "Allocate /28 because private endpoints share one subnet address.|Each private endpoint consumes its own private IP.", "Allocate /27 and add another disjoint subnet when full while keeping the same endpoint IPs.|Adding a subnet does not expand the existing subnet or move endpoints transparently.", "Allocate /32 because endpoints are single hosts.|Azure subnets cannot be modeled as a one-address host route."),
    Q("vnet-overview", "Two VNets will be peered later. VNet-A uses 10.20.0.0/16. Which proposed VNet-B address space preserves that option?", "Use 10.21.0.0/16 after checking it against every connected network.", "Peering requires nonoverlapping address spaces, and 10.21.0.0/16 does not overlap 10.20.0.0/16.", "Use 10.20.128.0/17 because it is a smaller prefix.|That range is contained within VNet-A's /16.", "Use 10.20.0.0/24 because only deployed subnets matter.|VNet address spaces themselves must not overlap for peering.", "Reuse 10.20.0.0/16 and rely on longest-prefix routing.|Identical address spaces make destinations ambiguous and block peering."),
    Q("bastion-overview", "Azure Bastion will be deployed to a VNet. How should its subnet be defined?", "Create a dedicated subnet named AzureBastionSubnet with a supported prefix size and no unrelated workloads.", "Azure Bastion discovers a specifically named dedicated subnet and requires adequate address capacity.", "Place Bastion in the application subnet and add a Bastion service endpoint.|Bastion does not use a service endpoint and needs its dedicated subnet.", "Name the subnet BastionGateway and delegate it to Microsoft.Web.|Neither the name nor delegation matches Bastion requirements.", "Deploy Bastion directly into GatewaySubnet to reuse gateway capacity.|GatewaySubnet is reserved for virtual network gateways."),
    Q("vnet-manage-subnets", "What sequence safely adds a subnet for 120 application instances to an existing VNet?", "Inventory current and connected prefixes, choose a nonoverlapping prefix with reservation and growth headroom, add the subnet, attach required security and routes, then deploy and verify.", "Address validation precedes subnet creation, and network controls should be ready before workloads depend on the new segment.", "Deploy instances first, then create any subnet that contains their assigned addresses.|NICs require an existing subnet and valid IP configuration.", "Add an overlapping prefix temporarily, move resources, then remove the overlap.|Azure rejects overlapping sibling subnets.", "Create the smallest possible subnet and depend on automatic in-place expansion.|Subnet prefixes do not grow automatically when capacity is exhausted."),
    Q("vnet-manage-subnets", "A /24 subnet must be expanded to /23, and the adjacent /24 is unused. What should be verified before changing the prefix?", "Verify the /23 fits the VNet, does not overlap another subnet, and that current resources and service constraints permit the prefix change.", "Free adjacent address space is necessary but deployed resources or subnet features can still constrain an update.", "Verify only that the route table has a /23 route.|Routing does not determine whether the subnet prefix is structurally valid.", "Delete the VNet address space first so the subnet can claim /23.|A subnet must remain contained within a VNet address space.", "Convert every NIC to a public IP before editing the subnet.|Public association is unrelated to subnet-prefix validity."),
    Q("vnet-manage-subnets", "A subnet will be delegated to Azure App Service VNet integration. What sequence is appropriate?", "Choose an empty, sufficiently sized subnet, add the required Microsoft.Web/serverFarms delegation, configure integration, then verify routes, DNS, and capacity.", "Delegated services require the documented delegation and may impose exclusivity and sizing constraints.", "Deploy VMs first and add the delegation after they share the subnet.|A delegated integration subnet should not contain unrelated VM workloads.", "Delegate the entire VNet because delegations apply at address-space scope.|Delegation is configured on a subnet.", "Add a private endpoint policy and assume that delegates the subnet.|Endpoint policy and service delegation are different controls."),
    Q("vnet-overview", "A company needs a new address space added to a VNet without disrupting existing subnets. What sequence is safest?", "Confirm the new CIDR does not overlap connected networks, add it to the VNet, create nonoverlapping subnets within it, update peerings or routing as required, then validate connectivity.", "The address space must be routable and visible to connected networks before new subnets carry production traffic.", "Create a subnet outside every VNet address space, then add its parent later.|Azure requires a subnet prefix to be contained at creation time.", "Reuse an on-premises prefix and hide it with an NSG.|Security rules cannot remove routing ambiguity from overlapping ranges.", "Delete and recreate the VNet because address spaces are immutable.|VNets support adding and removing address prefixes subject to constraints."),
    Q("vnet-manage-subnets", "What sequence removes an unused subnet from production safely?", "Confirm no NICs, private endpoints, delegated resources, or other attachments remain, remove associations deliberately, delete the subnet, then verify routes and automation no longer reference it.", "Azure blocks deletion while dependent resources remain, and stale deployment definitions can recreate or reference the subnet.", "Delete its route table first and assume that deletes the subnet.|A route-table association is not ownership of the subnet resource.", "Remove the subnet prefix from the VNet while NICs still use it.|In-use addresses prevent safe removal and would strand dependencies.", "Rename the subnet to deleted and leave resources attached.|A name change neither removes the segment nor its dependencies."),
    Q("vnet-overview", "An infrastructure template must create a VNet with front-end and back-end subnets. What dependency order should it express?", "Create the VNet address space, create each contained subnet with distinct prefixes and controls, then create NICs and services that reference those subnet IDs.", "Consumers need stable subnet resources whose prefixes already fit the parent address space.", "Create NICs with arbitrary subnet strings before defining the VNet.|NIC subnet references must resolve to existing or concurrently declared resources.", "Create both subnets with the full VNet prefix so each has maximum capacity.|Sibling subnet prefixes cannot overlap.", "Create public IPs as parent resources of the VNet.|Public IP resources do not establish subnet containment."),
    Q("vnet-manage-subnets", "A subnet prefix update fails with `InUseSubnetCannotBeDeleted`. What effective state should be checked first?", "Enumerate every resource and IP configuration attached to the subnet, including private endpoints and delegated service instances.", "The error indicates the subnet still has dependencies that prevent destructive prefix replacement or deletion.", "Check whether the subscription has a DNS zone.|DNS hosting does not explain an in-use subnet deletion error.", "Raise the public IP quota.|Public IP quota is unrelated to private subnet attachments.", "Disable all NSG rules.|Packet filtering does not release resources from a subnet."),
    Q("vnet-overview", "A VM in 10.30.1.0/24 cannot reach a VM in 10.30.2.0/24 in the same VNet after a subnet was added. Which structural check comes first?", "Confirm both prefixes are valid nonoverlapping subnets in the same VNet and inspect effective routes and security on both NIC paths.", "Same-VNet system routing normally connects subnets, so topology, effective route, and policy are the first layers to verify.", "Create VNet peering between the two subnets.|Peering connects VNets, not subnets within one VNet.", "Assign both VMs the same private IP so traffic remains local.|Duplicate private addresses are invalid and do not create routing.", "Add a public DNS CNAME between the VM names.|Name aliases do not establish packet reachability."),
    Q("vnet-manage-subnets", "A /29 subnet accepts fewer workloads than an engineer calculated from its eight addresses. Why?", "Azure reserves five addresses in every subnet, leaving only three assignable addresses in a /29.", "The first four and last address are reserved for platform functions, so raw CIDR size is not usable capacity.", "Azure reserves only the broadcast address, leaving seven usable.|Azure has additional platform reservations beyond the last address.", "The NSG consumes five addresses.|NSGs are policy resources and consume no subnet IPs.", "A route table consumes one address per route.|Routes do not allocate addresses from the subnet."),
    Q("subnet-delegation-overview", "A subnet already contains two VMs. An engineer plans to delegate it to an Azure PaaS service. What must be determined before assuming the VMs must be moved?", "Check that service's delegation model: each service defines shared versus dedicated subnet support, while delegation itself does not affect existing workloads.", "Subnet delegation is service-specific; an injected service may require a dedicated subnet or may support sharing, and its policies cannot modify existing workloads.", "Always evacuate every existing VM before adding any Azure service delegation.|A dedicated subnet is not a universal delegation rule; the selected service defines whether sharing is supported.", "Assume delegation converts the existing VMs into instances of the delegated service.|Delegation grants the service permission to inject its own resources and does not transform customer VMs.", "Enable a service endpoint because it has the same shared-versus-dedicated rules as delegation.|Service endpoints extend subnet identity to service public endpoints and do not define injected-service placement."),
    Q("vnet-cli-reference", "Which Azure CLI command creates a VNet and its first subnet in one operation?", "Run `az network vnet create --resource-group <rg> --name <vnet> --address-prefixes <vnet-cidr> --subnet-name <subnet> --subnet-prefixes <subnet-cidr>`.", "The VNet create command can declare the parent address space and initial child subnet together.", "Run `az network nic create --address-prefixes <vnet-cidr>`.|NIC creation does not define VNet address spaces.", "Run `az network route-table create --subnet-prefixes <subnet-cidr>`.|A route table contains routes but does not create a subnet.", "Run `az network public-ip create --subnet <subnet>`.|A public IP resource does not materialize a VNet or subnet."),
    Q("vnet-manage-subnets", "Which Azure CLI operation adds a new subnet to an existing VNet?", "Run `az network vnet subnet create --resource-group <rg> --vnet-name <vnet> --name <subnet> --address-prefixes <cidr>` with any required NSG, route table, or delegation settings.", "The vnet subnet command creates a child subnet under an existing VNet.", "Run `az network vnet peering create --address-prefixes <cidr>`.|Peering connects VNets and cannot add a subnet.", "Run `az network private-endpoint create --subnet-prefix <cidr>`.|A private endpoint consumes an existing subnet; it does not define one.", "Run `az network dns zone create --address-prefix <cidr>`.|DNS zones contain names, not IP prefix allocations."),
    Q("vnet-overview", "What is the boundary between a VNet address space and a subnet prefix?", "A VNet address space defines the regional private ranges owned by the VNet; each subnet is a nonoverlapping slice contained within one of those ranges.", "The parent range establishes ownership while subnets provide deployable segmentation.", "A subnet may use any RFC1918 prefix even when outside the VNet.|Every subnet must be contained in a declared VNet address space.", "The VNet prefix is an effective route table and the subnet is an NSG.|Address ownership and packet policy are separate constructs.", "A VNet can contain only one subnet per address space.|An address space can be divided into many nonoverlapping subnets."),
    Q("vnet-manage-subnets", "What is the boundary between subnet delegation and a service endpoint?", "Delegation authorizes a platform service to deploy or integrate resources into the subnet; a service endpoint extends subnet identity to a supported service's public endpoint.", "One controls service placement or integration, while the other changes the network path and service-side network identity.", "Both create a private IP for the service inside the subnet.|A service endpoint does not allocate a private endpoint NIC.", "Both reserve the subnet exclusively for one service.|Service endpoints can coexist with general subnet workloads.", "Delegation is an NSG rule and a service endpoint is a route table.|Neither feature is defined as those policy resources."),
  ],
  "AZ104-N07-B02": [
    Q("nic-ip-config", "A database VM must keep the same private address through stop and deallocate cycles. What should be configured?", "Set the NIC IP configuration to static and choose an available address within its subnet.", "Azure then reserves that private address for the NIC configuration independent of VM power state.", "Configure the address only inside the guest OS.|Azure may assign a different address and the control plane will not own the guest-only value.", "Attach a static public IP and use it as the private database address.|Public and private IP configurations solve different paths.", "Create a DNS A record without reserving the NIC address.|DNS does not prevent the underlying private address from changing."),
    Q("vnet-ip-addresses", "An internet-facing load balancer requires a stable, zone-resilient frontend address. Which resource is appropriate?", "Create a Standard public IP with the required zone configuration and associate it to the load balancer frontend.", "Standard public IP resources support explicit zone behavior and stable allocation for supported frontends.", "Assign a dynamic private IP to one backend NIC.|A backend private address is not the public frontend.", "Use a Basic public IP because it automatically spans all zones.|Basic public IP does not provide the requested Standard zone-resilient model.", "Publish the load balancer name in private DNS only.|Private DNS cannot create an internet-routable frontend."),
    Q("nat-gateway-overview", "Several VMs need predictable outbound internet egress without individual public IPs. Which design is preferred?", "Use a NAT Gateway with one or more Standard public IPs or a public IP prefix on the subnet.", "NAT Gateway centralizes scalable outbound SNAT for private workloads without inbound exposure per VM.", "Assign the same public IP directly to every NIC.|A public IP resource cannot be simultaneously associated to arbitrary independent NIC configurations.", "Create one DNS A record for all private VM addresses.|DNS naming does not perform outbound source NAT.", "Enable IP forwarding on every VM without an appliance.|Forwarding does not supply an internet egress public identity."),
    Q("bastion-overview", "A jump VM should be administered without exposing RDP or SSH on a VM public IP. Which Azure approach fits?", "Use Azure Bastion and leave the target VM NIC without a public IP.", "Bastion provides browser or native-client administration over the VM's private address.", "Assign a dynamic public IP only during business hours.|The VM is still directly internet-addressable while administration occurs.", "Use a public DNS alias to the private address.|Internet clients cannot route to an RFC1918 address through DNS.", "Enable a service endpoint for Microsoft.Compute.|Service endpoints do not provide interactive VM administration."),
    Q("public-ip-create", "A public service must retain its address when the backend VM is replaced. How should the address lifecycle be modeled?", "Create the public IP as an independent resource and associate it with the supported frontend so backend replacement does not recreate the address.", "Separating the public endpoint resource from the compute lifecycle preserves the allocated address.", "Let each replacement VM request a new dynamic public IP.|A newly allocated resource can receive a different address.", "Hard-code the desired internet address in the guest NIC settings.|Azure allocates public IPs through control-plane resources, not guest configuration.", "Use a private IP marked static and announce it to the internet.|Private VNet addresses are not public IP allocations."),
    Q("nic-ip-config", "A VM needs addresses in two application subnets. What must be true?", "Use multiple NICs or supported multiple IP configurations, ensuring each NIC attaches only to subnets in the same VNet and region constraints are satisfied.", "NIC and VM attachment rules constrain multi-address designs; one NIC cannot span subnets through a single IP configuration.", "Configure one IP configuration with two subnet IDs.|Each IP configuration references one subnet.", "Attach NICs from unrelated VNets directly to the same VM.|A multi-NIC VM's NICs must follow the documented common-VNet and regional constraints.", "Add two public DNS records and omit the second IP configuration.|DNS names cannot create NIC network attachment."),
    Q("public-ip-create", "What sequence creates and exposes a VM with a Standard public IP while minimizing accidental access?", "Create the Standard public IP, create the NIC and associate the IP, apply an NSG with only required inbound flows, attach the NIC to the VM, then test from an approved source.", "Address association and packet admission are separate steps, and Standard public IPs are secure by default without an allowing NSG rule.", "Create an allow-any NSG, then decide which service is needed after deployment.|Broad admission violates the stated minimal exposure goal.", "Configure the public IP inside the guest before Azure creates it.|The guest does not allocate the Azure public IP resource.", "Create a private DNS zone and expect it to open the inbound port.|Name resolution does not create an NSG allowance."),
    Q("nic-ip-config", "What sequence changes a NIC from dynamic to a chosen static private IP?", "Confirm the address is available in the NIC's subnet, update the Azure NIC IP configuration to static with that address, allow the guest to receive Azure DHCP configuration, then validate connectivity and DNS.", "Azure must own the reservation, while guest settings should remain consistent with the platform-assigned configuration.", "Set the guest address first and leave the Azure allocation dynamic.|The control plane can assign another address, creating mismatch or conflict.", "Move the NIC to another VNet during the same update.|NIC subnet and VNet moves are constrained and unnecessary for an allocation-method change.", "Create a public IP with the same numeric value.|Public and private address spaces are independently allocated."),
    Q("nic-ip-config", "A VM was configured with a static address only inside the guest and becomes unreachable after restart. What is the likely cause?", "The guest address does not match the private IP allocated to the NIC by Azure; reserve the intended address on the NIC and use Azure-provided configuration.", "Azure routes the NIC IP configuration, so an uncoordinated guest-only address can break return traffic.", "The public DNS TTL expired.|TTL expiry causes refresh, not a private NIC/guest mismatch.", "The VNet requires an ExpressRoute circuit for every static address.|Static private addressing works within an ordinary VNet.", "The VM needs a second public IP for DHCP.|Public association does not repair private address ownership."),
    Q("vnet-ip-addresses", "A Standard public IP is associated with a VM, but inbound traffic remains blocked. Which effective state should be checked?", "Check the NIC and subnet NSG rules and confirm a matching inbound allow rule exists for the service and source.", "Standard public IP association does not implicitly open inbound flows; security policy must admit them.", "Change the public IP allocation from static to dynamic.|Allocation mode does not create an NSG allow rule.", "Add the public IP to a private DNS zone.|Private DNS affects name resolution, not packet filtering.", "Enable IP forwarding on the VM NIC.|IP forwarding is for forwarding transit traffic, not accepting a local service."),
    Q("public-ip-cli-reference", "Which Azure CLI command creates a zone-redundant Standard public IP where supported?", "Run `az network public-ip create --resource-group <rg> --name <pip> --sku Standard --zone 1 2 3` in a region that supports the requested zones.", "The public-ip create command defines SKU and zone placement on the address resource.", "Run `az network nic ip-config create --zone-redundant` without a public IP.|A NIC IP configuration cannot invent a zone-redundant public address.", "Run `az vm open-port --zones 1 2 3`.|Open-port changes security rules and does not create public IP zone placement.", "Run `az network dns record-set a add-record --zone 1 2 3`.|DNS records do not allocate zone-resilient addresses."),
    Q("nic-ip-config", "What is the boundary between dynamic and static private IP allocation in Azure?", "Dynamic lets Azure choose and retain an available subnet address for the NIC until lifecycle changes release it; static lets the administrator select and reserve the specific subnet address on the NIC configuration.", "Both are Azure control-plane allocations; static does not mean configuring an arbitrary address only in the guest.", "Dynamic changes on every packet while static never routes through Azure.|Dynamic is an allocation method, not per-packet behavior.", "Static private IPs are internet-routable but dynamic ones are not.|Allocation method does not change private scope.", "Dynamic addresses cannot have DNS records.|DNS can point to either, although lifecycle stability affects record management."),
    Q("vnet-ip-addresses", "What is the boundary between a public IP resource and a NIC private IP?", "A public IP supplies an internet-routable endpoint or frontend association; a NIC private IP identifies the workload within its VNet subnet.", "Azure can translate or route between the two, but they have separate allocation and exposure semantics.", "A public IP is merely the private address displayed in hexadecimal.|They are distinct resources and address scopes.", "A private IP always provides implicit outbound and inbound internet access.|Private addressing alone does not guarantee either path.", "Both must use identical numeric addresses for NAT to work.|Public-to-private mapping does not require address equality."),
    Q("nat-gateway-overview", "What is the boundary between inbound public exposure and outbound SNAT?", "A public frontend or NIC association enables an addressable inbound endpoint when security permits; NAT Gateway supplies subnet outbound source translation without creating unsolicited inbound reachability.", "The direction and connection-initiation semantics differ even when both use public IP resources.", "NAT Gateway opens every inbound port on its public IP.|NAT Gateway is outbound-only for workload-initiated flows.", "A load balancer frontend automatically becomes the subnet's only outbound method.|Outbound behavior depends on explicit configuration and platform rules.", "DNS determines whether a public IP is inbound or outbound.|DNS names do not define NAT direction."),
  ],
  "AZ104-N07-B03": [
    Q("vnet-peering", "Two nonoverlapping VNets in different Azure regions need low-latency private connectivity over the Microsoft backbone. Which feature fits?", "Configure global VNet peering between the two VNets and validate any regional feature limitations.", "Global peering directly exchanges private VNet traffic without deploying a gateway for this requirement.", "Create matching public IPs on every VM.|Public addressing exposes endpoints and does not create a private VNet relationship.", "Add the same address range to both VNets.|Overlapping address spaces prevent peering.", "Create a private DNS link only.|DNS resolution does not provide network reachability."),
    Q("vnet-peering", "A hub VNet has a VPN gateway to on-premises. A spoke must use that gateway without deploying its own. What peering settings are required?", "Enable gateway transit on the hub-to-spoke peering and use remote gateways on the spoke-to-hub peering.", "Gateway transit is advertised by the gateway-owning hub and consumed by the spoke on the opposite peering direction.", "Enable use remote gateways on both sides.|The hub owns the gateway and cannot consume itself as a remote gateway.", "Enable forwarded traffic only and omit gateway settings.|Forwarded traffic does not advertise gateway routes to the spoke.", "Create a second GatewaySubnet in the spoke with no gateway.|An empty gateway subnet does not provide transit."),
    Q("vnet-peering", "Spoke-A and Spoke-B are each peered only with Hub. Can their VMs communicate automatically through the hub?", "No. VNet peering is nontransitive; configure explicit spoke peering or route through a forwarding appliance with the required peering and UDR settings.", "A shared hub peering does not automatically forward one spoke's traffic to another.", "Yes, because all peered networks form one broadcast domain.|Peering neither creates transitivity nor a shared broadcast domain.", "Yes, if both spokes use the same private DNS zone.|Common name resolution does not create a packet path.", "No, unless both spokes have public IPs.|Private connectivity can be built without public exposure."),
    Q("vnet-peering", "A security appliance in the hub must inspect spoke traffic sent from another peered VNet. Which peering capability is relevant?", "Allow forwarded traffic on the peering direction that must accept packets whose source is not the directly peered VNet, and configure UDRs and appliance forwarding.", "Forwarded-traffic permission complements routing when an NVA relays packets across peering.", "Enable VM autoregistration on the peering.|Autoregistration is a Private DNS VNet-link feature.", "Enable service endpoints on the hub subnet.|Service endpoints do not authorize transit packets over peering.", "Change the peering to a public IP association.|Peering remains a private VNet control."),
    Q("vnet-peering-manage", "What sequence creates a same-subscription VNet peering safely?", "Verify address spaces do not overlap, create or configure both peering directions with required access flags, confirm Connected state, inspect effective routes, then test traffic under NSG policy.", "Peering topology, route propagation, and security are distinct validation steps.", "Create one peering direction and assume Azure always creates the reverse.|Some tooling can create both, but a complete relationship still requires both peering objects.", "Attach one VNet as a subnet of the other.|VNets cannot be nested as subnets.", "Disable all NSGs before checking address overlap.|Security policy cannot make overlapping VNets peerable."),
    Q("vnet-peering-manage", "Two VNets are in different subscriptions and administrators control one subscription each. What sequence is required?", "Ensure both operators have required permissions, use the full remote VNet resource IDs, create each direction in its owning subscription, then verify both show Connected.", "Cross-subscription peering requires authorization and configuration on both VNet resources.", "Move both VNets into one resource group because peering cannot cross subscriptions.|Peering supports cross-subscription VNets when identities have permission.", "Share a storage account key between administrators.|Storage credentials do not authorize network resource changes.", "Create identical peering names and omit remote resource IDs.|Names alone cannot identify the peer across subscription scope."),
    Q("vnet-peering-manage", "A VNet address space changes after peering exists. What should operators do before using the new prefix remotely?", "Update or synchronize the peering so the remote side learns the new address space, verify Connected state and effective routes, then test the prefix.", "The remote peering view must reflect the changed address space before reliable routing to it.", "Change only a DNS A record to the new address.|DNS can expose the name while the route is still absent.", "Create an overlapping subnet in the peer to advertise the range.|Overlap is invalid and would create ambiguity.", "Delete every NSG because address synchronization is security-driven.|Peering address-space knowledge is independent of NSG rules."),
    Q("vnet-peering", "What sequence enables a spoke to consume a hub gateway?", "Confirm the hub gateway is provisioned, enable allow gateway transit on hub-to-spoke, enable use remote gateways on spoke-to-hub, ensure the spoke has no conflicting gateway, inspect routes, then test on-premises connectivity.", "The gateway owner exports transit while the consumer opts in and receives propagated routes.", "Enable remote gateways before any hub gateway exists and expect routes immediately.|There is no gateway route source to consume.", "Create a VPN gateway in each spoke and also select the hub remote gateway.|A VNet cannot use its own gateway and a remote gateway simultaneously.", "Configure only a DNS forwarder in the hub.|DNS forwarding cannot establish the gateway data path."),
    Q("vnet-peering", "Both peering objects show Connected, but traffic is blocked only from VNet-A to VNet-B. What should be inspected first?", "Inspect directional peering access flags, effective NSG rules, effective routes, and any forwarded-traffic requirement for the A-to-B path.", "Connected state proves the control relationship, not that directional policy permits the tested flow.", "Recreate both VNets with public ranges.|Address replacement is unjustified when peering is already Connected.", "Change the VM hostnames.|Names do not explain a directional packet denial after address resolution.", "Add a second peering from A to B with a different name.|Each VNet pair uses one peering object per direction; duplication is not a policy fix."),
    Q("vnet-peering", "A peered VNet route appears as Invalid in effective routes. Which change commonly explains this state?", "A more specific or preferred route, an address-space update, or a disabled peering can supersede the route; inspect route source, prefix, peering state, and recent changes.", "Effective-route status reflects the route selection and current network relationship, not just the original definition.", "The route is invalid because peering routes always require BGP.|Peering supplies system routes without requiring customer BGP.", "The VM needs a public IP to validate private routes.|Public association is unrelated to a peering route's validity.", "The DNS zone has no SOA record.|DNS authority does not determine route validity."),
    Q("vnet-peering", "Spoke traffic reaches the hub NVA but never arrives at another spoke. What peering state is most likely missing?", "The relevant peerings may not allow forwarded traffic, or the UDR/appliance return path is absent; verify all three together.", "Transit through an NVA requires permission to receive forwarded packets plus explicit symmetric routing.", "The destination spoke needs a public DNS zone.|Public DNS does not admit forwarded private packets.", "The NVA requires a public IP on every NIC.|An NVA can forward entirely on private addresses.", "The hub must use the same subnet prefix as the spokes.|Overlapping prefixes would prevent valid peering."),
    Q("vnet-peering", "A spoke cannot enable use remote gateways. The option reports that another remote gateway is already in use. Why?", "A VNet can use a remote gateway from only one peering, so an existing peering already owns that setting.", "Remote-gateway consumption is exclusive at the VNet level.", "Every subnet can independently choose a different remote gateway.|The setting belongs to VNet peering rather than individual subnets.", "The spoke needs two GatewaySubnet resources.|A VNet cannot contain duplicate subnets with the reserved name.", "The hub gateway SKU must be Basic for multiple spokes.|Gateway transit is not enabled by downgrading to Basic."),
    Q("vnet-peering-manage", "A peering remains Initiated instead of Connected. What effective state does this indicate?", "Only one direction is configured or the reverse peering cannot validate the relationship; create or repair the peer's peering object and permissions.", "Initiated commonly indicates the local side exists while the remote side has not completed the pair.", "The VM guest firewall rejected an application port.|Guest filtering does not set the peering control-plane state.", "The parent DNS delegation is stale.|Public DNS delegation does not establish VNet peering.", "The route table lacks a default route.|UDRs can affect traffic but not change Initiated to Connected."),
    Q("vnet-peering", "A spoke uses a remote hub gateway, but on-premises learns no route to a newly added spoke prefix. What should be checked?", "Check peering address synchronization, gateway route propagation and BGP advertisement, and the on-premises learned-route state for the new prefix.", "The prefix must be known through peering and exported through the gateway routing relationship.", "Add a private DNS record for the prefix.|DNS cannot advertise network prefixes through BGP or VPN routing.", "Assign public IPs to spoke VMs.|Public addresses do not repair missing on-premises private routes.", "Disable gateway transit and rely on VNet system routes.|On-premises requires a gateway path to the spoke prefix."),
    Q("vnet-peering-manage", "Which Azure CLI command creates a peering from VNet-A to VNet-B?", "Run `az network vnet peering create --resource-group <rgA> --vnet-name <vnetA> --name <peerAtoB> --remote-vnet <vnetB-resource-id> --allow-vnet-access`.", "The peering create command materializes the local direction against the remote VNet resource ID.", "Run `az network vnet subnet create --remote-vnet <id>`.|Subnet creation cannot connect two VNets.", "Run `az network vpn-connection create --peering <name>`.|A VPN connection is a gateway resource, not a VNet peering object.", "Run `az network route-table create --remote-vnet <id>`.|A route table cannot create the peering relationship."),
    Q("vnet-peering-cli-reference", "Which Azure CLI setting makes a spoke consume the peered hub's gateway?", "Update the spoke-to-hub peering with `--use-remote-gateways true` after the hub side allows gateway transit.", "The consumer-side peering explicitly selects the remote gateway after the provider exports it.", "Set `--allow-forwarded-traffic false` on the hub only.|That can restrict transit and does not select a remote gateway.", "Set `--allow-vnet-access false` on both sides.|Disabling VNet access works against connectivity.", "Add `--gateway-subnet true` to the spoke peering.|GatewaySubnet is a subnet resource, not a peering flag."),
    Q("vnet-peering", "What is the boundary between peering connectivity and gateway transit?", "Peering directly routes between the two VNet address spaces; gateway transit lets one VNet use a gateway located in its peer to reach networks beyond that peer.", "Direct peer prefixes and remote cross-premises prefixes have different route sources and configuration controls.", "Gateway transit is required for any traffic between peered VNets.|Ordinary peer-to-peer traffic does not need a gateway.", "Peering automatically shares every gateway with no opt-in.|Provider and consumer flags are required.", "Gateway transit makes all hub-spoke peerings transitive.|It shares the gateway, not arbitrary spoke-to-spoke forwarding."),
    Q("vnet-peering", "What is the boundary between peering and DNS resolution?", "Peering supplies IP routing between VNet prefixes; DNS resolution requires Azure-provided naming, linked private zones, or a deliberate resolver and forwarding design.", "A packet path and a name-to-address path are independent prerequisites.", "Peering automatically links every private DNS zone in either subscription.|Private zone VNet links are separate resources.", "Private DNS links automatically create peering.|A DNS link grants resolution, not network reachability.", "If ping by IP works, every hostname must resolve.|Successful routing does not prove a DNS path exists."),
  ],
  "AZ104-N07-B04": [
    Q("vnet-routing", "A subnet must send all internet-bound traffic through a firewall VM at 10.40.0.4. Which route is appropriate?", "Associate a route table containing 0.0.0.0/0 with next-hop type VirtualAppliance and next-hop address 10.40.0.4.", "A default UDR to the appliance overrides the normal internet system route for that subnet.", "Create 0.0.0.0/0 with next-hop type VNetLocal.|VNetLocal is not an appliance hop and does not force inspection.", "Add the firewall IP as the subnet's DNS server.|DNS configuration does not redirect packets through the firewall.", "Create a public DNS wildcard pointing to 10.40.0.4.|Name resolution cannot implement a default network route."),
    Q("vnet-routing", "A subnet must block all traffic to 10.90.0.0/16 regardless of NSGs. Which UDR can enforce a routing black hole?", "Create a route for 10.90.0.0/16 with next-hop type None and associate the route table to the subnet.", "A None next hop drops packets selected by that prefix.", "Create the route with next hop Internet.|That sends selected traffic toward the internet path rather than discarding it.", "Use VirtualNetworkGateway without a gateway.|A nonexistent gateway is not a deliberate supported discard design.", "Add an NSG service tag named None.|None is a route next-hop type, not an NSG service tag."),
    Q("route-table-create", "An engineer wants Azure to load-balance 10.50.0.0/16 across two NVAs by adding two routes with that identical prefix to one route table. What should be decided?", "A route table cannot contain duplicate destination prefixes; select one next hop for the prefix and use a supported NVA high-availability design rather than duplicate UDRs.", "Azure requires each route-table route prefix to be unique, so duplicate UDR entries do not provide equal-cost multipath.", "Create both identical prefixes and give the preferred NVA route a lower priority number.|UDRs have no NSG-style priority property, and duplicate prefixes in one table are rejected.", "Create one identical route per NVA NIC and let Azure alternate by route name.|Route names do not create multipath selection for a duplicate prefix.", "Add both NVA IPs to one VirtualAppliance next-hop field.|A VirtualAppliance UDR accepts one next-hop IP rather than a list of appliance addresses."),
    Q("vnet-routing", "A UDR and a BGP route advertise the same destination prefix. Which one is preferred by default?", "The user-defined route is preferred over the BGP route for the equal prefix.", "Azure route-source preference favors UDR over BGP when prefix length is identical.", "BGP always wins because it is dynamic.|Dynamic origin does not outrank an equal-prefix UDR.", "The route with the lower next-hop IP wins.|Next-hop numeric order is not a selection rule.", "Both are ignored until a DNS record selects one.|DNS has no role in IP route precedence."),
    Q("route-table-create", "What sequence applies a new UDR to production instances safely?", "Create the route table and route, validate prefix and next hop, associate it with a test or intended subnet, inspect effective routes, test both directions, then expand rollout.", "A route definition has no workload effect until association and should be verified at the NIC before broad rollout.", "Associate an empty table first and add routes without testing.|That can unexpectedly remove intended custom routing during the transition.", "Change every guest default gateway to the NVA.|Azure guests should use the platform gateway; UDRs drive fabric routing.", "Publish the route in public DNS before association.|DNS does not deploy routing policy."),
    Q("nva-routing", "What sequence enables a Linux NVA to forward traffic between two subnets?", "Enable IP forwarding on the NVA NIC, enable forwarding in the guest OS, create UDRs pointing required prefixes to its private IP, permit traffic in NSGs and appliance policy, then verify symmetric routes.", "Azure fabric, guest kernel, security, and return routing must all allow the transit path.", "Enable only NIC IP forwarding and leave the guest unable to route.|The platform can deliver transit packets, but the guest will not forward them.", "Add two public IPs and omit UDRs.|Public associations do not steer private subnet traffic to the appliance.", "Set the NVA as a custom DNS server.|A DNS server address is not a routing next hop."),
    Q("route-table-create", "A route table should affect only the application subnet, not the database subnet. How should it be deployed?", "Associate the route table only with the application subnet and verify the database subnet has its intended separate association or system routes.", "Route tables attach at subnet scope, allowing deliberate segmentation of routing policy.", "Attach the route table to the entire VNet resource.|Route tables are not associated at VNet root scope.", "Associate it with the NSG used by the application.|NSGs and route tables have independent associations.", "Add the route to the application NIC only.|Azure route tables associate to subnets, not individual NICs."),
    Q("vnet-routing", "A gateway-learned default route must not propagate into one workload subnet. What sequence fits?", "Use a route table with virtual network gateway route propagation disabled, add any required explicit routes, associate it to that subnet, then inspect effective routes.", "Disabling propagation on the associated table prevents gateway routes from becoming active for that subnet.", "Disable BGP on every on-premises router.|That is much broader and removes dynamic routing for all consumers.", "Delete GatewaySubnet from the VNet.|Removing the gateway subnet destroys the shared connectivity foundation.", "Add a DNS forwarding rule for 0.0.0.0/0.|DNS rules match names, not IP route prefixes."),
    Q("effective-routes", "A route exists in a route table but does not appear on a VM NIC. What should be checked first?", "Check that the route table is associated with the VM's actual subnet and that provisioning state succeeded.", "An unassociated route-table definition cannot contribute to the NIC's effective routes.", "Check the VM's public DNS label.|DNS labels do not attach route tables.", "Reboot the VM until Azure discovers the route table.|Association is a control-plane configuration, not guest discovery.", "Add the same route to an NSG.|NSGs do not contain next-hop routes."),
    Q("vnet-routing", "Traffic still follows a /16 system route instead of a /24 UDR shown as Invalid. What effective state could make the UDR invalid?", "Its next hop may be unavailable or the route may be overridden by topology changes; inspect effective-route status, appliance IP, association, and peering or gateway changes.", "Only active valid routes participate in forwarding even when a route definition still exists.", "The /24 is too specific for Azure routing.|Azure supports specific prefixes and normally prefers the /24.", "System routes always override user routes.|UDRs can override many system routes when valid.", "The destination lacks a PTR record.|Reverse DNS does not determine route validity."),
    Q("effective-routes", "After a VPN gateway begins advertising a default route, a workload subnet sends internet traffic toward the gateway even though it has no 0.0.0.0/0 UDR. What effective state likely explains the change?", "A gateway-propagated BGP default route became active on the NIC; inspect effective routes and then deliberately disable gateway propagation on the subnet route table or apply the intended routing policy.", "Gateway-propagated routes can add a default path to workload effective routes without an administrator-authored UDR.", "A Standard public IP automatically created a VirtualNetworkGateway default route.|Public IP association does not inject a gateway-propagated BGP route into the subnet.", "The subnet's DNS server rewrote 0.0.0.0/0 to the VPN endpoint.|DNS resolves names and cannot create an IP default route.", "An inbound NSG deny selected the gateway as an alternate next hop.|NSGs allow or deny flows and do not choose fallback routes."),
    Q("nva-routing", "Packets reach an NVA NIC but leave with no response. What should be inspected?", "Inspect guest IP forwarding, appliance firewall or NAT policy, NSGs, and the destination and return effective routes.", "The Azure NIC delivery proves only the first leg; the NVA and reverse path must still forward and admit the flow.", "Create another identical UDR on the same subnet.|Duplicate steering does not make the appliance forward.", "Change the client VM's hostname.|Naming is irrelevant after packets already reach the NVA.", "Associate a public IP with the route table.|Route tables cannot own public IP resources."),
    Q("nva-routing", "A UDR still targets 10.40.0.4 after the NVA was rebuilt with private IP 10.40.0.7. Effective routes show the old VirtualAppliance next hop and traffic fails. What should be corrected?", "Update the route's next-hop IP to the stable active NVA address, verify IP forwarding and appliance policy, then retest the effective and return paths.", "A VirtualAppliance UDR stores a specific next-hop private IP and does not follow a replaced VM by name.", "Add 10.40.0.7 as a DNS alias for 10.40.0.4.|UDRs use an IP next hop and never resolve a DNS alias.", "Keep the old next hop and assign the new NVA only a public IP.|A public association does not make the stale private next-hop address active.", "Change the route destination prefix to the NVA's subnet prefix.|The destination prefix must describe traffic being forwarded, not the appliance's own subnet."),
    Q("vnet-routing", "A service endpoint route appears more specific than a forced-tunnel default route. Which path does Azure select for that service prefix?", "The more specific service-endpoint route is selected over the 0.0.0.0/0 UDR unless the design changes the relevant route behavior.", "Longest-prefix match makes the service prefix win over a default route.", "The default UDR always wins because it is user-defined.|A /0 loses to a more specific prefix before source preference is compared.", "Traffic is duplicated to both next hops.|Azure chooses one effective route rather than mirroring the packet.", "DNS TTL selects whether the endpoint or appliance wins.|TTL has no role in route selection."),
    Q("route-table-create", "Which Azure CLI commands create a route table route to an NVA and associate it to a subnet?", "Use `az network route-table route create ... --address-prefix <prefix> --next-hop-type VirtualAppliance --next-hop-ip-address <nva-ip>`, then `az network vnet subnet update ... --route-table <table>`.", "The first operation defines the route and the second applies the table to the subnet.", "Use `az network nic update --default-gateway <nva-ip>`.|Azure NICs do not accept an arbitrary guest default gateway property.", "Use `az network nsg rule create --next-hop-ip-address <nva-ip>`.|NSG rules have allow or deny actions, not routing next hops.", "Use `az network dns record-set a add-record --ipv4-address <nva-ip>`.|A DNS A record does not steer subnet traffic."),
    Q("route-table-route-cli-reference", "Which Azure CLI command lists only the configured route entries inside a named route table before an association change?", "Run `az network route-table route list --resource-group <rg> --route-table-name <table>`.", "The route-table route list operation reads administrator-defined entries in the selected table, distinct from NIC effective routes.", "Run `az network nic show-effective-route-table` without a NIC name.|Effective-route inspection requires a NIC and includes system and propagated routes beyond the named table.", "Run `az network route-table list --include-routes-from-all-nics`.|The route-table list command has no such aggregation argument and lists table resources.", "Run `az network watcher show-next-hop --route-table-name <table>`.|Next hop evaluates a source VM and destination rather than enumerating one table's configured routes."),
    Q("vnet-routing", "What is the boundary between a route table and an NSG?", "A route table selects the next hop for a destination prefix; an NSG permits or denies a flow based on direction, protocol, addresses, and ports.", "A packet needs both a viable route and admitting security state.", "An NSG can redirect allowed traffic to an appliance.|NSGs filter but do not select next hops.", "A route table can allow TCP 443 while denying TCP 80.|Routes do not evaluate transport ports.", "Whichever resource was created last replaces the other.|Routing and filtering are composed, not mutually exclusive."),
    Q("vnet-routing", "What is the boundary between a system route and a user-defined route?", "Azure creates system routes from VNet topology and connectivity; administrators create UDRs to override or refine selected prefixes and next hops within documented rules.", "System connectivity is the default, while UDRs express intentional routing policy.", "System routes can be deleted directly while UDRs are immutable.|System routes are not directly deleted, and UDRs can be changed.", "UDRs exist only inside guest operating systems.|Azure applies them in the virtual network fabric through subnet route tables.", "A system route always has a longer prefix than any UDR.|Prefix length depends on topology and administrator definitions."),
  ],
  "AZ104-N07-B05": [
    Q("hub-spoke", "Four workload VNets need shared firewall inspection and on-premises connectivity while retaining separate address and security boundaries. Which topology fits?", "Use a hub-spoke topology with the firewall and gateway in the hub, peer each nonoverlapping spoke to the hub, and configure explicit routing and transit.", "The hub centralizes shared services while spokes remain separate workload network boundaries.", "Merge all workloads into GatewaySubnet.|GatewaySubnet is reserved for gateway instances, not application placement.", "Peer every subnet directly to every other subnet.|Peering operates between VNets, and a full mesh does not centralize shared services.", "Publish all private services through public IPs.|Public exposure does not meet shared private inspection and intersite requirements."),
    Q("nva-routing", "All spoke-to-spoke traffic must pass through a stateful hub firewall. What is required?", "Use UDRs on spoke subnets to point remote-spoke prefixes to the firewall, allow forwarded traffic on peerings, enable appliance forwarding, and provide symmetric return routes.", "Peering alone is nontransitive; the NVA must be the valid next hop in both directions.", "Enable only hub gateway transit.|Gateway transit shares a gateway but does not force spoke traffic through the firewall.", "Link both spokes to one private DNS zone.|Shared DNS does not build transitive inspected routing.", "Add identical default gateways inside every guest.|Azure guest gateways should remain platform-provided."),
    Q("vpn-gateway", "A branch office needs encrypted connectivity over the public internet to an Azure VNet. Which service fits?", "Deploy an Azure VPN Gateway with a site-to-site connection and a correctly defined local network gateway for the branch prefixes and VPN device.", "A site-to-site VPN supplies encrypted IPsec/IKE connectivity between the on-premises device and Azure gateway.", "Use VNet peering to the branch router.|Peering connects Azure VNets and cannot terminate an on-premises internet VPN.", "Use Azure DNS Private Resolver as the tunnel endpoint.|Private Resolver handles DNS forwarding, not IPsec connectivity.", "Attach a public IP directly to every private workload.|That exposes hosts and does not create a site network tunnel."),
    Q("expressroute-overview", "A company needs provider-backed private connectivity from its WAN to Azure with predictable bandwidth and no dependency on internet routing. Which service should it evaluate?", "Evaluate Azure ExpressRoute with the appropriate connectivity provider, circuit bandwidth, peering, and redundancy design.", "ExpressRoute extends private WAN connectivity into Microsoft cloud services through a provider circuit.", "Use only a public Azure DNS zone.|Authoritative DNS hosting does not provide private WAN transport.", "Use global VNet peering from the on-premises router.|On-premises networks are not Azure VNets that can be globally peered.", "Use a NAT Gateway for inbound branch traffic.|NAT Gateway is not an intersite circuit or VPN termination service."),
    Q("vpn-gateway", "A hub already has a VPN gateway. Ten spokes need on-premises routes without ten gateways. Which design minimizes gateway duplication?", "Use hub gateway transit across each hub-spoke peering and ensure route propagation and on-premises return routes include every spoke prefix.", "One hub gateway can serve multiple eligible spokes through peering transit.", "Create an empty GatewaySubnet in each spoke and omit transit flags.|An empty reserved subnet supplies no gateway routes.", "Use service endpoints from each spoke to the VPN device.|Service endpoints target supported Azure services, not on-premises gateways.", "Put all spoke address spaces into the hub VNet without moving resources.|Address declarations alone cannot absorb existing remote VNet resources."),
    Q("hub-spoke", "What sequence introduces a hub firewall into existing spoke traffic?", "Inventory flows and prefixes, deploy and test the firewall, configure peering forwarded-traffic settings, add staged symmetric UDRs, verify effective routes and policy, then expand rollout with monitoring.", "A stateful inspection insertion must preserve both forward and return paths and should be introduced incrementally.", "Apply 0.0.0.0/0 to every spoke before the firewall is ready.|That can black-hole production traffic.", "Delete all direct routes and rely on DNS failover.|DNS cannot replace the unavailable routing path.", "Change guest default gateways on every VM before Azure UDRs.|Guest gateway changes conflict with Azure's virtual networking model."),
    Q("vpn-gateway", "What sequence prepares a VNet for a site-to-site VPN gateway?", "Plan a nonoverlapping VNet and on-premises prefix set, create a correctly named and sized GatewaySubnet, create public IP and VPN gateway, define the local network gateway, create the connection with matching IPsec settings, then validate routes and tunnel state.", "Gateway subnet capacity, endpoint identity, remote prefixes, shared settings, and routing all precede usable connectivity.", "Create the connection before the gateway and local network objects.|The connection depends on both gateway endpoints.", "Place application VMs in GatewaySubnet to test it.|GatewaySubnet is reserved for gateway instances.", "Use overlapping Azure and branch prefixes and let NAT occur automatically.|Ordinary site-to-site routing cannot distinguish overlapping destinations without an explicit supported translation design."),
    Q("nva-routing", "A hub firewall route receives incoming spoke packets, but replies leave through another path. Connections fail. What should be corrected?", "Correct UDRs and learned routes so the return path traverses the same stateful firewall and verify source translation policy where deliberately used.", "Asymmetric routing prevents the firewall from maintaining a valid bidirectional connection state.", "Add another private DNS record for the client.|Packet return selection does not depend on an extra hostname.", "Disable IP forwarding on the firewall NIC.|That would stop transit rather than restore symmetry.", "Move the firewall to GatewaySubnet.|GatewaySubnet is not general NVA placement and would not inherently fix routes."),
    Q("vpn-gateway", "A site-to-site tunnel is Connected, but a new spoke prefix is unreachable from on-premises. What should be checked?", "Check gateway transit and peering synchronization, effective spoke routes, BGP or local-network prefix advertisement, and the on-premises return route for the new spoke range.", "Tunnel state proves the security association, not that every new prefix is exchanged and routed both ways.", "Rotate the VPN shared key immediately.|A working tunnel makes a key mismatch unlikely.", "Create a public IP on each spoke VM.|That bypasses rather than repairs private hybrid routing.", "Change the spoke's private DNS suffix.|DNS suffix does not advertise a route."),
    Q("effective-routes", "Which command output should be captured first to prove a spoke NIC sends a branch prefix toward the hub?", "Use `az network nic show-effective-route-table` for the spoke NIC and inspect the branch prefix's next hop.", "Effective routes show whether gateway propagation or a UDR actually applies to that workload.", "Use `az network public-ip show` on the hub gateway.|The gateway public endpoint does not prove the spoke NIC's selected route.", "Use `az network dns zone export`.|Zone records do not expose hybrid route selection.", "Use `az vm list-sizes`.|VM sizes have no route evidence."),
    Q("vpn-gateway", "What is the boundary between a VPN gateway and an NVA?", "VPN Gateway terminates managed VPN or supported cross-network gateway connections; an NVA runs vendor or custom packet processing and requires explicit forwarding, routing, scale, and availability design.", "They can coexist, but tunnel termination and traffic inspection are distinct responsibilities.", "Every NVA automatically becomes an Azure-managed VPN gateway.|An appliance VM does not acquire managed gateway semantics.", "VPN Gateway executes arbitrary guest firewall software.|The managed gateway does not expose a customer guest OS.", "Both can be placed in any ordinary application subnet.|VPN Gateway requires GatewaySubnet; an NVA uses workload subnets."),
    Q("expressroute-overview", "What is the boundary between ExpressRoute and site-to-site VPN?", "ExpressRoute uses provider-based private connectivity and peering; site-to-site VPN uses encrypted IPsec/IKE tunnels, commonly over the public internet.", "Private transport, encryption, provisioning, bandwidth, and failover characteristics differ.", "ExpressRoute always encrypts every packet by default while VPN never does.|Standard ExpressRoute is not inherently encrypted, whereas VPN provides tunnel encryption.", "Both require public IPs on every Azure VM.|Workloads can remain privately addressed behind either connectivity model.", "A VPN connection cannot coexist as ExpressRoute backup.|Hybrid designs can use VPN as an alternate path when planned correctly."),
  ],
  "AZ104-N07-B06": [
    Q("dns-overview", "A company registered contoso.example with a registrar and wants Azure to host its public records. What must it do?", "Create the public DNS zone in Azure, then delegate the domain at the registrar to the exact Azure-assigned name servers.", "Azure DNS becomes publicly authoritative only when the parent zone points resolvers to its name servers.", "Transfer domain registration automatically by creating the zone.|Azure DNS hosting does not itself act as the registrar transfer.", "Link the public zone to a VNet.|VNet links belong to Private DNS zones and are not public delegation.", "Add the registrar's IP as an NSG rule.|Network security groups do not configure DNS authority."),
    Q("dns-records", "The zone apex must point to an Azure public IP resource and track its address lifecycle. Which record design is appropriate?", "Create an Azure DNS alias A record at the zone apex targeting the supported public IP resource.", "Alias records can reference supported Azure resources and are valid at the zone apex without a CNAME.", "Create a CNAME at the apex pointing to the resource ID.|The apex cannot use an ordinary CNAME alongside required zone records, and resource IDs are not DNS targets.", "Create a PTR record in the forward zone.|PTR records express reverse mapping rather than apex service resolution.", "Create an MX record with the public IP.|MX records name mail exchangers and cannot directly model this web endpoint."),
    Q("dns-records", "A web endpoint name should resolve to another canonical hostname managed by a SaaS provider. Which record type fits?", "Use a CNAME record for the non-apex hostname, subject to provider validation requirements.", "CNAME aliases one DNS name to another and lets the provider manage the destination addresses.", "Use an NS record at the host label.|NS delegates a child zone rather than aliases a service hostname.", "Use a TXT record containing the hostname.|TXT data is not followed as an address alias by clients.", "Use a PTR record in the public forward zone.|PTR is used for reverse lookup semantics."),
    Q("dns-records", "A mail domain must direct senders to two mail servers with different preferences. Which records are required?", "Create MX records in one record set with the two hostnames and their intended preference values, and ensure those names resolve.", "MX preference orders candidate mail exchangers while their target hostnames need address resolution.", "Create two A records with preference values.|A records contain addresses and have no mail priority field.", "Create CNAME records at the apex for both servers.|Multiple apex CNAMEs conflict with zone authority records and do not express mail priority.", "Create two NS records for the mail servers.|NS records delegate DNS authority, not SMTP delivery preference."),
    Q("dns-delegate", "What sequence delegates a newly created public Azure DNS zone?", "Create the zone, copy all assigned Azure name-server hostnames, replace the registrar or parent NS delegation with those values, wait for propagation, then query the parent and an Azure authoritative server.", "Delegation is a parent-zone change and must use the exact server set assigned to that zone.", "Create A records first and wait for clients to discover the zone without parent changes.|Records remain undiscoverable until the parent delegates authority.", "Copy name servers from another Azure zone because they share the subscription.|Each zone receives its own assigned name-server set.", "Point the registrar to the zone resource ID.|Registrars require DNS NS hostnames, not Azure resource identifiers."),
    Q("dns-records", "What sequence performs a low-risk public DNS address cutover?", "Lower TTL in advance, wait for the previous TTL to age out, update the record, verify authoritative responses and endpoint health, monitor, then restore a suitable TTL.", "TTL planning limits how long resolvers can retain the old answer during the controlled change.", "Change the record and lower TTL afterward for already cached answers.|Existing caches honor the old TTL and are not shortened retroactively.", "Delete the zone and recreate it with the new address.|That risks delegation drift and loss of unrelated records.", "Change NS servers for a simple A-record cutover.|Replacing authority is unnecessary and broadens failure scope."),
    Q("dns-records", "What sequence delegates `dev.contoso.example` while Azure DNS continues hosting the parent zone?", "Create the child zone, obtain its assigned name servers, add an NS record set named dev in the parent zone with those servers, then verify child authority and records.", "A child-zone NS set in the parent transfers authority for that subdomain.", "Add a CNAME named dev to the child zone resource ID.|CNAME does not establish zone delegation and resource IDs are not DNS names.", "Link both public zones to the same VNet.|Public authority is unrelated to VNet linking.", "Copy the parent's SOA record into the child label.|SOA duplication in the parent does not delegate the child."),
    Q("dns-records", "What sequence adds a TXT record used for domain ownership verification?", "Obtain the exact label and token from the validating service, add them to the correct Azure DNS TXT record set, wait for TTL propagation, query the authoritative server, then retry validation.", "Ownership systems compare an exact token at an exact DNS name.", "Put the token in an NS record because it proves authority.|NS values must be name servers and will not satisfy TXT validation.", "Add the token only to a private DNS zone.|External validators cannot query a private zone.", "Create an A record whose numeric address encodes the token.|Validation expects the documented TXT value."),
    Q("dns-records", "What sequence safely removes an obsolete public record?", "Confirm no clients or certificates depend on it, lower TTL ahead of removal when useful, remove the record, verify authoritative NXDOMAIN or remaining record-set behavior, then monitor.", "Dependency review and cache aging reduce surprises from a public-name deletion.", "Delete the parent NS delegation to remove one host.|That takes the entire zone out of public resolution.", "Replace the record with 0.0.0.0 indefinitely.|A sink address is not equivalent to deliberate record retirement and can mask dependencies.", "Remove the SOA record from the zone.|Azure manages mandatory zone authority records."),
    Q("dns-delegate", "Public queries for a new Azure DNS zone fail intermittently. The parent delegates three current Azure name servers plus one retired provider server. What should be corrected?", "Replace the parent NS set with the complete exact name-server set assigned to the Azure zone and remove the stale provider server.", "Resolvers may select any delegated server, so a stale or nonauthoritative member creates intermittent failures even when other servers answer correctly.", "Add the retired provider server as an A record inside the Azure zone.|An address record cannot make an unrelated server authoritative or correct the parent NS set.", "Increase the application A-record TTL so resolvers avoid the stale name server.|Record TTL applies after an authoritative answer and does not repair inconsistent delegation.", "Link the public zone to every VNet to synchronize the four servers.|Public DNS delegation is controlled at the parent, not through Private DNS VNet links."),
    Q("dns-records", "Some clients still receive the old address immediately after an A-record update, while the authoritative server returns the new one. Why?", "Recursive resolvers or client caches still hold the old answer until its prior TTL expires.", "Authoritative correctness does not invalidate already cached data before the cache lifetime ends.", "Azure DNS is randomly returning both values even though only one remains.|The authoritative result disproves a current multi-value record set.", "VNet peering has not synchronized the public record.|Public DNS cache state is independent of VNet peering.", "The public IP allocation changed from static to dynamic.|That cannot explain clients retaining a specifically cached old answer."),
    Q("dns-records", "A CNAME creation at the zone apex fails. Which DNS rule explains the failure?", "The apex already requires SOA and NS records and cannot be a CNAME; use a supported alias record or appropriate A/AAAA records instead.", "A CNAME cannot coexist with other data at the same owner name.", "CNAME records can point only to IP addresses.|CNAME targets are names, not addresses, but that is not the apex conflict.", "Azure public zones allow only TXT records at the apex.|Apex A, AAAA, MX, alias, and other supported records are possible.", "The zone must first be linked to a subnet.|Public record creation does not require a VNet link."),
    Q("dns-a-cli-reference", "Which Azure CLI command adds an IPv4 address to an A record set?", "Run `az network dns record-set a add-record --resource-group <rg> --zone-name <zone> --record-set-name <name> --ipv4-address <address>`.", "The A record-set add operation appends the IPv4 value at the specified owner name.", "Run `az network private-dns link vnet create --ipv4-address <address>`.|A VNet link does not create a public A record.", "Run `az network public-ip create --zone-name <zone>`.|A public IP allocation does not automatically author this record set.", "Run `az network nsg rule create --destination-address-prefix <address>`.|NSG policy is not authoritative DNS data."),
    Q("dns-zone-cli-reference", "Which command output identifies the Azure name servers that the registrar must use?", "Use `az network dns zone show --resource-group <rg> --name <zone>` and read the `nameServers` collection.", "The zone resource returns its assigned authoritative name-server hostnames.", "Use `az network vnet show` and read `dhcpOptions`.|VNet DNS server settings are resolver configuration, not public authority.", "Use `az network public-ip show` and read `ipTags`.|Public IP metadata does not contain zone delegation servers.", "Use `az account show` and read tenantId.|Tenant identity is not a DNS NS target."),
    Q("dns-overview", "What is the boundary between a DNS registrar and Azure DNS?", "A registrar manages registration and parent-domain delegation; Azure DNS hosts authoritative zone data after that delegation points to its name servers.", "Domain ownership and authoritative record serving are separate responsibilities.", "Creating an Azure zone automatically renews the registered domain.|Zone hosting does not manage registrar renewal.", "A registrar stores Azure resource IDs as A records.|Registrars manage domain and delegation data, not Azure resource topology.", "Azure DNS can host a zone only if Microsoft is the registrar.|Azure DNS can host domains registered with other registrars."),
    Q("dns-records", "What is the boundary between an A record, CNAME, and alias record?", "An A record stores an IPv4 address, a CNAME redirects a non-apex name to another name, and an Azure alias record targets a supported Azure resource while remaining an A or other supported record type.", "The record types differ in stored target and apex or lifecycle behavior.", "All three store arbitrary text tokens.|TXT, not these address and alias types, stores general verification text.", "CNAME is preferred at every zone apex.|Apex CNAME conflicts with required authority records.", "An alias record delegates a child zone.|NS records perform delegation."),
  ],
  "AZ104-N07-B07": [
    Q("dns-private-overview", "VMs in two VNets need to resolve `db.corp.internal` privately without publishing it to the internet. Which design fits?", "Create an Azure Private DNS zone for corp.internal and link both client VNets for resolution.", "Private DNS VNet links make the zone visible through Azure-provided resolution only to intended networks.", "Create a public Azure DNS zone and omit registrar delegation.|An undelegated public zone is not a deliberate cross-VNet private resolution design.", "Add the name to each VM hosts file.|Per-host files do not provide managed shared DNS lifecycle.", "Peer the VNets and assume the zone appears automatically.|Peering provides routing but does not link private DNS zones."),
    Q("dns-private-links", "Only one VNet should automatically register VM names, while three other VNets should query them. How should links be configured?", "Enable autoregistration on the registration VNet link and create resolution-only links for the other three VNets.", "Autoregistration ownership can be limited while every linked VNet receives query access.", "Enable autoregistration on all links to avoid duplicate ownership concerns.|Broad registration can create unintended records and conflicts.", "Create no links and rely on peering transit.|Private zone visibility is controlled by VNet links, not transitive peering.", "Publish the records in a public zone instead.|That violates the private-only naming requirement."),
    Q("dns-private-resolver", "On-premises clients must resolve Azure private endpoint names without running DNS forwarder VMs. Which service fits?", "Deploy Azure DNS Private Resolver with an inbound endpoint reachable from on-premises and configure the on-premises conditional forwarder to that endpoint.", "The inbound endpoint provides a managed target for hybrid queries into Azure private DNS resolution.", "Delegate the private zone at the public registrar.|Public DNS hierarchy cannot expose an Azure private zone safely.", "Use a NAT Gateway as the DNS endpoint.|NAT Gateway provides outbound SNAT, not recursive DNS service.", "Add a public IP to every private endpoint.|That defeats private endpoint isolation and does not establish private DNS forwarding."),
    Q("dns-private-resolver", "Azure VMs must resolve `branch.example` using an on-premises DNS server. Which managed configuration is appropriate?", "Use a Private Resolver outbound endpoint and a forwarding ruleset rule for branch.example targeting the on-premises DNS server, then link the ruleset to the querying VNets.", "Outbound endpoints and rulesets forward selected suffixes from linked VNets to external resolvers.", "Create an inbound endpoint only and expect it to send Azure queries outward.|Inbound endpoints receive queries into Azure; they are not the outbound forwarding path.", "Add an A record for the branch DNS server in every Azure zone.|Knowing the server address does not forward branch.example queries to it.", "Configure a UDR whose destination is the DNS suffix.|Routes match IP prefixes, not domain names."),
    Q("dns-private-links", "What sequence enables private-zone autoregistration for a workload VNet?", "Create or select the private zone, create a VNet link with registration enabled, verify the link reaches Completed, deploy or restart eligible VM records as required, then query the zone and validate lifecycle behavior.", "The registration setting belongs to the VNet link and records appear only after the link and eligible resources are active.", "Create records first in a public zone and convert it to private.|Public and private zones are distinct resource types.", "Enable registration on a peering object.|VNet peering has no DNS registration flag.", "Attach the zone to a subnet route table.|Route tables cannot register hostnames."),
    Q("dns-private-resolver", "What sequence builds Azure-to-on-premises conditional forwarding?", "Create a dedicated delegated subnet and outbound endpoint, create a ruleset with a suffix rule targeting reachable on-premises DNS IPs, link the ruleset to client VNets, ensure routing and UDP/TCP 53, then test authoritative and client queries.", "Resolver deployment, suffix matching, network reachability, and DNS transport must all work.", "Create an inbound endpoint and link it as a route-table next hop.|A resolver endpoint is not a VirtualAppliance route next hop.", "Forward every suffix to on-premises including the same Azure private zones it forwards back.|That risks a forwarding loop.", "Open only TCP 443 because Azure DNS uses HTTPS.|Conventional DNS forwarding uses UDP and TCP port 53."),
    Q("dns-private-resolver", "What sequence enables on-premises-to-Azure private-zone resolution?", "Create a correctly delegated inbound-endpoint subnet, deploy the inbound endpoint, ensure hybrid routing and DNS security reach its private IP, configure an on-premises conditional forwarder for the Azure private suffix, then test from on-premises.", "The on-premises resolver needs a reachable private target that can resolve the linked Azure zone.", "Create only an outbound endpoint and point on-premises to it.|Outbound endpoints originate forwarding and are not listener targets for on-premises clients.", "Publish the private records through the registrar.|That exposes names publicly and bypasses the private resolver design.", "Use the Azure platform address 168.63.129.16 directly from on-premises.|That platform virtual IP is not a general on-premises resolver endpoint."),
    Q("dns-private-links", "A linked VNet cannot resolve a private-zone record, but another linked VNet can. What should be inspected first?", "Inspect the failing VNet's link state, custom DNS configuration, forwarding path, and whether the query suffix matches the intended private zone.", "A VNet link can be healthy while custom resolvers bypass Azure-provided resolution or forward the suffix incorrectly.", "Recreate the A record because it works from the other VNet.|The successful VNet already proves the record exists.", "Add a public IP to the querying VM.|Public association does not grant private-zone visibility.", "Enable gateway transit on the DNS zone.|Gateway transit is a peering setting, not a zone property."),
    Q("dns-private-links", "Automatic VM records appear for one VNet but not another. What effective state explains the difference?", "The first VNet link has autoregistration enabled while the second is resolution-only, or the second resources are not eligible for registration.", "Record registration is optional per link and distinct from query resolution.", "The second VNet has too many NSG rules.|NSG count does not toggle Private DNS autoregistration.", "The second VNet uses a /24 instead of /16.|Subnet size does not determine registration mode.", "The first VNet is peered to a public DNS zone.|Public zones do not use VNet autoregistration links."),
    Q("dns-private-resolver", "A forwarding rule targets an on-premises DNS IP, but queries time out. Which layers should be checked?", "Check ruleset link and suffix match, outbound endpoint provisioning, route and firewall reachability to the target, and both UDP and TCP 53 response paths.", "A valid rule still depends on endpoint, routing, security, and DNS transport.", "Change private-zone TTL to zero.|TTL cannot restore an unreachable upstream resolver.", "Enable VM autoregistration on the ruleset.|Autoregistration belongs to private-zone VNet links.", "Add an NS record pointing to the outbound endpoint IP.|NS values must be server hostnames and do not configure the forwarding service."),
    Q("dns-private-overview", "A private endpoint hostname resolves publicly from one VNet despite an approved endpoint. What should be corrected?", "Ensure the recommended private DNS zone contains the endpoint record and is linked to the client VNet or reachable through its resolver forwarding path.", "Private endpoint approval creates the network interface, but client DNS must select its private address.", "Enable IP forwarding on the endpoint NIC.|Private endpoint NIC forwarding is not the client name-resolution mechanism.", "Add the endpoint to a public DNS A record.|That can expose or misdirect the private address and does not fix scoped private resolution.", "Create a second public IP for the endpoint.|Private endpoints use private IP connectivity."),
    Q("dns-private-resolver", "Queries alternate between success and forwarding-loop failure after hybrid DNS was introduced. What topology should be examined?", "Examine conditional forwarders and rulesets for a suffix being sent from on-premises to Azure and then back to on-premises without an authoritative stopping point.", "Circular forwarding causes recursion failure even when every individual resolver is reachable.", "Increase the VNet address space.|CIDR capacity does not terminate a DNS forwarding loop.", "Lower every record TTL.|Caching changes frequency but does not remove circular delegation.", "Add another outbound endpoint with the same rules.|Duplicating the loop does not create authority."),
    Q("private-dns-link-cli-reference", "Which Azure CLI command creates a resolution-only VNet link to a private DNS zone?", "Run `az network private-dns link vnet create --resource-group <rg> --zone-name <zone> --name <link> --virtual-network <vnet-id> --registration-enabled false`.", "The private-dns VNet link controls zone visibility and its registration mode.", "Run `az network vnet peering create --private-dns-zone <zone>`.|Peering does not create a zone link.", "Run `az network dns zone create --link <vnet-id>`.|The public DNS command group does not manage Private DNS links.", "Run `az network route-table route create --next-hop-type PrivateDns`.|PrivateDns is not a route next-hop type."),
    Q("dns-private-overview", "What is the boundary between public and private Azure DNS zones?", "Public zones answer through internet DNS delegation; private zones answer only through linked VNets or designed resolver paths and require no registrar delegation.", "They can use the same textual suffix in split-horizon designs, but their authorities and audiences differ.", "Private zones become public when a VNet has a public IP.|VNet exposure does not publish the private authority.", "Public zones require VNet links for internet clients.|Internet authority follows parent NS delegation.", "Both automatically synchronize records with each other.|Public and private zone contents are independent."),
  ],
  "AZ104-N07-B08": [
    Q("network-watcher-ip-flow", "A VM cannot accept TCP 443, and the administrator needs the exact effective NSG rule that denies the packet. Which diagnostic fits?", "Run Network Watcher IP flow verify with inbound TCP, the VM's local address and port 443, and the actual remote address and port.", "IP flow verify evaluates effective NSG policy and returns allow or deny with the matching rule.", "Run Next hop without specifying ports.|Next hop diagnoses routing and cannot name the winning NSG port rule.", "Export the public DNS zone.|DNS records do not expose NSG evaluation.", "Check only the NSG attached to the NIC and ignore the subnet NSG.|Effective policy composes both associations."),
    Q("network-watcher-next-hop", "A VM reaches the wrong firewall for 10.80.3.5. Which diagnostic should identify the chosen Azure route?", "Run Network Watcher Next hop from the VM to 10.80.3.5 and compare the returned route with the NIC's effective route table.", "Next hop evaluates the selected path for the exact destination.", "Use IP flow verify and treat Allow as proof of firewall selection.|An allowed NSG flow does not identify the route next hop.", "Query the destination's TXT record.|TXT content is unrelated to fabric routing.", "List every route table in the subscription and choose the newest.|Only associated effective routes matter, and creation time is not precedence."),
    Q("network-watcher-connection", "An application intermittently fails to connect from a VM to a database endpoint. Which point-in-time tool can report reachability, latency, and failure location?", "Use Network Watcher Connection troubleshoot for the source VM and database address and port during the incident.", "Connection troubleshoot exercises the endpoint path and reports path or connectivity evidence for the specified flow.", "Use public DNS delegation validation only.|Correct authority does not measure the application network path.", "Use a resource lock report.|Locks do not measure data-plane reachability.", "Use VNet peering list and assume Connected proves the database port works.|Peering state omits NSG, route, DNS, and service health."),
    Q("effective-routes", "A VM lost on-premises access after a route-table change. What evidence should be captured before editing routes again?", "Capture the NIC effective route table for the on-premises prefix, the selected next hop, gateway propagation state, and the reverse route from on-premises.", "The effective and return paths reveal whether the change removed or superseded hybrid routing.", "Capture only the VM CPU chart.|Compute utilization cannot prove the network next hop.", "Replace the VM NIC to force new routes.|Replacement destroys useful state and may reproduce the same subnet policy.", "Create a public CNAME for the on-premises IP.|DNS aliasing cannot restore a missing route."),
    Q("network-watcher-ip-flow", "What sequence diagnoses an inbound NSG denial?", "Resolve the actual source and destination tuple, run IP flow verify, inspect the named rule and priority across subnet and NIC NSGs, correct the narrowest rule, rerun the test, then validate the application.", "Effective-state testing should precede a least-privilege policy correction and end with an end-to-end check.", "Add an allow-any rule at priority 100 before collecting evidence.|That broad change can hide the original policy conflict and expands exposure.", "Delete the subnet NSG and assume the NIC NSG is sufficient.|Removing a security layer is not a scoped diagnosis.", "Change the destination DNS name while testing the same IP.|A known IP-level denial is not repaired by renaming."),
    Q("network-watcher-next-hop", "What sequence diagnoses an unexpected next hop?", "Run Next hop for the exact destination, inspect all effective routes and prefix lengths, identify the winning route source and association, correct the responsible UDR or propagation setting, then retest both directions.", "Route selection follows effective prefix and source rules, and the reverse path may fail independently.", "Edit the broadest route first without identifying the winner.|That risks unrelated destinations and may not affect a more specific route.", "Restart Network Watcher until it returns the desired hop.|The tool reports configuration; it does not choose a random route.", "Clear client DNS cache even though a destination IP was tested.|DNS cache is bypassed when the diagnostic uses a fixed IP."),
    Q("dns-private-resolver", "What sequence diagnoses a hybrid private-name failure?", "Query the client resolver, trace the conditional forwarding suffix, test reachability to resolver endpoints on UDP and TCP 53, query the authoritative private zone from the Azure path, correct links or rules, then retest from the original client.", "Hybrid DNS spans client choice, forwarding, transport, Azure zone visibility, and authoritative data.", "Create duplicate records in public DNS immediately.|That changes audience and can leak private naming without diagnosing forwarding.", "Replace VNet peerings before checking the DNS server used by the client.|Routing replacement is premature without proving the resolver path.", "Disable recursion on every resolver.|Conditional forwarding depends on resolver recursion to follow the designed path."),
    Q("network-watcher-connection", "What sequence distinguishes DNS failure from network failure for an HTTPS endpoint?", "Resolve the hostname and record returned address, test connection to that address and port, compare results with Connection troubleshoot and effective routes or NSGs, then correct the failed layer and repeat by hostname.", "Separating name resolution from IP reachability prevents DNS and transport symptoms from being conflated.", "Change both DNS and NSG simultaneously.|Concurrent changes destroy evidence about the failing layer.", "Assume a successful `nslookup` proves TCP 443 works.|DNS success does not test security, routing, or application listening.", "Assume an IP connection proves the hostname certificate is valid.|TLS name validation can still fail when the raw address is reachable."),
    Q("network-watcher-ip-flow", "IP flow verify reports Deny by `DefaultRule_DenyAllInBound`. What does that effective state mean?", "No higher-priority inbound allow rule matched the tested tuple, so the default inbound deny rule won.", "NSG processing stops on the first matching priority rule and reaches the default deny when no custom or earlier default allow matches.", "The route table has no default route.|IP flow verify is reporting an NSG rule name, not a routing result.", "The VM application rejected the TLS certificate.|Application processing occurs after NSG admission.", "The DNS zone lacks an MX record.|Mail routing records do not affect this inbound TCP rule evaluation."),
    Q("network-watcher-next-hop", "Next hop reports `None` for a destination that should be reachable. What does that indicate?", "The selected effective route discards the destination or no viable next hop exists; inspect the winning prefix and its source.", "A None next hop represents a routing black hole before application traffic can arrive.", "An NSG allowed the packet.|NSG admission cannot override a discard route.", "The destination DNS record has a long TTL.|TTL cannot change a tested IP next-hop result.", "The VM must use a public IP even for private destinations.|Private routing can work without public addressing when a viable route exists."),
    Q("effective-routes", "A peering route is present, but a more specific UDR to an old NVA is Active. Which path is used?", "The more specific active UDR to the old NVA is used; remove or correct it only after validating the intended replacement path.", "Longest-prefix match makes the specific UDR win over the broader peering system route.", "The peering route always wins because its status is also Active.|Active routes can coexist; prefix specificity decides first.", "Traffic is copied to both the NVA and peering.|Azure route selection does not mirror packets across matching routes.", "The route with the alphabetically first name wins.|Resource names are not routing precedence."),
    Q("dns-delegate", "Public DNS resolves through an old provider even though the Azure zone contains current records. What effective state should be inspected?", "Inspect the parent NS delegation; it likely still names the old provider rather than the Azure-assigned servers.", "Resolvers follow the DNS hierarchy, so an undelegated Azure zone is not queried.", "Inspect the subnet route table for the authoritative server.|Azure public DNS authority is not reached through the customer's subnet routes.", "Add a VNet link to the public zone.|Public zones do not use private VNet links.", "Increase the Azure record TTL.|TTL inside an authority clients never reach cannot redirect delegation."),
    Q("dns-private-links", "A VM using custom DNS cannot resolve a linked private zone, while VMs using Azure-provided DNS can. What is the likely cause?", "The custom DNS server does not forward that private suffix to Azure through a supported resolver path.", "A VNet link makes the zone available to Azure resolution, but custom resolvers must deliberately forward queries into that path.", "The private zone must be registered with a public registrar.|Private zones require no public delegation.", "The VM needs a Standard public IP.|Public addressing does not grant access to private DNS authority.", "The linked VNet prefix is too small for DNS.|CIDR size does not control private-zone query visibility."),
    Q("network-watcher-connection", "Connection troubleshoot reaches the destination IP but the application still returns HTTP 503. What boundary has been crossed?", "Network reachability is working; investigate application readiness, listener, dependency, and service-layer health rather than changing routes first.", "A successful connection path does not guarantee the endpoint can serve a healthy application response.", "A 503 proves VNet peering is disconnected.|An HTTP response demonstrates that a network connection reached a responder.", "Add a broader NSG allow rule.|More network admission does not repair an unhealthy application already responding.", "Change the destination's DNS TTL.|TTL does not alter the service process returning 503."),
    Q("network-watcher-ip-flow-cli-reference", "Which Azure CLI command runs IP flow verification for a VM?", "Run `az network watcher test-ip-flow` with the resource group, VM, direction, protocol, local and remote addresses, and ports.", "The test-ip-flow operation evaluates the specified tuple against effective NSG rules.", "Run `az network watcher show-next-hop --destination-port 443`.|Next hop does not evaluate NSG port rules.", "Run `az network dns record-set list --protocol TCP`.|DNS record listing cannot test NSG state.", "Run `az vm open-port --diagnose-only`.|Open-port changes policy and is not the IP flow verification command."),
    Q("network-watcher-next-hop-cli-reference", "Which Azure CLI command tests the selected next hop from a VM to a destination IP?", "Run `az network watcher show-next-hop --resource-group <rg> --vm <vm> --source-ip <source> --dest-ip <destination>`.", "The show-next-hop operation returns the effective next-hop type and address for the destination.", "Run `az network route-table route create --dest-ip <destination>`.|Route creation changes state and does not diagnose the existing selection.", "Run `az network watcher test-ip-flow --next-hop-only`.|IP flow verify has a different NSG-focused contract.", "Run `az network private-dns record-set a show --dest-ip <destination>`.|Private DNS record inspection does not evaluate routing."),
    Q("network-watcher-next-hop", "What is the boundary between Next hop and IP flow verify?", "Next hop identifies the effective route selected for a destination; IP flow verify reports whether the specified flow is allowed or denied by effective NSG rules.", "Routing and packet filtering answer different questions and often must both succeed.", "Next hop tests the application response while IP flow verify tests DNS.|Neither description matches the tools' network-layer roles.", "IP flow verify changes the NSG rule that blocks traffic.|It reports effective state and does not remediate policy.", "Both return the same evidence and are interchangeable.|One reports route selection and the other NSG evaluation."),
    Q("network-watcher-connection", "What is the boundary between point-in-time Connection troubleshoot and continuous Connection Monitor?", "Connection troubleshoot diagnoses one requested path now; Connection Monitor repeatedly measures configured endpoints and can alert on reachability or latency changes over time.", "The first is an incident probe, while the second establishes ongoing observability.", "Connection Monitor creates routes while Connection troubleshoot deletes them.|Both are diagnostic capabilities and do not implement routing policy.", "Connection troubleshoot can monitor thousands of paths indefinitely without configuration.|A one-time test is not a persistent monitoring topology.", "Connection Monitor replaces DNS and NSG configuration.|Monitoring observes the path but does not supply its networking controls."),
  ],
};

const curriculum = JSON.parse(await readFile(curriculumPath, "utf8"));
const slotOrdinal = (slot) => Number(slot.slotId.match(/-(\d+)$/)?.[1] ?? 0);
const expectedBlocks = curriculum.blockPlans.filter((block) => block.nodeId === NODE);
const expectedMixByOperation = { decision_selection: "scenarioDecision", configuration_sequence: "configurationSequence", decision_diagnosis: "troubleshootingEffectiveState", tool_selection: "toolOrIacCommand", boundary_analysis: "boundaryOrContrast" };
const exactToolSourceByCard = new Map(Object.entries({
  "AZ104-N07-B01-15": "vnet-cli-reference",
  "AZ104-N07-B01-16": "vnet-manage-subnets",
  "AZ104-N07-B02-11": "public-ip-cli-reference",
  "AZ104-N07-B03-15": "vnet-peering-manage",
  "AZ104-N07-B03-16": "vnet-peering-cli-reference",
  "AZ104-N07-B04-15": "route-table-create",
  "AZ104-N07-B04-16": "route-table-route-cli-reference",
  "AZ104-N07-B05-10": "effective-routes",
  "AZ104-N07-B06-13": "dns-a-cli-reference",
  "AZ104-N07-B06-14": "dns-zone-cli-reference",
  "AZ104-N07-B07-13": "private-dns-link-cli-reference",
  "AZ104-N07-B08-15": "network-watcher-ip-flow-cli-reference",
  "AZ104-N07-B08-16": "network-watcher-next-hop-cli-reference",
}));
const seenPrompts = new Set();
const seenOptions = new Set();
const seenWrongTuples = new Set();

if (expectedBlocks.length !== 8 || Object.keys(blocks).length !== 8) throw new Error("Expected exactly 8 N07 blocks.");
let totalCards = 0;
for (const block of expectedBlocks) {
  const cards = blocks[block.blockId];
  const slots = curriculum.slots.filter((slot) => slot.blockId === block.blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  if (!cards || cards.length !== slots.length) throw new Error(`${block.blockId}: ${cards?.length ?? 0} cards for ${slots.length} slots.`);
  const actualMix = {};
  for (const slot of slots) actualMix[expectedMixByOperation[slot.learningOperation]] = (actualMix[expectedMixByOperation[slot.learningOperation]] ?? 0) + 1;
  for (const [key, count] of Object.entries(block.questionMix)) if (actualMix[key] !== count) throw new Error(`${block.blockId}: ${key} ${actualMix[key]} != ${count}.`);
  cards.forEach((card, index) => {
    const spec = sourceById.get(card.source);
    if (!spec) throw new Error(`${block.blockId}-${index + 1}: unknown source ${card.source}.`);
    const toolKey = `${block.blockId}-${index + 1}`;
    if (slots[index].learningOperation === "tool_selection" && exactToolSourceByCard.get(toolKey) !== card.source) throw new Error(`${toolKey}: tool source ${card.source} is not the reviewed exact source ${exactToolSourceByCard.get(toolKey)}.`);
    if (!card.prompt || !card.correct || !card.reason || card.wrongs.length !== 3) throw new Error(`${block.blockId}-${index + 1}: incomplete card.`);
    if (seenPrompts.has(card.prompt)) throw new Error(`Duplicate prompt: ${card.prompt}`);
    seenPrompts.add(card.prompt);
    const options = [card.correct, ...card.wrongs.map((wrong) => wrong.text)];
    if (new Set(options).size !== 4) throw new Error(`${block.blockId}-${index + 1}: duplicate local option.`);
    for (const option of options) {
      if (seenOptions.has(option)) throw new Error(`${block.blockId}-${index + 1}: duplicate global option: ${option}`);
      seenOptions.add(option);
    }
    const tuple = card.wrongs.map((wrong) => wrong.text).join("\n");
    if (seenWrongTuples.has(tuple)) throw new Error(`${block.blockId}-${index + 1}: duplicate distractor tuple.`);
    seenWrongTuples.add(tuple);
    if (card.wrongs.some((wrong) => !wrong.text || !wrong.why || /all of the above|none of the above|obviously wrong|placeholder|temporary/i.test(`${wrong.text} ${wrong.why}`))) throw new Error(`${block.blockId}-${index + 1}: generic or incomplete distractor.`);
    const topicalTokens = new Set(`${spec.sourceId} ${spec.title} ${spec.authoritativeFor.join(" ")}`.toLowerCase().split(/[^a-z0-9]+/).filter((token) => token.length >= 3));
    const promptTokens = new Set(`${card.prompt} ${card.correct}`.toLowerCase().split(/[^a-z0-9]+/));
    if (![...topicalTokens].some((token) => promptTokens.has(token))) throw new Error(`${block.blockId}-${index + 1}: source ${card.source} lacks topical token relevance.`);
  });
  totalCards += cards.length;
}
if (totalCards !== 128 || seenPrompts.size !== 128 || seenOptions.size !== 512 || seenWrongTuples.size !== 128) throw new Error(`N07 global uniqueness failed: ${totalCards}/${seenPrompts.size}/${seenOptions.size}/${seenWrongTuples.size}.`);

if (process.argv.includes("--check")) {
  console.log(`AZ104-N07 slot-order, exact-mix, topical-source, prompt, option, and explicit-distractor QA passed for ${totalCards} cases and ${seenOptions.size} globally unique options across ${expectedBlocks.length} blocks.`);
  process.exit(0);
}

const registrySources = sourceSpecs.map(({ boundary, transfer, ...source }) => source);
const sourceRecords = new Map(curriculum.sourceRecords.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = sourceRecords.get(source.sourceId);
  sourceRecords.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
curriculum.sourceRecords = [...sourceRecords.values()].sort((a, b) => a.sourceId.localeCompare(b.sourceId));

for (const [blockId, cards] of Object.entries(blocks)) {
  const slots = curriculum.slots.filter((slot) => slot.blockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  slots.forEach((slot, index) => {
    const requirement = slot.sourceRequirements.directFirstPartyDocumentation[0];
    requirement.testedProperties = [`${blockId.toLowerCase()}-${slot.learningOperation}-${String(index + 1).padStart(2, "0")}`];
    requirement.sourceRefs = [cards[index].source];
    requirement.resolutionState = "resolved_exact_direct";
  });
  const target = curriculum.targetPlans.find((entry) => entry.blockId === blockId);
  if (!target) throw new Error(`${blockId}: missing target plan.`);
  target.sourceRefs = [...new Set(cards.map((card) => card.source))].sort();
}

const activeSourceIds = new Set();
for (const slot of curriculum.slots) {
  for (const ref of slot.sourceRequirements.officialObjective?.sourceRefs ?? []) activeSourceIds.add(ref);
  for (const requirement of slot.sourceRequirements.directFirstPartyDocumentation ?? []) for (const ref of requirement.sourceRefs ?? []) activeSourceIds.add(ref);
}
const collectBlueprintSources = (value) => {
  if (Array.isArray(value)) return value.forEach(collectBlueprintSources);
  if (!value || typeof value !== "object") return;
  for (const [key, child] of Object.entries(value)) key === "sourceRefs" && Array.isArray(child) ? child.forEach((ref) => activeSourceIds.add(ref)) : collectBlueprintSources(child);
};
collectBlueprintSources(curriculum.examSimulationBlueprint);
curriculum.sourceRecords = curriculum.sourceRecords.filter((source) => activeSourceIds.has(source.sourceId));
const { promotionProvenance, contentFingerprint, ...fingerprintPayload } = curriculum;
curriculum.contentFingerprint = createHash("sha256").update(JSON.stringify(fingerprintPayload)).digest("hex");
await writeFile(curriculumPath, `${JSON.stringify(curriculum, null, 2)}\n`);

const anchorRegistry = JSON.parse(await readFile(anchorsPath, "utf8"));
const anchorSources = new Map(anchorRegistry.anchors.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = anchorSources.get(source.sourceId);
  anchorSources.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
const retainedSourceIds = new Set(curriculum.sourceRecords.map((source) => source.sourceId));
anchorRegistry.anchors = [...anchorSources.values()].filter((source) => retainedSourceIds.has(source.sourceId)).sort((a, b) => a.sourceId.localeCompare(b.sourceId));
await writeFile(anchorsPath, `${JSON.stringify(anchorRegistry, null, 2)}\n`);

const { manifest, model } = await buildManifest(ROOT);
const manifestTrack = manifest.tracks.find((entry) => entry.trackId === TRACK);
const canonicalTrack = model.curricula.get(TRACK);
const normalizedById = new Map(canonicalTrack.normalized.slots.map((slot) => [slot.slotId, slot]));
const optionIds = ["A", "B", "C", "D"];

for (const [blockId, cards] of Object.entries(blocks)) {
  const batchId = `az104-2026-08-16-${blockId}`;
  const provenance = { authoringMethod: "manual", approvalStatus: "unapproved", author: "Codex", createdAt: CREATED_AT, contentBatchId: batchId };
  const slots = manifestTrack.slots.filter((slot) => slot.learningBlockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  const items = slots.map((slot, index) => {
    const card = cards[index];
    const normalized = normalizedById.get(slot.slotId);
    if (!slot.authoringAdmitted || !slot.sourceBinding || !slot.sourceBinding.sourceRefs.includes(card.source)) throw new Error(`${slot.slotId}: invalid source binding.`);
    const spec = sourceById.get(card.source);
    const raw = [{ text: card.correct, why: card.reason, correct: true }, ...card.wrongs.map((wrong) => ({ ...wrong, correct: false }))];
    const shift = index % 4;
    const arranged = [...raw.slice(shift), ...raw.slice(0, shift)];
    const answer = arranged.findIndex((option) => option.correct);
    return {
      itemId: `az104-${blockId}-${String(index + 1).padStart(3, "0")}`,
      slotId: slot.slotId,
      nodeId: NODE,
      learningBlockId: blockId,
      taxonomy: { examDomainId: normalized.raw.primarySimulationDomainId, competencyAreaId: NODE, topicId: blockId, skillAtomId: normalized.raw.directSkillOrDecisionAtomId },
      prompt: card.prompt,
      constraints: [spec.boundary, spec.transfer],
      interaction: { type: "choice", selectionMode: "single", options: arranged.map((option, i) => ({ optionId: optionIds[i], text: option.text })), acceptedOptionIds: [optionIds[answer]] },
      scoringContract: { type: "choice", resultSemantics: "exact_selected_set_with_partial_v1", selectionMode: "single" },
      feedback: {
        Reason: card.reason,
        Details: { mechanismOrProperty: card.reason, scenarioApplication: card.correct, errorCorrection: arranged.filter((option) => !option.correct).map((option) => option.why).join(" "), boundaryOrTradeoff: spec.boundary, transfer: spec.transfer, url: spec.url },
        wrongOptionExplanationsByOptionId: Object.fromEntries(arranged.map((option, i) => [optionIds[i], option.why]).filter((_, i) => i !== answer)),
        omittedCorrectElementExplanationsByOptionId: {},
      },
      modeEligibility: slot.modeEligibility,
      sourceBinding: slot.sourceBinding,
      authoringProvenance: provenance,
    };
  });
  const payload = { schemaVersion: "certification-manual-source-v2", batchId, trackId: TRACK, familyId: "certification", contentVersion: "microsoft-azure-administrator-associate-az-104-authoring-v2026.08.15", taxonomyVersion: "2026.08.15", nodeId: NODE, learningBlockId: blockId, slotIds: items.map((item) => item.slotId), items, authoringProvenance: provenance };
  const output = join(ROOT, "manual/source", TRACK, NODE, `${blockId}.json`);
  await mkdir(join(output, ".."), { recursive: true });
  await writeFile(output, `${JSON.stringify(payload, null, 2)}\n`);
  console.log(`Authored ${items.length} reviewed items at ${output}.`);
}
