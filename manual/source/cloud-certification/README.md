# Manual Certification ingress

Paste manually authored JSON source batches here. The builder reads only `.json` files in this directory, sorted by relative path. It never reads `tracks/cloud-certification` as a fallback.

This directory intentionally contains no question bank.
