import { createHash } from "node:crypto";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { join } from "node:path";
import { buildManifest, ROOT } from "./lib/model.mjs";

const TRACK = "microsoft-azure-administrator-associate-az-104";
const NODE = "azure_network_security_private_access_and_load_balancing";
const CREATED_AT = "2026-08-16T00:00:00Z";
const curriculumPath = join(ROOT, "config/curricula", `${TRACK}.json`);
const anchorsPath = join(ROOT, "evidence/curriculum", `${TRACK}-source-anchors.json`);

const S = (sourceId, title, url, authority, boundary, transfer, topics) => ({
  sourceId,
  provider: "Microsoft",
  sourceType: "direct_first_party_product_documentation",
  title,
  url,
  checkedDate: "2026-08-16",
  sourceVolatility: "high",
  versionContext: "current Microsoft Learn documentation checked for AZ104-N08 authoring",
  resolutionState: "resolved_exact_first_party",
  authoritativeFor: [authority],
  propertyAuthorityBasis: "manual_slot_property_review_2026_08_16",
  boundary,
  transfer,
  topics,
});

const sourceSpecs = [
  S("nsg-overview", "Network security groups", "https://learn.microsoft.com/en-us/azure/virtual-network/network-security-groups-overview", "nsg-rule-processing-and-default-rules", "NSGs filter five-tuple flows by direction and priority; subnet and NIC NSGs both apply, and stateful return traffic does not need a mirrored rule.", "Model the exact flow, both associations, priorities, service tags, and default rules before changing access.", ["nsg", "security", "rule", "priority", "stateful"]),
  S("asg-overview", "Application security groups", "https://learn.microsoft.com/en-us/azure/virtual-network/application-security-groups", "application-security-group-membership", "ASGs group NIC IP configurations so NSG rules can express application roles without hard-coded IP addresses; members used together must satisfy documented VNet constraints.", "Use ASGs for stable workload roles, then verify NIC membership and effective NSG rules after scale events.", ["asg", "application", "group", "membership", "nsg"]),
  S("nsg-effective", "Diagnose a virtual machine network traffic filter problem", "https://learn.microsoft.com/en-us/azure/virtual-network/diagnose-network-traffic-filter-problem", "effective-nsg-diagnosis", "Effective security rules combine subnet and NIC NSGs; an allow in one association cannot override a deny in the other.", "Capture the real packet tuple and the winning rule at every association before editing policy.", ["effective", "nsg", "deny", "nic", "subnet"]),
  S("ip-flow-verify", "IP flow verify overview", "https://learn.microsoft.com/en-us/azure/network-watcher/ip-flow-verify-overview", "network-watcher-ip-flow-verify", "IP flow verify reports whether a specified packet is allowed or denied by effective NSG rules and identifies the matching rule.", "Supply the actual direction, protocol, local and remote addresses, and ports; then validate the application separately.", ["ip", "flow", "verify", "nsg", "packet"]),
  S("nsg-rule-cli", "az network nsg rule", "https://learn.microsoft.com/en-us/cli/azure/network/nsg/rule?view=azure-cli-latest", "azure-cli-nsg-rule-syntax", "The Azure CLI NSG rule command group creates and inspects rules with explicit priority, direction, access, protocol, addresses, and ports.", "Review current CLI syntax and avoid broad prefixes or service tags when the intended flow is narrower.", ["az", "network", "nsg", "rule", "cli"]),
  S("asg-cli", "az network asg", "https://learn.microsoft.com/en-us/cli/azure/network/asg?view=azure-cli-latest", "azure-cli-asg-syntax", "The Azure CLI ASG command group creates and manages application security group resources.", "Create the ASG in the workload region, assign NIC IP configurations, and validate the NSG rules that consume it.", ["az", "network", "asg", "cli", "group"]),
  S("vnet-flow-logs", "Virtual network flow logs", "https://learn.microsoft.com/en-us/azure/network-watcher/vnet-flow-logs-overview", "vnet-flow-log-observability", "VNet flow logs record IP flow metadata for a virtual network and are the recommended successor to NSG flow logs.", "Choose retention and analytics deliberately and remember that flow metadata is not packet payload capture.", ["flow", "logs", "vnet", "traffic", "watcher"]),
  S("nsg-flow-logs", "NSG flow logs", "https://learn.microsoft.com/en-us/azure/network-watcher/nsg-flow-logs-overview", "nsg-flow-log-retirement", "NSG flow logs are on a retirement path and new creation is no longer the strategic design; VNet flow logs are the migration target.", "For new observability designs, confirm current retirement dates and use VNet flow logs unless a documented transition constraint applies.", ["nsg", "flow", "logs", "retirement", "vnet"]),
  S("bastion-overview", "What is Azure Bastion?", "https://learn.microsoft.com/en-us/azure/bastion/bastion-overview", "bastion-private-administration", "Azure Bastion provides managed RDP and SSH to VM private IPs without public IPs on target VMs through a dedicated AzureBastionSubnet.", "Size the dedicated subnet for the SKU and features, then scope target NSG access to the Bastion path.", ["bastion", "rdp", "ssh", "private", "admin"]),
  S("bastion-settings", "Configure Azure Bastion settings", "https://learn.microsoft.com/en-us/azure/bastion/configuration-settings", "bastion-sku-and-features", "Bastion capabilities such as native client, IP-based connection, tunneling, shareable links, and host scaling depend on SKU and settings.", "Select a supported SKU from administration requirements before enabling features or automation.", ["bastion", "sku", "native", "tunneling", "scaling"]),
  S("bastion-native", "Connect to a VM using the native client", "https://learn.microsoft.com/en-us/azure/bastion/connect-vm-native-client-windows", "bastion-native-client-connectivity", "Native-client connections require a supported Bastion SKU and client tooling, while target VMs can remain private.", "Confirm the Bastion feature setting, user authorization, target protocol, and NSG path before troubleshooting credentials.", ["bastion", "native", "client", "ssh", "rdp"]),
  S("bastion-cli", "az network bastion", "https://learn.microsoft.com/en-us/cli/azure/network/bastion?view=azure-cli-latest", "azure-cli-bastion-connect-syntax", "The Azure CLI Bastion command group documents tunnel, SSH, and RDP operations for supported configurations.", "Use the operation matching the target protocol and pass the Bastion and VM resource identifiers in the correct subscription context.", ["az", "network", "bastion", "ssh", "tunnel"]),
  S("service-endpoints", "Virtual network service endpoints", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-service-endpoints-overview", "service-endpoint-routing-and-identity", "A service endpoint extends a subnet's identity to a supported Azure service over the Azure backbone; the service still uses its public endpoint and DNS name.", "Enable the endpoint on the client subnet and configure the resource firewall; do not treat it as a private IP deployment.", ["service", "endpoint", "subnet", "firewall", "public"]),
  S("storage-network", "Configure Azure Storage firewalls and virtual networks", "https://learn.microsoft.com/en-us/azure/storage/common/storage-network-security", "storage-vnet-firewall-rules", "Storage network rules can admit selected subnets using service endpoints while denying other network origins.", "Enable the correct Storage service endpoint, add the subnet rule, and test from the intended source after propagation.", ["storage", "firewall", "subnet", "service", "endpoint"]),
  S("service-endpoint-policies", "Virtual network service endpoint policies", "https://learn.microsoft.com/en-us/azure/virtual-network/virtual-network-service-endpoint-policies-overview", "service-endpoint-policy-scope", "Service endpoint policies restrict supported service traffic from a subnet to allowed Azure resource destinations; they do not create inbound private endpoints.", "Associate a policy only after validating supported services and resources, then preserve other required service destinations.", ["service", "endpoint", "policy", "storage", "subnet"]),
  S("subnet-cli", "az network vnet subnet", "https://learn.microsoft.com/en-us/cli/azure/network/vnet/subnet?view=azure-cli-latest", "azure-cli-subnet-service-endpoint-syntax", "The Azure CLI subnet command group documents service-endpoints and service-endpoint-policy association settings.", "Update the intended subnet and verify that existing delegation, policies, and prefixes are preserved.", ["az", "network", "vnet", "subnet", "service"]),
  S("app-service-vnet-integration", "Integrate your app with an Azure virtual network", "https://learn.microsoft.com/en-us/azure/app-service/overview-vnet-integration", "app-service-outbound-vnet-integration", "App Service VNet integration gives an app outbound access through a dedicated integration subnet; it does not provide private inbound access to the app.", "Treat outbound integration and inbound private endpoints as separate paths with their own subnet, routing, DNS, and access controls.", ["app", "service", "vnet", "integration", "outbound"]),
  S("private-endpoint", "What is a private endpoint?", "https://learn.microsoft.com/en-us/azure/private-link/private-endpoint-overview", "private-endpoint-private-ip-model", "A private endpoint is a NIC with a private IP in a consumer VNet that connects to a specific Private Link resource subresource.", "Plan the target subresource, approval state, DNS, subnet policies, and client routing as separate controls.", ["private", "endpoint", "subresource", "nic", "approval"]),
  S("private-endpoint-dns", "Azure private endpoint DNS configuration", "https://learn.microsoft.com/en-us/azure/private-link/private-endpoint-dns", "private-endpoint-dns-integration", "Private endpoint clients must resolve the service FQDN through the recommended privatelink zone and record path to the endpoint's private IP.", "Link or forward the correct private zone to every client network and avoid overriding unrelated public resolution.", ["private", "endpoint", "dns", "zone", "fqdn"]),
  S("private-endpoint-policies", "Manage network policies for private endpoints", "https://learn.microsoft.com/en-us/azure/private-link/disable-private-endpoint-network-policy", "private-endpoint-subnet-network-policies", "Private endpoint network-policy support is controlled on the subnet and determines whether supported NSG and UDR processing applies to private endpoint traffic.", "Confirm the current subnet policy state and feature support before relying on an NSG or UDR around an endpoint.", ["private", "endpoint", "network", "policy", "subnet"]),
  S("private-link-service", "What is Azure Private Link service?", "https://learn.microsoft.com/en-us/azure/private-link/private-link-service-overview", "private-link-service-provider-model", "A Private Link service publishes a provider service behind a Standard Load Balancer so consumers can create approved private endpoints.", "Separate provider NAT configuration, visibility and auto-approval from each consumer endpoint and DNS design.", ["private", "link", "service", "provider", "load"]),
  S("private-endpoint-cli", "az network private-endpoint", "https://learn.microsoft.com/en-us/cli/azure/network/private-endpoint?view=azure-cli-latest", "azure-cli-private-endpoint-syntax", "The Azure CLI private-endpoint command group creates endpoints with a target resource ID, group ID, subnet, and connection name.", "Use the target service's exact group ID and verify approval plus DNS after resource creation.", ["az", "network", "private", "endpoint", "group"]),
  S("private-dns-zone-group-cli", "az network private-endpoint dns-zone-group", "https://learn.microsoft.com/en-us/cli/azure/network/private-endpoint/dns-zone-group?view=azure-cli-latest", "azure-cli-private-endpoint-zone-group-syntax", "A private endpoint DNS zone group associates the endpoint with private DNS zone configuration for supported record management.", "Attach the recommended zone and validate its VNet links rather than assuming the endpoint alone changes client DNS.", ["az", "network", "private", "dns", "zone"]),
  S("lb-components", "Azure Load Balancer components", "https://learn.microsoft.com/en-us/azure/load-balancer/components", "load-balancer-component-model", "A Load Balancer design combines frontend IP configuration, backend pool, health probe, and rule; each component has a distinct role.", "Validate frontend SKU and zone, backend membership, probe reachability, rule ports, and NSGs as one data path.", ["load", "balancer", "frontend", "backend", "rule"]),
  S("lb-probes", "Azure Load Balancer health probes", "https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-custom-probe-overview", "load-balancer-health-probe-behavior", "Health probes determine which backend instances receive new flows; probe protocol, port, path, interval, and threshold must match workload readiness.", "Permit AzureLoadBalancer probe traffic and make the probe test service readiness rather than only process existence.", ["load", "balancer", "health", "probe", "backend"]),
  S("lb-distribution", "Azure Load Balancer distribution modes", "https://learn.microsoft.com/en-us/azure/load-balancer/distribution-mode-concepts", "load-balancer-session-distribution", "The default distribution hashes the five-tuple; source-IP affinity modes reduce tuple inputs to preserve client affinity with documented tradeoffs.", "Choose affinity only when application state requires it and test scale and failover behavior.", ["load", "balancer", "distribution", "affinity", "tuple"]),
  S("lb-floating-ip", "Floating IP configuration", "https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-floating-ip", "load-balancer-floating-ip", "Floating IP changes the frontend-to-backend address mapping and is used for direct server return and specific high-availability port scenarios.", "Enable it only for a workload architecture that configures the frontend address correctly on backend hosts.", ["load", "balancer", "floating", "direct", "return"]),
  S("lb-outbound", "Outbound rules Azure Load Balancer", "https://learn.microsoft.com/en-us/azure/load-balancer/outbound-rules", "load-balancer-outbound-snat", "Outbound rules provide explicit SNAT configuration for Standard Load Balancer backends using a frontend IP and allocated ports.", "Size SNAT ports for concurrency and prefer NAT Gateway when its subnet-level model better fits outbound-only scale.", ["load", "balancer", "outbound", "snat", "rule"]),
  S("lb-probe-cli", "az network lb probe", "https://learn.microsoft.com/en-us/cli/azure/network/lb/probe?view=azure-cli-latest", "azure-cli-load-balancer-probe-syntax", "The Azure CLI load balancer probe command group creates and updates probes with protocol, port, path, interval, and threshold settings.", "Match the command to the workload listener and validate NSG admission from the AzureLoadBalancer service tag.", ["az", "network", "load", "probe", "cli"]),
  S("lb-rule-cli", "az network lb rule", "https://learn.microsoft.com/en-us/cli/azure/network/lb/rule?view=azure-cli-latest", "azure-cli-load-balancer-rule-syntax", "The Azure CLI load balancer rule command group binds frontend and backend ports, protocol, pool, and probe.", "Reference existing components of the same Load Balancer and check HA Ports or Floating IP only when required.", ["az", "network", "load", "rule", "cli"]),
  S("lb-troubleshoot-probe", "Troubleshoot Azure Load Balancer health probe status", "https://learn.microsoft.com/en-us/azure/load-balancer/load-balancer-troubleshoot-health-probe-status", "load-balancer-probe-diagnosis", "Unhealthy probe status can result from listener, path, TLS, NSG, guest firewall, or platform configuration issues.", "Test the exact probe from the backend context and preserve the production listener while isolating each failed layer.", ["load", "balancer", "probe", "unhealthy", "troubleshoot"]),
  S("app-gateway-overview", "What is Azure Application Gateway?", "https://learn.microsoft.com/en-us/azure/application-gateway/overview", "application-gateway-layer-seven-routing", "Application Gateway is a regional layer-seven HTTP(S) load balancer with host, path, TLS, and backend HTTP settings.", "Choose it when request-layer routing or TLS control is required, then configure health probes and backend host semantics explicitly.", ["application", "gateway", "http", "layer", "routing"]),
  S("waf-overview", "Azure Web Application Firewall on Application Gateway", "https://learn.microsoft.com/en-us/azure/web-application-firewall/ag/ag-overview", "application-gateway-waf-protection", "WAF on Application Gateway evaluates HTTP(S) requests against managed and custom web rules; an NSG does not provide equivalent application-layer inspection.", "Tune exclusions from evidence, keep policy mode explicit, and preserve network controls around the gateway.", ["waf", "application", "gateway", "http", "protection"]),
  S("traffic-manager", "What is Traffic Manager?", "https://learn.microsoft.com/en-us/azure/traffic-manager/traffic-manager-overview", "traffic-manager-dns-load-balancing", "Traffic Manager is DNS-based global traffic distribution and does not proxy the application connection.", "Account for DNS TTL and endpoint monitoring when designing failover expectations.", ["traffic", "manager", "dns", "global", "endpoint"]),
  S("front-door-overview", "What is Azure Front Door?", "https://learn.microsoft.com/en-us/azure/frontdoor/front-door-overview", "front-door-global-http-proxy", "Azure Front Door is a global layer-seven reverse proxy for HTTP(S), with edge routing and health-based origin selection.", "Choose it for global web acceleration and edge controls, and validate origin reachability and host headers.", ["front", "door", "global", "http", "origin"]),
  S("firewall-overview", "What is Azure Firewall?", "https://learn.microsoft.com/en-us/azure/firewall/overview", "azure-firewall-central-inspection", "Azure Firewall is a managed stateful network security service for centralized traffic filtering and logging, not an application delivery load balancer.", "Route intended flows symmetrically through the firewall and separate inspection policy from load-balancer health and distribution.", ["azure", "firewall", "inspection", "routing", "security"]),
];

const sourceById = new Map(sourceSpecs.map((source) => [source.sourceId, source]));
const P = (value) => { const split = value.indexOf("|"); return { text: value.slice(0, split), why: value.slice(split + 1) }; };
const Q = (source, prompt, correct, reason, ...wrongs) => ({ source, prompt, correct, reason, wrongs: wrongs.map(P) });

const blocks = {
  "AZ104-N08-B01": [
    Q("nsg-overview", "A web subnet must accept HTTPS from the Internet but reject direct SSH. Which NSG design is least-privilege?", "Create an inbound TCP 443 allow rule for the web subnet and leave SSH to the default inbound deny.", "An explicit narrow HTTPS allow is evaluated before the default deny, while no SSH exception expands administration exposure.", "Allow the Internet service tag to destination port * at priority 100.|The wildcard destination ports also expose SSH and every other listener.", "Create an outbound deny for TCP 22 only.|An outbound rule does not block new inbound SSH sessions.", "Delete DefaultRule_DenyAllInBound after adding HTTPS.|Default rules cannot be deleted and removing the deny model would be unsafe."),
    Q("nsg-overview", "An API tier should accept TCP 8443 only from a web-tier subnet 10.20.1.0/24. Which rule expresses that flow?", "Allow inbound TCP from 10.20.1.0/24 to the API subnet on destination port 8443 with a priority above broader denies.", "NSG address and port fields can describe the exact source tier and destination listener.", "Allow inbound UDP from 10.20.1.0/24 to source port 8443.|The application requires TCP and 8443 is the destination listener port.", "Allow outbound TCP 8443 on the API subnet.|The initiating web-to-API packet is evaluated inbound at the API association.", "Allow the VirtualNetwork tag to every destination port.|That admits unrelated connected networks and services beyond the stated web tier."),
    Q("bastion-overview", "Administrators use Azure Bastion, and workload VMs must not accept RDP from peer VNets. Which NSG source should the RDP allow target?", "Target only the AzureBastionSubnet prefix as the inbound source for TCP 3389 on the workload tier.", "A subnet-prefix rule limits RDP admission to the managed Bastion path instead of the broader VirtualNetwork tag.", "Target the Internet service tag because Bastion has a public frontend.|Target VMs receive Bastion traffic from the Bastion subnet, not directly from Internet clients.", "Target 0.0.0.0/0 and rely on VM credentials.|Authentication does not reduce network exposure to the approved administration path.", "Target the workload subnet itself.|That does not admit the separate Bastion subnet and can enable lateral RDP among workloads."),
    Q("nsg-overview", "A subnet hosts DNS servers that accept client queries on UDP 53 and zone transfers from one secondary on TCP 53. Which rule set is correct?", "Use separate inbound rules: UDP 53 from client prefixes and TCP 53 only from the secondary DNS address.", "Protocol-specific rules preserve broad query access while restricting zone-transfer traffic to the authorized peer.", "Use one Any-protocol port 53 rule from all connected networks.|That grants TCP zone-transfer access to every client prefix.", "Allow UDP 53 from the secondary and TCP 53 from clients.|The protocols and audiences are reversed.", "Open source port 53 and leave destination port unrestricted.|Clients normally use ephemeral source ports, and the server listener is destination port 53."),
    Q("nsg-overview", "A deployment needs to allow Azure Load Balancer health probes without allowing arbitrary Internet traffic. Which NSG source is designed for this?", "Use the AzureLoadBalancer service tag in a narrow inbound probe-port allow rule.", "The platform service tag represents Load Balancer probe origins and avoids an Internet-wide source prefix.", "Use the Storage service tag because probe status is stored by Azure.|Storage addresses do not represent Load Balancer probe sources.", "Use the Internet service tag with the probe port.|That exposes the listener to all public sources rather than the platform probe set.", "Use the VirtualNetwork tag and deny AzureLoadBalancer.|The platform probe source is not safely replaced by the connected-network aggregate."),
    Q("nsg-overview", "A security team wants to deny an untrusted subnet 10.30.5.0/24 while retaining a broader VirtualNetwork allow. How should priority be assigned?", "Create the specific deny with a numerically lower priority than the broader VirtualNetwork allow.", "NSGs process rules from lowest priority number upward and stop at the first match.", "Give the deny priority 65001 so it runs after default rules.|Custom priorities cannot override a higher-precedence broad allow from a later number.", "Assign both rules the same priority and rely on specificity.|NSG rules require unique priorities and do not resolve ties by prefix length.", "Place the deny only in an outbound NSG on the untrusted subnet.|That does not guarantee inbound filtering at every protected destination."),
    Q("nsg-overview", "What sequence safely introduces an inbound HTTPS rule into a production NSG?", "Identify the exact source and destination tuple, choose an unused priority, create the narrow allow, inspect effective rules, test HTTPS, and retain the default deny.", "The sequence preserves least privilege and validates the composed policy before broadening anything else.", "Delete all existing inbound rules, add HTTPS, and reconstruct them later.|Removing policy first creates an avoidable exposure and outage window.", "Add AllowAnyInbound at priority 100, test, then narrow it if convenient.|The broad diagnostic rule can mask the real required tuple and expose unrelated listeners.", "Change the application to HTTP before inspecting NSG state.|Changing protocol does not establish the required secure network policy."),
    Q("nsg-overview", "What sequence changes an NSG rule priority without creating a traffic gap?", "Inventory overlapping rules, add or update the intended narrow rule at a validated unused priority, confirm effective behavior, then remove the superseded rule.", "Evaluating overlap and effective state avoids an accidental interval with no valid allow or an unintended broad match.", "Remove the current rule first and wait for users to report failures.|That deliberately creates a gap and uses production impact as the test.", "Set every custom rule to priority 100.|Priorities must be unique within direction and duplicate values are invalid.", "Rename the NSG because names determine evaluation order.|Evaluation follows numeric priority, not lexical rule names."),
    Q("nsg-overview", "A database listener moves from TCP 1433 to TCP 15433. What is the safe NSG update sequence?", "Confirm the new listener, add a source-scoped TCP 15433 allow, test from the application tier, remove the old 1433 allow, then recheck effective rules.", "Coordinating listener and policy changes preserves service while eliminating the obsolete port after verification.", "Change the NSG destination port to 15433 before the database listens there.|That causes an outage with no reachable listener during the transition.", "Keep both ports open indefinitely because NSGs are stateful.|Statefulness does not justify retaining an obsolete new-flow admission rule.", "Add UDP 15433 and remove TCP 1433.|A TCP database listener cannot be reached through a UDP rule."),
    Q("nsg-overview", "What sequence uses a service tag safely in a new outbound rule?", "Verify the tag represents the required Azure service and region scope, constrain protocol and destination port, stage the rule at the intended priority, and validate effective egress.", "Service tags simplify address maintenance but do not remove the need to scope protocol, port, direction, and precedence.", "Copy the service tag into the source field of an inbound allow.|That reverses the intended outbound destination semantics.", "Pair the service tag with destination port * because Microsoft owns the addresses.|Ownership does not make every service port necessary.", "Export the tag IP list and hard-code it permanently.|Service tag membership changes, so a frozen list defeats managed updates."),
    Q("nsg-overview", "What sequence retires a broad inbound VirtualNetwork allow after application flows have been enumerated?", "Create tested source-and-port-specific replacement rules, inspect effective rules on representative NICs, remove the broad allow, and monitor denied flows.", "Specific replacements must be proven across affected tiers before the permissive rule is removed.", "Delete the VirtualNetwork allow first and infer missing dependencies from outages.|That risks production and produces incomplete dependency evidence.", "Keep the broad allow above the specific rules for rollback.|The earlier broad match makes the narrow replacements ineffective.", "Convert the broad allow into an outbound rule with the same priority.|Changing direction does not preserve required inbound flows."),
    Q("nsg-overview", "A new deny rule will block an obsolete partner prefix. What rollout sequence preserves established-flow awareness?", "Record current connections, add the higher-priority deny, test new connections from the partner, monitor stateful existing flows, and terminate them separately if policy requires immediate cutoff.", "NSGs are stateful, so changing a rule affects new flow evaluation while existing connection records can persist.", "Restart every VM before adding the deny.|A broad restart is unnecessary and does not validate the intended rule tuple.", "Add an outbound allow to force the inbound deny to reevaluate old sessions.|A separate allow does not flush stateful flow records.", "Lower the deny beneath the existing broad allow and expect prefix specificity to win.|NSGs use first matching priority, not longest-prefix precedence."),
    Q("nsg-effective", "An inbound TCP 443 rule allows a client in the subnet NSG, but the connection is denied. The NIC NSG has a higher-priority deny for that source. What is the effective result?", "The flow is denied because it must be allowed by both the subnet and NIC NSG associations.", "Effective filtering composes both NSGs; an allow at one layer cannot override a deny at another.", "The subnet allow wins because subnet policy is evaluated first.|Passing one association does not bypass a deny at the other.", "The most recently edited rule wins across associations.|Modification time is not an NSG precedence rule.", "Azure merges the two rules and selects the narrower destination port.|Rules in separate NSGs are both enforced rather than merged by specificity."),
    Q("nsg-overview", "A newly added inbound allow does not work. Its priority is 300, while an existing deny for the same tuple is priority 200. Why?", "The priority-200 deny matches first, so processing stops before the priority-300 allow.", "Lower numeric priority values are evaluated earlier for a direction.", "The allow needs a higher numeric value than every deny.|Higher numbers execute later and cannot override an earlier matching deny.", "Inbound allows are evaluated only after all inbound denies.|Access type does not define a separate evaluation phase.", "The rule needs the same name as the application.|Rule names do not link policy to processes."),
    Q("nsg-overview", "A TCP client remains connected after its allow rule is removed, but new connections fail. What explains this state?", "NSGs are stateful; the existing flow record can continue while new connection attempts are evaluated against the updated rules.", "Rule changes do not necessarily terminate already established stateful flows immediately.", "The VM is ignoring all NSG changes until reboot.|New failures prove the updated policy is already being evaluated.", "TCP 443 is covered by an immutable default allow.|There is no default Internet HTTPS allow in an NSG.", "The removed rule was automatically recreated by Azure Policy.|That would also normally permit new matching connections and requires separate evidence."),
    Q("nsg-overview", "An outbound allow exists for an HTTPS request, yet responses are blocked after an explicit inbound deny was added. Which NSG behavior should be checked first?", "Check whether the response belongs to an established stateful flow; a mirrored inbound allow is normally unnecessary, but a different new flow still needs its own admission.", "NSG state tracks permitted connections and automatically permits return traffic for that flow.", "Add an inbound allow for every ephemeral client port by default.|Mirrored response rules are not required for established stateful connections.", "Delete the outbound HTTPS rule because only inbound rules matter.|The initiating egress packet still requires outbound admission.", "Convert the HTTPS rule to UDP so return traffic is stateless.|Changing protocol breaks the TCP application rather than fixing evaluation."),
    Q("nsg-rule-cli", "Which Azure CLI command creates a source-scoped inbound HTTPS allow in an existing NSG?", "Run `az network nsg rule create` with the NSG, priority, direction Inbound, access Allow, protocol Tcp, source prefix, and destination port 443.", "The NSG rule create operation exposes the full rule tuple and priority required for deterministic policy.", "Run `az network nsg create --open-port 443`.|NSG creation does not use that shortcut to define the reviewed source-scoped rule.", "Run `az network watcher test-ip-flow --access Allow`.|IP flow verify diagnoses effective state and does not create policy.", "Run `az vm open-port --port 443 --source-address-prefixes *`.|That shortcut is broader and does not express the reviewed existing-NSG rule contract."),
    Q("nsg-rule-cli", "Which Azure CLI operation lists the custom rules in a named NSG before a priority change?", "Run `az network nsg rule list --resource-group <rg> --nsg-name <nsg>` and inspect priorities and tuples.", "The rule list operation returns the administrator-defined rules needed to detect overlap and priority conflicts.", "Run `az network nsg list-effective` without a NIC.|Effective composition is tied to a network interface and is not this rule-list operation.", "Run `az network route-table route list --nsg-name <nsg>`.|Route tables do not contain NSG security rules.", "Run `az policy state list --security-rule-only`.|Policy state is not the authoritative list of rules inside the NSG."),
    Q("nsg-overview", "What is the boundary between an NSG service tag and an ASG?", "A service tag represents Microsoft-managed IP prefixes for a service or network category; an ASG represents selected workload NIC IP configurations for use in NSG rules.", "The two abstractions solve address maintenance for different ownership domains: platform services versus application roles.", "A service tag groups NICs manually while an ASG is maintained by Microsoft.|Those ownership models are reversed.", "Both create routes to their members.|They are security-rule operands, not route-table next hops.", "An ASG can be used in any Azure service firewall that accepts a service tag.|ASGs are an NSG workload-group abstraction, not a universal platform service tag."),
    Q("nsg-overview", "What is the boundary between NSG statefulness and application session persistence?", "NSG statefulness permits return packets for an admitted network flow; it does not preserve user sessions across backend instances or application restarts.", "Transport flow tracking and application session storage operate at different layers.", "NSG state automatically replicates application cookies between servers.|Security flow records do not synchronize application data.", "Session persistence makes an NSG outbound rule unnecessary.|Application affinity cannot bypass network policy.", "Both are configured through Load Balancer distribution mode.|NSG flow tracking is inherent to NSG processing, not a load-balancing affinity option."),
  ],
  "AZ104-N08-B02": [
    Q("asg-overview", "A VM scale set web tier changes addresses during scale operations. How should NSG rules identify it without maintaining CIDR lists?", "Place the web-tier NIC IP configurations in an ASG and reference that ASG in role-based NSG rules.", "ASG membership follows selected NIC IP configurations and keeps security intent independent of changing workload addresses.", "Put the VM names in a service tag.|Service tags are Microsoft-managed prefix sets and cannot be populated with customer VM names.", "Create one NSG rule per not-yet-assigned private IP.|Scale-set addresses are not stable enough for manual per-IP policy.", "Use a DNS hostname as the NSG source.|NSG source fields do not dynamically resolve application hostnames into members."),
    Q("asg-overview", "Web and database NICs share a subnet, but only web members may initiate TCP 1433 to database members. Which design fits?", "Assign Web-ASG and Db-ASG memberships, then allow outbound or inbound TCP 1433 between those ASGs with matching effective policy.", "ASGs express source and destination application roles inside NSG rules even when tiers share a subnet.", "Split traffic by VM resource tags directly in the NSG.|Azure resource tags are not NSG address operands.", "Allow the entire subnet to destination port 1433.|That grants database access to unrelated subnet workloads.", "Add a route whose next hop is Db-ASG.|ASGs are not route targets and cannot forward packets."),
    Q("nsg-overview", "A NIC and its subnet each have an NSG. The subnet allows TCP 22 from Bastion, and the NIC has no matching custom allow. Will the connection pass?", "It passes only if the NIC NSG's effective rules also allow the flow, such as through an applicable default VirtualNetwork rule; both layers must admit it.", "Subnet admission is necessary but not sufficient when a NIC NSG is also associated.", "It always passes because subnet NSGs override NIC NSGs.|Neither association has override status; both filters apply.", "It always fails because two NSGs cannot be attached along one path.|Subnet and NIC NSGs are supported together.", "The rule with the shortest NSG name decides.|Resource naming has no precedence in effective rules."),
    Q("vnet-flow-logs", "A new network-flow observability design is required in 2026. Which flow-log model should be selected?", "Use Virtual Network flow logs and design storage, retention, and analytics around the VNet-level records.", "VNet flow logs are the recommended current model and avoid starting a new dependency on retiring NSG flow logs.", "Create new NSG flow logs because only they record five-tuples.|NSG flow logs are on a retirement path, while VNet flow logs provide current flow metadata.", "Enable packet capture continuously on every VM as the primary log.|Continuous full packet capture is heavier and solves a different diagnostic need.", "Use Azure Activity Log to reconstruct every data-plane flow.|Activity Log records control-plane events, not each network flow tuple."),
    Q("asg-overview", "What sequence migrates IP-prefix NSG rules to ASG-based role rules?", "Create role ASGs, assign representative NIC IP configurations, add equivalent ASG rules at validated priorities, test effective access, expand membership, then remove obsolete prefix rules.", "A staged migration proves membership and rule behavior before the address-coupled policy is retired.", "Delete all prefix rules before creating ASGs.|That causes a policy gap while the replacement has no members or rules.", "Create ASGs and assume existing rules use them automatically.|ASG resources do not rewrite NSG rules by themselves.", "Move the subnet into an ASG.|ASGs contain NIC IP configurations, not subnet resources."),
    Q("asg-overview", "What sequence adds a newly scaled database VM to role-based network policy?", "Assign its intended NIC IP configuration to Db-ASG, verify membership and effective rules, test only the approved application flow, and monitor denies.", "Membership connects the new NIC to existing ASG-based policy without duplicating address rules.", "Add the VM resource ID to the NSG destination prefix.|NSG prefixes accept address constructs, not VM resource identifiers.", "Clone every ASG rule with the VM name embedded.|Duplicating role rules defeats centralized membership.", "Wait for DNS registration to place the VM in the ASG.|ASG membership is explicit and independent of DNS."),
    Q("nsg-effective", "What sequence diagnoses a flow when both subnet and NIC NSGs are present?", "Capture the exact tuple, run IP flow verify, inspect effective security rules for the NIC, identify the winning rule at each association, change the narrowest responsible rule, and retest.", "The combined effective view prevents a misleading fix at only one policy layer.", "Inspect only the NSG with more custom rules.|Rule count does not identify the association causing the deny.", "Detach both NSGs and test the application first.|Removing protection broadens exposure and destroys the failing effective state.", "Add matching allows to both NSGs without finding the deny.|Blind duplication can preserve the higher-priority block and accumulate policy debt."),
    Q("vnet-flow-logs", "What sequence enables VNet flow logs for an investigation with retained evidence?", "Choose the target VNet and Network Watcher context, configure a storage destination and retention or analytics settings, enable the flow log, generate known test traffic, and validate emitted records.", "Known traffic and destination validation prove that collection, not just configuration, is working.", "Enable NSG diagnostics on one NIC and call it a VNet flow log.|A NIC diagnostic setting is not the VNet flow-log resource.", "Start with an indefinite zero-retention design and no storage review.|Retention and cost are part of an operational evidence design.", "Change NSG rules until logs appear.|Policy changes are not required merely to prove logging configuration."),
    Q("nsg-effective", "A subnet NSG allows Web-ASG to Db-ASG on 1433, but a NIC NSG denies VirtualNetwork on all ports at priority 150. What is the effective state?", "Database traffic is denied at the NIC because the priority-150 VirtualNetwork deny matches there despite the subnet ASG allow.", "Each association must allow the packet, and the NIC's earlier matching deny wins within that NSG.", "The ASG rule wins because role rules are more specific.|NSGs use numeric priority rather than semantic specificity.", "The two rules are averaged into an allow for port 1433.|Effective state is conjunctive across associations, not an average.", "The deny applies only to peered VNets, not the local subnet.|VirtualNetwork includes the local VNet context represented by the service tag."),
    Q("asg-overview", "A new VM was added to Web-ASG, but its secondary NIC IP configuration sends traffic outside the expected rule. Why?", "ASG membership is assigned per NIC IP configuration, so the secondary configuration must be inspected and assigned deliberately.", "Membership on one IP configuration does not automatically prove identical membership for another.", "ASGs support only primary VM disks, not secondary NICs.|ASGs operate on NIC IP configurations and are unrelated to disks.", "The NSG caches ASG membership until the VM is reimaged.|Reimaging is not the membership control.", "Secondary IP configurations always inherit every ASG in the subscription.|Membership is explicit rather than subscription-wide inheritance."),
    Q("nsg-effective", "A configured subnet NSG rule allows a flow, yet the application times out. What is the next network-policy check?", "Inspect the combined effective security rules, including the NIC NSG and reverse or stateful flow, then continue to routes, guest firewall, and listener if the effective NSG result allows.", "One configured subnet allow does not establish the combined effective policy or the entire end-to-end path.", "Create another identical subnet allow at a lower priority.|Duplicate admission does not test the other policy and host layers.", "Assume DNS is broken even though the intended destination IP is known.|A fixed-IP policy symptom does not implicate name resolution first.", "Replace the VM because a configured NSG allow guarantees the listener.|A configured allow neither proves combined filtering nor application readiness."),
    Q("nsg-overview", "Effective rules show `DenyAllInBound` after an expected ASG allow. What should be verified?", "Verify the destination NIC IP configuration is actually in the referenced ASG and that the rule's direction, protocol, port, and priority match the tested tuple.", "A missing membership or tuple mismatch causes processing to fall through to the default deny.", "Raise `DenyAllInBound` to a larger priority number.|Default rules are fixed and the missing custom match must be corrected.", "Convert the ASG into a service tag.|ASGs and service tags have different ownership models and are not convertible.", "Add a route to the ASG resource ID.|Routing cannot make a security rule operand match."),
    Q("nsg-flow-logs", "An automation job for new NSG flow logs fails after the service stopped accepting new configurations. What is the correct response?", "Migrate the design to VNet flow logs and validate the current retirement guidance, storage, analytics, and schema consumers.", "The failure aligns with the NSG flow-log retirement path and requires a supported collection model.", "Retry creation with a lower NSG rule priority.|Security-rule priority has no effect on diagnostic resource availability.", "Create a classic NSG because retirement applies only to Standard SKU.|NSGs do not use that SKU distinction to preserve new legacy flow-log creation.", "Disable Network Watcher and recreate it weekly.|Removing the regional diagnostic service does not restore a retired creation path."),
    Q("nsg-effective", "An ASG rule worked until a VM NIC was replaced. The new NIC is not a member. What effective state results?", "The new NIC no longer matches the ASG rule and can fall through to another rule or default deny until membership is restored.", "ASG membership belongs to NIC IP configurations and does not follow a replaced object automatically.", "The VM name preserves ASG membership independently of its NIC.|The security group does not bind by VM display name.", "The old NIC forwards ASG matches to the replacement.|Detached NIC policy membership is not proxied to another NIC.", "Azure converts the ASG rule to the VM's current IP automatically.|ASG rules retain the group operand rather than rewriting to addresses."),
    Q("asg-cli", "Which Azure CLI command creates an application security group for a workload role?", "Run `az network asg create --resource-group <rg> --name <asg> --location <region>`.", "The ASG create operation defines the regional grouping resource later assigned to NIC IP configurations.", "Run `az network nsg rule create --asg-name <asg>` without creating the group.|A rule cannot materialize a missing ASG resource.", "Run `az network service-tag create --name <asg>`.|Customer-defined ASGs are not created through service-tag commands.", "Run `az vm identity assign --network-role <asg>`.|Managed identity assignment does not establish ASG membership."),
    Q("asg-cli", "An NSG rule references Web-ASG, and troubleshooting needs the ASG resource definition before checking NIC membership. Which Azure CLI operation should be used?", "Run `az network asg show --resource-group <rg> --name <asg>` to retrieve the referenced application security group.", "The ASG show operation confirms the exact regional resource before membership and rule evidence are compared.", "Run `az network watcher test-ip-flow` first without resolving the ASG reference.|IP flow diagnosis cannot explain a missing or wrong ASG resource definition.", "Run `az network nsg rule create` to recreate the rule immediately.|Changing policy before validating the referenced group can mask the configuration error.", "Run `az network vnet subnet update --service-endpoints` on the workload subnet.|Service-endpoint settings do not retrieve or validate an ASG resource."),
    Q("nsg-effective", "What is the boundary between configured NSG rules and effective security rules?", "Configured rules are the entries in one NSG; effective rules show the combined result applied to a NIC from its subnet and NIC associations.", "The effective view is the relevant artifact when multiple NSGs participate in the data path.", "Configured rules include guest firewall rules while effective rules exclude Azure rules.|Azure NSG views do not import host firewall configuration.", "Effective rules are a proposed template that has not been deployed.|They represent policy currently applied to the selected interface.", "The two terms differ only by portal wording and always contain identical rows.|Association composition makes them materially different."),
    Q("vnet-flow-logs", "What is the boundary between VNet flow logs and packet capture?", "VNet flow logs record flow metadata and decisions at scale; packet capture records packet-level data for a scoped diagnostic interval.", "The metadata stream supports traffic analysis, while packet capture provides deeper payload and protocol evidence with higher collection cost.", "Flow logs contain every packet payload and packet capture contains only counters.|Those data-depth descriptions are reversed.", "Packet capture replaces retention and analytics planning for long-term flows.|Short scoped captures are not a scalable historical flow-log system.", "Both change NSG rules while collecting traffic.|They observe traffic and do not need to rewrite access policy."),
  ],
  "AZ104-N08-B03": [
    Q("bastion-overview", "Administrators need RDP to private Windows VMs without public IPs or a self-managed jump host. Which service fits?", "Deploy Azure Bastion in the VNet and connect to the VM private IPs.", "Bastion supplies managed RDP and SSH connectivity while targets remain privately addressed.", "Assign a public IP to every VM and restrict passwords.|That recreates direct Internet exposure the requirement excludes.", "Use Traffic Manager to proxy RDP.|Traffic Manager is DNS-based endpoint selection and does not proxy RDP sessions.", "Create a Storage service endpoint on the VM subnet.|A service endpoint does not provide interactive administration."),
    Q("bastion-overview", "Where must Azure Bastion be deployed in a classic dedicated deployment?", "Use a dedicated subnet named AzureBastionSubnet with a supported prefix size in the VNet.", "The managed service discovers the reserved subnet name and needs sufficient addresses for its instances and scaling.", "Use GatewaySubnet because both services provide connectivity.|VPN Gateway and Bastion require different reserved subnets.", "Install Bastion into each workload VM NIC.|Bastion is a managed VNet resource rather than a per-VM NIC extension.", "Use any application subnet if it has a /32 free.|The dedicated subnet name and supported capacity are required."),
    Q("bastion-settings", "A team must use native SSH clients through Bastion. What design decision is required?", "Select a Bastion SKU and configuration that supports native client connectivity, then authorize the user and target path.", "Native client capability is feature- and SKU-dependent rather than inherent in every deployment.", "Enable an NSG service tag named NativeSSH on the VM.|There is no such service tag that enables the Bastion feature.", "Add a public SSH load-balancing rule.|That bypasses Bastion and exposes a direct public data path.", "Change the VM disk to Premium SSD.|Storage tier does not enable a Bastion client mode."),
    Q("bastion-settings", "A Bastion deployment needs more concurrent sessions. Which capacity control should be reviewed?", "Review the supported SKU and host scaling configuration for the required session load.", "Bastion capacity is tied to supported scale-unit or host-scaling settings, not the number of target public IPs.", "Add more DNS A records for the Bastion name.|DNS records do not add managed Bastion instances.", "Attach multiple NSGs to AzureBastionSubnet.|A subnet supports one NSG association, and rule count is not session capacity.", "Increase every VM's CPU solely to scale Bastion.|Target compute does not scale the managed access service."),
    Q("bastion-overview", "Security policy prohibits inbound Internet ports on target VMs. How should Bastion change the VM design?", "Remove target VM public IPs and admit only the documented Bastion-to-target management flow from the Bastion subnet.", "The managed connection reaches private addresses, so direct Internet RDP or SSH is unnecessary.", "Keep public IPs as a silent fallback with 0.0.0.0/0 SSH.|That violates the explicit exposure policy.", "Deny all traffic from AzureBastionSubnet to target ports.|That blocks the managed administration flow.", "Publish each VM through Application Gateway WAF.|Application Gateway handles HTTP(S), not general RDP or SSH administration."),
    Q("bastion-overview", "What sequence deploys Bastion for private VM administration?", "Plan the supported SKU and features, create the correctly named dedicated subnet with adequate size, deploy Bastion, configure NSGs, authorize operators, connect to a private target, and remove direct public management paths.", "The order establishes capacity and policy before proving the managed path and retiring exposure.", "Delete all VM public access before the Bastion subnet exists.|That can strand administrators before the replacement path is validated.", "Create Bastion inside GatewaySubnet and share the VPN gateway IP.|The services have separate deployment and subnet requirements.", "Enable RDP on every public IP after Bastion succeeds.|That reverses the exposure-reduction objective."),
    Q("bastion-native", "What sequence validates a native-client Bastion connection?", "Confirm the Bastion SKU and native-client feature, verify RBAC, select the target VM and protocol, test target NSG admission from the Bastion path, then invoke the supported client command.", "Feature, authorization, network, and client layers must all align for a private session.", "Run SSH to the VM public IP first and add Bastion later.|That tests a different direct exposure path.", "Disable the target guest firewall before checking Bastion settings.|Broadly removing host protection is not the first diagnostic step.", "Create a private endpoint for the VM's SSH daemon.|Private endpoints do not publish arbitrary VM management listeners this way."),
    Q("bastion-overview", "The Bastion portal connects to one VM but not another in the same VNet. What should be compared first?", "Compare target protocol listener state, guest firewall, subnet and NIC NSGs, routes, credentials, and the private IP selected for each VM.", "A healthy Bastion resource narrows the failure to the distinct target path or authentication state.", "Redeploy Bastion immediately because one failed target proves service outage.|The successful target disproves a general Bastion failure.", "Add public IPs to both VMs for synchronization.|Public addressing is unrelated to consistency of the managed private path.", "Change the VNet DNS zone registrar.|Direct private-IP management does not depend on public delegation."),
    Q("bastion-settings", "A native-client command is rejected although portal sessions work. What effective state is most likely?", "The Bastion SKU or native-client/tunneling feature, local client version, or operator authorization does not meet native-client requirements.", "Portal connectivity proves the core path but not entitlement and configuration for a distinct client mode.", "The VM must have a Standard public IP for native mode.|Bastion native connectivity is designed to reach private targets.", "The NSG must allow the Internet service tag directly to SSH.|That would bypass the managed connection path.", "The AzureBastionSubnet name should be changed for native sessions.|The reserved name remains required and is not client-mode-specific."),
    Q("bastion-cli", "Which Azure CLI command group provides supported Bastion SSH or tunnel operations?", "Use the `az network bastion` command group and the operation matching SSH, RDP, or tunnel requirements.", "The Bastion CLI group accepts the managed resource and target identifiers for supported native operations.", "Use `az network lb inbound-nat-rule` for every Bastion session.|NAT rules expose load-balanced ports and are not Bastion client operations.", "Use `az vm open-port` before `ssh`.|Opening a public VM port bypasses the approved Bastion path.", "Use `az network private-link-service create --protocol ssh`.|Private Link service does not replace the Bastion administration client contract."),
    Q("bastion-overview", "What is the boundary between Azure Bastion and a VPN Gateway?", "Bastion provides managed per-session RDP/SSH access to private VMs; VPN Gateway connects networks or clients at the IP routing layer.", "A VPN can expose broader network reachability, while Bastion brokers supported management protocols without extending the whole VNet to the operator.", "Bastion advertises on-premises BGP routes while VPN Gateway renders VM consoles.|Those roles are reversed and Bastion does not advertise network routes.", "Both require the same GatewaySubnet resource.|Bastion uses its own dedicated subnet in the classic model.", "VPN Gateway is only an HTTP reverse proxy.|It supplies encrypted network connectivity rather than web request routing."),
    Q("bastion-overview", "What is the boundary between Azure Bastion and a public IP on a VM?", "Bastion centralizes a managed private-IP administration path; a VM public IP exposes that NIC to direct public routing subject to NSG and host controls.", "The Bastion design reduces per-VM public attack surface and separates entry from workload addressing.", "Bastion automatically creates a public IP on every target.|Targets can remain without public IP resources.", "A VM public IP provides the same managed session auditing and broker controls.|Direct addressing lacks the managed Bastion mediation model.", "Neither option requires network security policy.|Both designs still require scoped authorization and filtering."),
  ],
  "AZ104-N08-B04": [
    Q("service-endpoints", "VMs in one subnet must access Azure Storage over the Azure backbone while the account denies other VNet origins. Which design fits?", "Enable the Microsoft.Storage service endpoint on the client subnet and add that subnet to the storage account network rules.", "The endpoint supplies subnet identity to Storage, and the resource firewall admits that selected origin.", "Create only the service endpoint and leave Storage network access unrestricted.|The route optimization alone does not deny other origins.", "Add only a storage firewall subnet rule without enabling its required endpoint path.|The subnet identity must be enabled for the virtual-network rule design.", "Create a public IP on every VM and allow those addresses.|That couples access to public egress rather than the approved subnet identity."),
    Q("service-endpoints", "A PaaS resource must retain its public endpoint but accept traffic only from selected Azure subnets. Which feature should be considered?", "Use a supported virtual network service endpoint plus resource-side virtual network firewall rules.", "Service endpoints preserve the service's public endpoint while allowing subnet-scoped admission over the Azure backbone.", "Use Private Link and require the service to keep only a public IP.|A private endpoint introduces a private IP and is a different access model.", "Use an ASG as the PaaS firewall identity.|ASGs group workload NICs for NSGs and are not generic PaaS firewall operands.", "Use Traffic Manager to authorize subnets.|DNS endpoint selection does not enforce source-network admission."),
    Q("service-endpoint-policies", "A subnet may reach only two approved Storage accounts through its Microsoft.Storage endpoint. What additional control fits?", "Associate a supported service endpoint policy that allows the approved Storage resource destinations.", "The policy narrows service-endpoint egress from the subnet to listed supported resources.", "Add an NSG destination ASG containing the Storage accounts.|PaaS storage resources are not members of customer ASGs.", "Create a private DNS zone with only two account records.|DNS omission is not a secure destination authorization control.", "Use an inbound Load Balancer rule for the two accounts.|The accounts are managed service destinations, not backends of the customer's Load Balancer."),
    Q("storage-network", "A storage firewall should admit an application subnet but not an administration subnet in the same VNet. What should be configured?", "Enable the Storage service endpoint on the application subnet and add only that subnet as a storage virtual network rule.", "Storage network rules evaluate selected subnet identities, allowing separation within one VNet.", "Allow the entire VNet address space in an NSG attached to Storage.|A managed Storage account cannot have the customer's subnet NSG attached to it.", "Enable the endpoint only on the administration subnet.|That assigns identity to the wrong origin.", "Add the VNet resource ID without a subnet selection.|The supported firewall rule is scoped to a subnet resource."),
    Q("service-endpoints", "What sequence enables service-endpoint access to an existing PaaS resource without an outage?", "Confirm service and region support, enable the endpoint on the intended subnet, add the subnet to the resource firewall while retaining a tested administration path, validate clients, then remove obsolete broad access.", "The staged sequence establishes subnet identity before tightening the resource firewall.", "Set the resource firewall to selected networks first, then discover which subnet needs an endpoint.|That can block all clients before their identity path exists.", "Disable public network access before testing the service endpoint.|Service endpoints use the service's public endpoint, so disabling it conflicts with this model.", "Move clients to a new VNet after every firewall change.|A supported endpoint does not require wholesale workload relocation."),
    Q("service-endpoint-policies", "What sequence deploys a service endpoint policy on a production subnet?", "Inventory required service destinations, confirm policy support, create allow definitions, associate the policy to a test subnet, verify approved and denied destinations, then roll out to the production subnet.", "Destination restriction can break hidden dependencies, so inventory and staged negative testing are essential.", "Associate an empty policy first and add resources after outages.|An unvalidated deny posture can interrupt every service destination.", "Attach the policy to each Storage account instead of the subnet.|Service endpoint policies are subnet-side egress controls.", "Replace the service endpoint with an Internet route before applying policy.|That bypasses rather than validates the controlled endpoint path."),
    Q("storage-network", "What sequence removes a storage account's broad public-network rule after subnet access is ready?", "Validate the client subnet endpoint, add its storage network rule, test data-plane access from allowed and disallowed origins, narrow public network access, and monitor authorization failures.", "Positive and negative tests prove the intended firewall boundary before broad access is retired.", "Remove public access first and add the subnet rule during the outage.|The ordering needlessly interrupts all access.", "Add a DNS CNAME from the subnet to Storage.|DNS aliases do not establish firewall identity.", "Permit every VNet in the subscription as a transition.|That preserves a broad trust boundary unrelated to the selected application subnet."),
    Q("service-endpoints", "What sequence moves service-endpoint access from Subnet-A to Subnet-B?", "Enable the endpoint on Subnet-B, add its resource firewall rule, validate B, drain or move clients, remove Subnet-A's resource rule, then remove its endpoint if unused.", "Overlapping admission during the controlled move avoids a service interruption and ends with the obsolete identity removed.", "Delete Subnet-A first because endpoint configuration migrates automatically.|Deleting a subnet does not transfer its identity or clients.", "Change the VNet name and keep both subnet rules indefinitely.|Renaming does not move access, and stale rules preserve unnecessary trust.", "Disable the service's public endpoint before enabling Subnet-B.|Service endpoints depend on that endpoint remaining available."),
    Q("service-endpoint-policies", "What sequence investigates a service endpoint policy before adding a new Storage destination?", "Confirm the destination resource ID and policy definition scope, add the narrow resource entry, validate routing and resource firewall access, test from the subnet, and review policy logs or failures.", "Both subnet egress policy and service-side admission must permit the final data path.", "Add the account hostname as an NSG source prefix.|NSG address fields are not the service endpoint policy resource list.", "Turn the policy into an inbound rule on Storage.|The customer cannot attach an NSG to the managed account endpoint.", "Change all Storage accounts to the same DNS name.|DNS naming cannot grant policy authorization."),
    Q("service-endpoints", "A service endpoint is enabled, but clients still receive a resource-firewall denial. What effective state is missing?", "The PaaS resource firewall does not admit the client subnet, or the request originates from a different subnet than expected.", "Endpoint enablement supplies a network identity but does not automatically modify the service's access policy.", "The subnet must have a public Load Balancer frontend.|Load balancing is unrelated to service endpoint identity.", "The endpoint needs a private DNS zone group.|DNS zone groups are associated with private endpoints, not service endpoints.", "The VM requires membership in AzureLoadBalancer service tag.|Service tags are rule operands, not assignable VM memberships."),
    Q("storage-network", "Storage access works from Subnet-A but not Subnet-B, though both use the same VNet DNS. What should be compared?", "Compare Microsoft.Storage endpoint state and storage virtual-network rules for each subnet, plus the actual client source subnet.", "Service endpoint admission is subnet-specific and independent of sharing DNS settings.", "Compare only the VNet name lengths.|Resource names do not decide firewall identity.", "Add a Storage account public IP to Subnet-B.|Managed Storage does not attach its public endpoint to a customer subnet.", "Enable VM autoregistration in Private DNS.|Service endpoint firewall matching does not depend on VM DNS autoregistration."),
    Q("service-endpoint-policies", "After a restrictive endpoint policy is associated, approved Storage works but Azure Resource Manager calls fail. What does this show?", "The policy affects only its supported service endpoint traffic; the Resource Manager failure has another route, DNS, NSG, or authorization cause unless a relevant supported policy entry applies.", "A Storage endpoint policy is not a universal egress firewall for every Azure service.", "Every Azure control-plane call is automatically tunneled through Microsoft.Storage.|Resource Manager and Storage are different service destinations.", "The Storage account must host ARM metadata for the subnet.|Control-plane APIs are not supplied from the customer's storage account.", "Removing all NSGs is required for endpoint policies.|Endpoint policy and NSG controls can coexist and should be diagnosed independently."),
    Q("subnet-cli", "Which Azure CLI command group updates a subnet's service endpoint list?", "Use `az network vnet subnet update` with the resource group, VNet, subnet, and reviewed service-endpoints values.", "The subnet update operation owns the endpoint association while preserving other subnet configuration when arguments are used correctly.", "Use `az storage account update --create-subnet`.|Storage account commands do not create a VNet subnet endpoint association.", "Use `az network private-endpoint create --service-endpoint`.|Private endpoints and service endpoints are separate resources and command contracts.", "Use `az network nsg rule update --destination-service Storage`.|An NSG service tag rule does not enable a virtual network service endpoint."),
    Q("service-endpoints", "What is the boundary between a service endpoint and an NSG service tag?", "A service endpoint gives a subnet an identity and optimized path to a supported PaaS service; a service tag supplies Microsoft-managed prefixes for NSG or route rule operands.", "One participates in service-side virtual-network admission, while the other simplifies address matching in network policy.", "A service tag creates the PaaS firewall rule automatically.|Tags do not configure the destination service's resource firewall.", "A service endpoint is merely a downloadable IP list.|It is a subnet feature with service integration, not a static prefix document.", "Both assign private IPs from the client subnet to the PaaS service.|Service endpoints retain the service public endpoint, and service tags allocate no endpoint IP."),
  ],
  "AZ104-N08-B05": [
    Q("private-endpoint", "A storage account must be reachable through a private IP in an application VNet. Which resource should be created?", "Create a private endpoint in the VNet for the required Storage subresource.", "The endpoint provisions a consumer-side NIC and private IP connected to the selected service subresource.", "Enable only a Storage service endpoint on the subnet.|That retains the service public endpoint instead of assigning a private endpoint IP.", "Create an ASG containing the storage account.|Managed Storage resources cannot join a customer ASG.", "Add a public Load Balancer frontend for the account.|A customer Load Balancer cannot front the managed Storage endpoint this way."),
    Q("private-endpoint", "An account exposes blob and file subresources, but only blob access should be private. What must the private endpoint target?", "Select the blob group ID or subresource when creating the private endpoint.", "Private endpoint connections bind to a specific supported target subresource rather than every service capability automatically.", "Select the VNet resource ID as the group ID.|The target group identifies the service subresource, not the consumer network.", "Create one endpoint with group ID `all` for every service.|Supported group IDs are service-defined and do not use a universal wildcard.", "Select the private DNS zone as the target resource.|DNS integrates name resolution but is not the Private Link service resource."),
    Q("private-endpoint", "A consumer in another subscription needs private access to a provider resource and manual approval is required. Which state transition matters?", "Create the private endpoint connection and have an authorized owner of the target resource approve the pending request.", "Cross-ownership connections can remain pending until the provider explicitly approves them.", "Grant the consumer Owner on its own VNet only and expect auto-approval.|Consumer network ownership does not authorize the provider resource connection.", "Publish the service through a public DNS CNAME.|DNS publication does not approve a Private Link connection.", "Enable gateway transit between subscriptions.|Peering gateway flags are unrelated to endpoint approval."),
    Q("private-endpoint", "A private endpoint connection remains Pending after the consumer creates it. What should be checked first?", "Confirm the provider-side connection approval or auto-approval policy, then verify the connection state before troubleshooting DNS and client routing.", "A pending connection has not completed the provider-consumer handshake, so a correct DNS record alone cannot make the private path usable.", "Change the client DNS zone immediately while leaving the connection Pending.|DNS cannot activate an unapproved Private Link connection.", "Enable IP forwarding on the endpoint NIC.|The platform-managed endpoint NIC does not require client-controlled forwarding for approval.", "Create a second endpoint for the same subresource and ignore the first connection.|Duplicate endpoints do not resolve an outstanding approval state."),
    Q("private-endpoint", "What sequence creates a private endpoint for an Azure PaaS resource?", "Identify the target resource and subresource, choose subnet and policy state, create the endpoint connection, obtain approval, configure recommended private DNS, validate private resolution, and test access.", "Resource binding, connection state, DNS, and network policy are independent parts of a working endpoint.", "Create DNS first with a guessed private IP and skip the endpoint.|A DNS record alone does not provision a Private Link connection.", "Disable all public access before the connection is approved.|That can strand workloads before the private path exists.", "Add the PaaS resource to the subnet NSG backend pool.|NSGs do not create endpoint NICs or Private Link bindings."),
    Q("private-endpoint-dns", "What sequence integrates private endpoint DNS for clients in multiple VNets?", "Use the service-recommended private zone, associate the endpoint record through a zone group where supported, link each Azure client VNet or configure a resolver forwarding path, and verify the original service FQDN from every client.", "Clients must follow the service alias chain into one reachable private authority that returns the endpoint IP.", "Create separate public zones for every VNet.|Public authority would expose or conflict with private answers rather than scope them to clients.", "Point all clients directly at the endpoint IP and ignore the service hostname.|Many services require the documented hostname for TLS and routing semantics.", "Enable VNet peering and assume private zones become transitive.|Peering alone does not make Private DNS links transitive."),
    Q("private-endpoint-policies", "What sequence enables supported NSG processing for a private endpoint subnet?", "Review current feature support, set the subnet's private endpoint network-policy state as required, add narrow NSG rules, inspect effective behavior, and test endpoint traffic without breaking DNS.", "Subnet policy state determines whether supported controls participate; policy should be proven with the real endpoint tuple.", "Attach an NSG directly to the private endpoint resource.|NSGs associate with subnets or NICs under supported ownership, not as an arbitrary endpoint child setting.", "Turn the endpoint into a service endpoint before adding the NSG.|The two access models are not interchangeable conversions.", "Enable public network access so NSGs can see the traffic.|Private endpoint policy does not require reopening the service public endpoint."),
    Q("private-endpoint", "What sequence retires public access after private endpoint migration?", "Build and approve the endpoint, configure DNS, validate application traffic and dependencies through the private IP, test from every client network, disable or restrict public access, and monitor failures.", "The private path must be complete before closing the old service endpoint to avoid a name-resolution or approval outage.", "Disable public access first to force clients onto the endpoint.|Clients with unresolved DNS or pending approval will fail rather than migrate automatically.", "Keep public DNS pinned to the public address after disabling it.|That directs clients to the closed path.", "Delete the service resource and recreate it through Private Link.|Private Link connects to the existing provider resource and does not require deletion."),
    Q("private-endpoint", "The endpoint connection remains Pending and data traffic fails. What is the effective cause?", "An authorized target-resource owner has not approved the private endpoint connection, so the service binding is not active.", "A private IP resource can exist before the provider accepts the requested connection.", "The endpoint needs a public IP while pending.|Public addressing does not replace provider approval.", "The consumer must raise the NSG priority to 65000.|Security priority cannot transition Private Link connection state.", "The DNS TTL must be zero to auto-approve.|DNS caching is unrelated to connection authorization."),
    Q("private-endpoint-dns", "The service FQDN resolves privately in VNet-A but publicly in peered VNet-B. Why?", "VNet-B is not linked to the private DNS zone and has no forwarding path to a resolver that can answer it.", "Private DNS visibility does not transitively follow VNet peering.", "Private endpoints can serve only one VNet by protocol.|Any network with routing, DNS, and policy can potentially consume the private endpoint.", "The endpoint group ID changes per querying VNet.|The target subresource binding is independent of client VNet identity.", "Peering must use gateway transit for DNS links.|Gateway transit does not substitute for zone linking or forwarding."),
    Q("private-endpoint-dns", "Clients resolve a private endpoint IP but TLS reports a hostname mismatch after using a custom alias. What should be corrected?", "Use the service's documented FQDN and private DNS integration so TLS and service routing retain the expected host name.", "Private Link typically preserves the original service name while DNS maps it to the endpoint IP.", "Issue a certificate for the endpoint NIC's private IP from Azure automatically.|The platform does not make the private IP the service certificate identity.", "Turn off TLS validation because the network is private.|Private routing does not make identity verification unnecessary.", "Create an NSG rule that rewrites the Host header.|NSGs filter flows and do not modify TLS host names."),
    Q("private-endpoint-policies", "A UDR intended to inspect private endpoint traffic is ignored. What should be examined?", "Examine the subnet private endpoint network-policy setting, route prefix specificity and current supported behavior for the endpoint path.", "Private endpoint policy state and route selection determine whether the UDR can participate.", "Add the route to the provider's public IP only.|The resolved destination is the endpoint private IP, so a public prefix does not match it.", "Enable Floating IP on the endpoint.|Floating IP is a Load Balancer rule behavior, not a Private Link routing switch.", "Create a Traffic Manager profile for the route table.|DNS endpoint selection cannot associate a subnet UDR."),
    Q("private-endpoint", "One private endpoint for blob works, but Azure Files access remains public. What explains the split?", "The endpoint targets only the blob subresource; Files requires its own supported file subresource endpoint and DNS configuration.", "Private Link connections and recommended private zones are service-subresource-specific.", "Files cannot use Private Link under any Storage account.|Azure Files supports its own private endpoint group where available.", "The blob endpoint must be assigned two NIC IPs to cover Files.|Additional IPs do not change the selected group ID.", "Changing the endpoint name to `storage-all` expands the binding.|Resource display names do not alter provider subresource scope."),
    Q("private-endpoint", "A private endpoint is Approved and DNS is correct, but an NSG deny applies after network policies were enabled. What is the correct interpretation?", "The endpoint path is now subject to the supported subnet NSG policy, and the exact client-to-endpoint tuple must be allowed deliberately.", "Approval and DNS do not override an explicitly participating security filter.", "Approved Private Link connections bypass every customer NSG.|Policy support exists specifically to apply documented NSG processing.", "The endpoint should be moved to AzureBastionSubnet.|Bastion's reserved subnet is not a generic endpoint location.", "The client needs a service endpoint as an NSG exception.|A second access model does not override a deny on the private path."),
    Q("private-endpoint-cli", "Which Azure CLI command group creates a private endpoint with target resource ID, group ID, subnet, and connection name?", "Use `az network private-endpoint create` with the reviewed resource and subresource arguments.", "The private-endpoint create contract binds the consumer NIC to the provider's specific group.", "Use `az network private-link-service create` in the consumer subscription.|That publishes a provider service rather than creating a consumer endpoint.", "Use `az network vnet subnet update --private-ip <service-id>`.|A subnet update alone cannot create the endpoint connection resource.", "Use `az network lb rule create --group-id <group>`.|Load Balancer rules do not bind PaaS Private Link subresources."),
    Q("private-dns-zone-group-cli", "Which Azure CLI command group attaches private DNS zone configuration to an existing private endpoint?", "Use `az network private-endpoint dns-zone-group create` with the endpoint and private DNS zone resource.", "The DNS zone group is the endpoint-scoped integration object for supported record management.", "Use `az network dns zone create --endpoint <id>`.|The public DNS zone command does not create a private endpoint zone group.", "Use `az network private-dns link vnet create --endpoint <id>` only.|A VNet link grants zone visibility but does not attach the endpoint record configuration.", "Use `az storage account network-rule add --private-dns`.|Storage firewall rules do not create endpoint DNS zone groups."),
    Q("private-link-service", "What is the boundary between a private endpoint and a Private Link service?", "A private endpoint is the consumer-side private NIC; a Private Link service is a provider publication behind a supported Standard Load Balancer.", "Consumers instantiate endpoints, while service owners expose and govern the provider connection target.", "Both are consumer NICs with identical ownership.|The service is provider-side and has different lifecycle and configuration.", "A Private Link service is required for every Microsoft PaaS endpoint.|Microsoft services publish their own Private Link targets without a customer-created service.", "A private endpoint distributes traffic across provider VMs by itself.|Provider load distribution is outside the consumer endpoint resource."),
    Q("private-endpoint-dns", "What is the boundary between private endpoint connectivity and private DNS?", "The endpoint supplies a private network path and IP; private DNS makes the service hostname resolve to that IP for intended clients.", "Either layer can exist while the other is misconfigured, so both must be validated independently.", "DNS approval activates the endpoint connection.|Provider authorization is a Private Link connection state, not a DNS operation.", "The endpoint automatically links every VNet to its zone.|Client-zone visibility must be configured through links or resolvers.", "Private DNS encrypts the application payload through Private Link.|DNS resolves names; transport encryption remains the application's protocol responsibility."),
  ],
  "AZ104-N08-B06": [
    Q("service-endpoints", "A platform team compares three access models for a supported PaaS service. The service must keep its normal public endpoint and DNS name, identify calls by Azure client subnet, and require no private IP in the consumer VNet. Which model matches all three constraints?", "Use a virtual network service endpoint on each approved client subnet with matching service-side virtual network firewall rules.", "Service endpoints preserve the public service endpoint while extending subnet identity to the supported service; neither a consumer private NIC nor App Service worker egress is required.", "Use a private endpoint because it preserves a public-only address model.|A private endpoint creates a consumer-side private NIC and changes the intended DNS path to a private address.", "Use App Service VNet integration on the PaaS service to authenticate every client subnet.|VNet integration is app-originated outbound connectivity, not a general service-side subnet admission model.", "Use Azure Bastion because its dedicated subnet identifies every application request.|Bastion brokers administrative RDP and SSH sessions rather than PaaS data-plane calls."),
    Q("private-endpoint", "A managed database provider requires each consumer connection to be approved, and consumers must resolve the unchanged database FQDN to a private address in their own VNet. Which model provides both properties?", "Create an approved private endpoint for the database subresource and integrate the recommended private DNS zone with each consumer network.", "A Private Link endpoint has an explicit provider connection state and a consumer-side private NIC, while DNS preserves the service hostname over the private path.", "Use a service endpoint because it creates an approvable consumer NIC.|Service endpoints retain the service public endpoint and do not create per-consumer Private Link approval objects.", "Use App Service VNet integration because it publishes an inbound database address.|VNet integration changes outbound reachability for an app and does not publish this provider endpoint.", "Use an NSG service tag because it allocates the database FQDN a VNet address.|Service tags are managed prefix operands and create neither DNS records nor private endpoint connections."),
    Q("app-service-vnet-integration", "An App Service app needs outbound access to a VM through an integration subnet. Which feature serves the app's outbound path?", "Use App Service regional VNet integration with a supported dedicated integration subnet.", "VNet integration enables app-originated connectivity into the VNet; it is not an inbound private endpoint for the app.", "Use a service endpoint on the VM NIC.|Service endpoints connect subnets to supported Azure PaaS services, not arbitrary VM listeners.", "Create a private endpoint on the VM for every port.|Private endpoints do not publish arbitrary VM services directly without a supported provider model.", "Use Traffic Manager to inject a route into the app.|DNS distribution does not integrate App Service workers with a VNet."),
    Q("private-endpoint", "Clients in on-premises networks must privately consume Storage through Azure. Which design is appropriate when a private IP and hybrid DNS are required?", "Use a Storage private endpoint reachable through VPN or ExpressRoute and forward the recommended private DNS zone through the hybrid resolver path.", "The endpoint supplies the private IP, while hybrid routing and DNS make it usable from on-premises.", "Use a service endpoint directly on the on-premises LAN.|Service endpoint identity belongs to Azure VNet subnets, not external LAN segments.", "Use App Service VNet integration on each on-premises client.|The clients are not App Service workers and cannot use that feature.", "Publish the endpoint private IP in public DNS.|That leaks private addressing and does not create hybrid route reachability."),
    Q("service-endpoint-policies", "A subnet needs to restrict outbound Storage destinations but does not require inbound private IPs. Which control is closest?", "Use a Storage service endpoint with a supported service endpoint policy listing approved resources.", "The endpoint policy constrains supported PaaS destinations from the subnet without publishing a consumer endpoint.", "Create a Private Link service behind every Storage account.|Managed Storage is already a provider service, and a customer Private Link service is not the destination allow list.", "Use App Service VNet integration to filter all subnet VMs.|Integration does not enforce destination authorization for unrelated subnet workloads.", "Use a Bastion host as the subnet's default next hop.|Bastion is not an egress firewall or router."),
    Q("private-endpoint", "What sequence migrates a workload from service endpoint access to private endpoint access?", "Create and approve the private endpoint, configure and validate private DNS, test data traffic from every client network, update firewall or public-access settings, then remove obsolete service endpoint rules if no consumers remain.", "The migration changes both address resolution and resource admission, so the private path must be proven before old access is retired.", "Remove the service endpoint first and wait for DNS to create a private endpoint.|DNS cannot provision the missing network connection.", "Keep the public service address pinned in hosts files during validation.|That bypasses the private endpoint and produces a false-positive test.", "Convert the service endpoint object in place to Private Link.|Azure exposes separate configurations rather than a type conversion."),
    Q("service-endpoints", "What sequence selects among service endpoint, private endpoint, and App Service VNet integration?", "Identify traffic direction, required endpoint address, service support, public-access posture, client location, and DNS needs; then choose the feature and test its distinct network path.", "The three models differ by direction, address ownership, service scope, and name-resolution requirements.", "Choose the cheapest feature before identifying inbound or outbound direction.|A direction mismatch makes the design nonfunctional regardless of price.", "Enable all three on every subnet as a standard configuration.|Unnecessary overlapping paths complicate policy and do not create universal compatibility.", "Use the same DNS plan because all models allocate private endpoint IPs.|Service endpoints and outbound VNet integration do not inherently create the same private service IP."),
    Q("private-endpoint-dns", "A migration created a private endpoint but clients still traverse the service endpoint path. What is the likely effective state?", "The service hostname still resolves to its public address for those clients, so their requests never target the private endpoint IP.", "DNS determines which path the application selects when both models coexist.", "The service endpoint has a lower NSG priority than Private Link.|NSG priority does not select between DNS-resolved endpoint addresses.", "Private endpoints cannot coexist with service endpoint-enabled subnets.|The features can coexist; client resolution and service policy decide the used path.", "The endpoint must inherit the client's ASG.|ASG membership is not the route-selection mechanism for a managed endpoint."),
    Q("app-service-vnet-integration", "An app has VNet integration but inbound callers cannot reach it privately. What misconception caused the failure?", "VNet integration provides app outbound access into a VNet; inbound private access requires a supported private endpoint or another ingress service.", "Traffic direction is the defining boundary between these features.", "The integration subnet needs an inbound Load Balancer rule on the app workers.|Customers do not front the managed workers that way.", "A service endpoint on caller subnets automatically creates an app private IP.|Service endpoints preserve a public PaaS endpoint and require supported service firewall integration.", "The callers need Azure Bastion permissions.|Interactive VM administration does not provide application ingress."),
    Q("private-endpoint-cli", "Which Azure CLI resource creation best matches a requirement for a consumer-side private NIC to a supported PaaS subresource?", "Use `az network private-endpoint create` with the target resource ID and exact group ID.", "That operation materializes the consumer endpoint and requests the provider connection.", "Use `az network vnet subnet update --service-endpoints` only.|That configures subnet identity to a public PaaS endpoint instead.", "Use `az webapp vnet-integration add` on the PaaS provider.|VNet integration is specific to app outbound connectivity, not generic provider endpoint creation.", "Use `az network nsg rule create --private-link true`.|An NSG rule does not provision a Private Link NIC."),
    Q("private-link-service", "A company will publish an internal API through Azure Private Link service. Which provider-side prerequisites must be configured before consumers can connect?", "Place the API behind a supported Standard Load Balancer, create the Private Link service with its NAT IP configuration, define visibility or auto-approval, and then accept consumer endpoint requests.", "The provider publication combines load-balancer reachability, NAT, and connection-governance settings before any consumer endpoint can use it.", "Create only a private DNS zone in the provider subscription.|DNS does not publish the service or create the provider-side Private Link target.", "Create a consumer private endpoint in the provider VNet and skip the load balancer.|A provider service requires the documented Standard Load Balancer-backed publication model.", "Enable App Service VNet integration on the API callers.|That controls app outbound access and does not publish an internal provider service."),
    Q("app-service-vnet-integration", "What is the boundary between App Service VNet integration and a private endpoint for an app?", "VNet integration enables outbound calls from the app into the VNet; a private endpoint enables inbound clients to reach the app on a private IP where supported.", "One changes app egress reachability, while the other supplies private ingress.", "Both are inbound paths that differ only by subnet name.|Their traffic directions are different.", "VNet integration disables every public endpoint automatically.|Public exposure remains a separate access configuration.", "A private endpoint lets the app route all outbound traffic through its NIC.|The private endpoint is for consuming the app, not its worker egress path."),
  ],
  "AZ104-N08-B07": [
    Q("lb-components", "Three VMs serve TCP 443 and need one regional public frontend with health-based distribution. Which Azure resource fits?", "Use a Standard public Load Balancer with a frontend IP, backend pool, health probe, and TCP load-balancing rule.", "The four components provide regional layer-four distribution and remove unhealthy backends from new-flow selection.", "Use Traffic Manager alone to proxy every TCP packet.|Traffic Manager returns DNS answers and does not proxy the transport flow.", "Use an internal Basic Load Balancer with no frontend IP.|An internal frontend does not satisfy public client ingress.", "Use an NSG service tag as the backend pool.|Service tags match prefixes in policy and do not contain load-balanced NICs."),
    Q("lb-components", "An internal application tier needs a single private frontend address for callers in the VNet. Which Load Balancer design applies?", "Create an internal Standard Load Balancer frontend in the application VNet and add the tier instances to its backend pool.", "An internal frontend receives a subnet private IP while the backend pool distributes admitted flows.", "Assign a public frontend and hide its DNS record.|The address remains publicly routed regardless of DNS publication.", "Create one private endpoint per backend VM.|Private endpoints are provider-consumer bindings, not a general VM load-balancing pool.", "Use Front Door with a private RFC1918 frontend.|Front Door exposes a global web edge rather than a VNet-local private frontend."),
    Q("lb-probes", "A web process accepts TCP connections before it has loaded required data. Which health probe best represents readiness?", "Use an HTTP or HTTPS probe path that returns success only after the application dependencies are ready.", "An application-aware endpoint prevents new traffic from reaching a process that is listening but not serviceable.", "Use a TCP probe on any open management port.|A management listener does not prove web application readiness.", "Probe the Load Balancer frontend from each backend.|Health probes target backend readiness rather than recursively probing the frontend.", "Disable probes and rely on VM power state.|A running VM can still have an unavailable application."),
    Q("lb-distribution", "An application stores session state locally and cannot yet externalize it. Which Load Balancer distribution option may reduce client movement?", "Use a supported source-IP affinity distribution mode after evaluating proxy and NAT effects.", "Source-IP-based hashing can preserve client affinity, though shared client addresses can skew distribution.", "Enable Floating IP because it stores application cookies.|Floating IP changes address mapping and does not manage web session cookies.", "Use the five-tuple default to guarantee one backend per user forever.|Client source ports can change, causing five-tuple hashes to select different instances.", "Create an outbound rule with zero SNAT ports.|Outbound SNAT does not provide inbound session affinity."),
    Q("lb-outbound", "Backend VMs need explicit Internet egress through the Load Balancer frontend. Which component controls that behavior?", "Configure a Standard Load Balancer outbound rule with reviewed frontend IP and SNAT port allocation.", "Outbound rules explicitly define backend-to-frontend SNAT behavior and capacity.", "Add an inbound NAT rule for destination port 443.|Inbound NAT maps management or service ingress and does not define general outbound SNAT.", "Change the health probe to HTTPS.|Probe protocol does not provision egress addresses or ports.", "Add the Internet service tag to the backend pool.|Service tags cannot serve as Load Balancer pool members."),
    Q("lb-components", "What sequence builds a Standard Load Balancer for a new web tier?", "Create the frontend IP configuration, define the backend pool and add eligible NIC configurations, create a matching health probe, create the load-balancing rule, configure NSGs, and test healthy distribution.", "Each rule references existing frontend, backend, and probe components, while NSGs must admit client and probe flows.", "Create the rule before the frontend and pool exist.|The rule cannot reference missing components.", "Add every VM to the pool before confirming its listener or probe behavior.|Unready members create avoidable unhealthy state during rollout.", "Delete the VM NSGs because Standard Load Balancer is secure by default.|Standard SKU is closed to inbound traffic unless NSGs admit the required flows."),
    Q("lb-components", "What sequence adds a new VM to a production backend pool?", "Prepare the listener and probe endpoint, apply required NSG rules, add the NIC IP configuration to the pool, wait for healthy probe state, then observe traffic and errors.", "The backend should be ready and admitted before it becomes eligible for new flows.", "Add the VM first and deploy the application after it receives traffic.|The probe can remain unhealthy or, worse, a shallow probe can admit an unready service.", "Assign the frontend public IP directly to the VM NIC.|A Load Balancer frontend is not moved onto individual backend NICs.", "Clone the Load Balancer for every added instance.|A backend pool is specifically designed to group multiple instances."),
    Q("lb-probes", "What sequence changes a probe from TCP to an HTTPS readiness endpoint?", "Deploy and validate the readiness path on every backend, update NSGs and certificates as needed, create or update the HTTPS probe, associate it with the rule, observe health, then retire the old probe.", "Backend readiness and network admission must exist before the rule depends on the new application-level probe.", "Delete the TCP probe first and later implement HTTPS.|The load-balancing rule loses valid health evaluation during the gap.", "Point the HTTPS probe at the public frontend hostname.|The platform probe evaluates backends through the configured probe contract.", "Use a path that returns 500 while dependencies are healthy.|A failing response intentionally marks healthy instances unavailable."),
    Q("lb-components", "What sequence performs a zero-downtime backend-pool rotation?", "Prepare new instances, admit probe traffic, add them to the pool, wait until healthy, drain or stop new flows to old instances using the supported method, remove old members, and monitor.", "Overlapping healthy capacity allows traffic to transition before obsolete backends leave eligibility.", "Remove old members before new probes pass.|That creates a capacity gap and possible outage.", "Reuse one NIC IP configuration simultaneously across unrelated VNets.|Backend membership and frontend reachability must follow supported network topology.", "Disable the Load Balancer while swapping members.|Turning off the ingress path defeats zero downtime."),
    Q("lb-outbound", "What sequence configures explicit outbound SNAT for an existing backend pool?", "Estimate concurrent flows and destinations, choose the frontend public IPs, configure an outbound rule and SNAT allocation, verify NSGs and routes, test observed egress IPs, and monitor port usage.", "SNAT capacity must be sized and verified against the actual concurrency and path.", "Set allocated outbound ports to the smallest value without measurement.|Undersizing creates preventable SNAT exhaustion.", "Create an inbound rule with source port * and call it outbound.|Ingress load-balancing rules do not define backend-initiated SNAT capacity.", "Add a private DNS zone for Internet destinations.|DNS authority does not allocate source NAT ports."),
    Q("lb-distribution", "What sequence changes distribution mode for an application requiring affinity?", "Confirm why external session state is unavailable, select the least restrictive supported source-IP affinity, change the rule in a controlled window, test through real proxies and clients, and monitor backend skew.", "Affinity interacts with shared source addresses and can create uneven load, so representative testing matters.", "Change to affinity because one backend has high CPU without analyzing clients.|Uneven load can worsen under source-IP hashing and may have another cause.", "Enable HA Ports as a synonym for session persistence.|HA Ports changes port matching, not distribution hash affinity.", "Move cookies into the NSG rule.|NSGs do not inspect or persist HTTP application cookies."),
    Q("lb-floating-ip", "What sequence enables Floating IP for a supported direct server return design?", "Validate the workload architecture, configure the frontend address on backend loopback interfaces as documented, enable Floating IP on the rule, preserve probe reachability, and test both request and direct response paths.", "Floating IP requires coordinated guest and Load Balancer address behavior, not only a portal checkbox.", "Enable Floating IP before configuring backend hosts and rely on DHCP.|Backends may not accept the frontend destination and the return path will fail.", "Add the frontend IP as a public DNS server on each VM.|DNS client settings do not implement direct server return.", "Convert the health probe into an outbound rule.|Probe and SNAT components have separate functions."),
    Q("lb-troubleshoot-probe", "All backends show Unhealthy after an NSG change. What should be checked first?", "Check that the NSG admits the configured probe protocol and port from the AzureLoadBalancer service tag and that the backend listener responds.", "A simultaneous health loss after policy change points to the platform probe path or listener contract.", "Broaden Internet inbound access to every port.|Probe admission can be scoped to the platform tag and exact listener.", "Delete the frontend public IP to force new probes.|Removing the frontend disrupts the service and does not repair backend admission.", "Enable source-IP affinity.|Distribution mode does not make an unreachable probe healthy."),
    Q("lb-probes", "One backend is Unhealthy while peers using the same NSG are healthy. What should be compared?", "Compare that instance's listener, readiness path response, guest firewall, local routing, certificate behavior, and backend-pool NIC membership.", "Shared Load Balancer and NSG health narrows the fault to instance-specific application or host state.", "Recreate the entire Load Balancer because probes are global.|Healthy peers demonstrate that the common probe configuration can work.", "Add a Traffic Manager profile for the unhealthy VM.|DNS distribution does not repair the regional backend listener.", "Move the healthy VMs into the failed VM's ASG.|ASG reassignment does not fix an instance-specific probe response."),
    Q("lb-outbound", "Backends intermittently fail new Internet connections under high concurrency, while inbound service remains healthy. What effective state should be investigated?", "Investigate outbound SNAT port exhaustion, allocation per backend, destination concurrency, and whether NAT Gateway is a better egress design.", "Inbound health and concurrency-correlated egress failures are consistent with depleted source translation ports.", "Increase the health-probe threshold to route outbound traffic.|Probes control inbound backend eligibility, not SNAT allocation.", "Enable Floating IP on every rule.|Direct server return does not expand outbound SNAT ports.", "Add more private DNS records for public destinations.|Name multiplicity does not create translation capacity."),
    Q("lb-components", "Clients reach the frontend but no backend receives traffic, and the rule references an empty pool. What is the root cause?", "The load-balancing rule has no eligible backend NIC configurations to select.", "A frontend and rule cannot deliver flows without actual members in the referenced backend pool.", "The Load Balancer requires an ASG instead of a pool.|ASGs can aid NSG policy but do not replace backend pool membership.", "The frontend IP must be copied to each VM primary NIC.|Backends should not take ownership of the Load Balancer frontend address in the ordinary design.", "The health probe will create pool members automatically.|Probes evaluate configured members and do not enroll NICs."),
    Q("lb-probe-cli", "Which Azure CLI command group creates or updates a Load Balancer health probe?", "Use `az network lb probe create` or `az network lb probe update` with the Load Balancer, protocol, port, and any required path and thresholds.", "The probe command group owns the backend health-evaluation contract.", "Use `az network watcher test-ip-flow --probe-name <name>`.|IP flow verify evaluates NSGs and does not create Load Balancer probes.", "Use `az network lb frontend-ip create --health-path <path>`.|Frontend IP configuration does not own probe settings.", "Use `az monitor metrics alert create --backend-pool`.|An alert observes metrics and does not configure traffic eligibility."),
    Q("lb-rule-cli", "Which Azure CLI command group binds frontend and backend ports, a backend pool, and a probe?", "Use `az network lb rule create` with the existing Load Balancer component names and reviewed protocol and port mapping.", "The load-balancing rule is the component that connects frontend flow matching to backend selection and health.", "Use `az network lb outbound-rule create` for inbound HTTPS distribution.|Outbound rules define SNAT egress rather than client ingress mapping.", "Use `az network lb inbound-nat-rule create` once for the whole pool.|An inbound NAT rule maps a frontend port to a particular backend context, not general pool distribution.", "Use `az network nsg rule create --backend-pool`.|NSG rules filter traffic and do not bind Load Balancer components."),
    Q("lb-components", "What is the boundary between a load-balancing rule and an inbound NAT rule?", "A load-balancing rule distributes matching frontend flows across a backend pool; an inbound NAT rule maps a specific frontend port to a particular backend target.", "Pool-wide application delivery and per-instance port mapping solve different ingress needs.", "An inbound NAT rule performs health-based distribution across all members.|That is the load-balancing rule's role.", "A load-balancing rule is only for administrator RDP.|It can distribute general supported TCP or UDP application flows.", "Both allocate outbound SNAT ports identically.|Outbound behavior is configured separately and depends on the design."),
    Q("lb-outbound", "What is the boundary between a Load Balancer outbound rule and NAT Gateway?", "An outbound rule supplies SNAT for members of a Load Balancer backend pool; NAT Gateway supplies scalable subnet-level outbound SNAT independent of inbound load-balancing rules.", "The association scope and operational capacity model determine which egress service fits.", "NAT Gateway distributes inbound application traffic while outbound rules route subnets.|NAT Gateway is outbound-only, and outbound rules are tied to Load Balancer configuration.", "Both require public IPs on every backend NIC.|They centralize public egress resources so individual VM public IPs are unnecessary.", "An outbound rule always overrides every subnet route.|SNAT configuration does not replace Azure route selection."),
  ],
  "AZ104-N08-B08": [
    Q("app-gateway-overview", "A regional web application needs path-based routing, TLS termination, and backend HTTP health checks. Which service fits?", "Use Azure Application Gateway with listeners, routing rules, backend settings, and custom probes.", "Application Gateway operates at layer seven and can route HTTP(S) by host or path while controlling TLS and backend requests.", "Use Azure Load Balancer only because TCP 443 implies HTTP awareness.|A layer-four Load Balancer does not inspect URL paths or terminate HTTP routing semantics.", "Use Traffic Manager to terminate TLS in the VNet.|Traffic Manager returns DNS answers and does not terminate client connections.", "Use an NSG with one rule per URL path.|NSGs filter network tuples and cannot inspect HTTP paths."),
    Q("waf-overview", "A public web application requires managed OWASP rule inspection before requests reach backends. Which control fits?", "Attach an Azure WAF policy to Application Gateway and run it in the reviewed detection or prevention mode.", "WAF evaluates HTTP(S) requests against managed and custom application-layer rules.", "Add an NSG deny for every SQL injection string.|NSGs do not inspect HTTP payload patterns.", "Enable source-IP affinity on Load Balancer.|Affinity changes backend selection and provides no web attack inspection.", "Use a service endpoint policy for the public website.|Endpoint policies restrict supported PaaS destinations from a subnet, not inbound HTTP content."),
    Q("traffic-manager", "An application has public endpoints in two regions and needs DNS-based priority failover without proxying client traffic. Which service fits?", "Use Azure Traffic Manager with priority routing and endpoint monitoring.", "Traffic Manager changes DNS responses based on endpoint health and priority while clients connect directly to the chosen endpoint.", "Use an internal Load Balancer spanning both regions.|Azure Load Balancer is regional and an internal frontend is not global public DNS failover.", "Use Azure Firewall DNAT in one region as a global DNS service.|Firewall translation does not provide global authoritative endpoint selection.", "Use a private endpoint zone group for both public regions.|Private DNS endpoint integration does not implement public priority routing."),
    Q("front-door-overview", "A global HTTP application needs an edge reverse proxy, anycast ingress, acceleration, and health-based origin routing. Which service fits?", "Use Azure Front Door with origins, routes, health probes, and the required edge security policy.", "Front Door proxies HTTP(S) at the global edge and selects healthy origins.", "Use Traffic Manager when the client connection must traverse an Azure edge proxy.|Traffic Manager is DNS-based and clients connect directly to endpoints.", "Use a regional internal Load Balancer as the Internet anycast edge.|Internal regional frontends are neither global nor publicly anycast.", "Use Azure Bastion because it has a public frontend.|Bastion brokers VM administration and is not a web delivery network."),
    Q("app-gateway-overview", "What sequence deploys Application Gateway for path-based web routing?", "Define frontend IP and listener, configure backend pools and HTTP settings, add application-aware probes, create path-map routing rules, apply NSGs and certificates, then validate each path and unhealthy-backend behavior.", "Layer-seven routing depends on coordinated listener, host, path, backend, probe, TLS, and network settings.", "Create a wildcard NSG allow first and infer paths from traffic.|Network admission cannot create or discover HTTP path maps safely.", "Configure path rules before any backend or listener exists.|The routing rule needs concrete frontend and backend objects.", "Disable probes so every backend receives diagnostic traffic.|Sending production flows to unready backends is not a safe validation strategy."),
    Q("waf-overview", "What sequence moves an Application Gateway WAF policy from Detection to Prevention?", "Review detection logs, tune justified exclusions and custom rules, test representative legitimate and malicious requests, switch to Prevention in a controlled rollout, and monitor blocks and backend health.", "Evidence-driven tuning reduces false positives before enforcement begins rejecting requests.", "Switch to Prevention before collecting logs and exempt all reported clients afterward.|That risks broad outages and identity-based bypasses without rule evidence.", "Disable the Application Gateway NSG because WAF replaces network controls.|Application-layer inspection complements rather than replaces network admission.", "Turn every managed rule off and call the policy Prevention.|An enforcement label without protection does not meet the requirement."),
    Q("traffic-manager", "What sequence implements regional priority failover with Traffic Manager?", "Create the profile and priority method, add public endpoints with ordered priorities, configure monitoring protocol, port, and path, validate each endpoint, simulate primary failure, observe DNS after TTL, and restore deliberately.", "Endpoint health and DNS caching jointly determine when clients receive the secondary answer.", "Set DNS TTL to zero and omit endpoint monitoring.|Failover still needs health evidence, and zero TTL does not eliminate all resolver caching.", "Add both regions to one backend pool of a regional Load Balancer.|Regional Load Balancer pools do not provide cross-region DNS priority failover.", "Create a WAF exclusion for the secondary region.|WAF exclusions do not establish DNS priority or endpoint health."),
    Q("app-gateway-overview", "Application Gateway marks backends Unhealthy after a certificate change, while direct HTTP works. What should be checked?", "Check probe protocol, backend TLS trust and hostname settings, probe path response, backend HTTP settings, NSGs, and listener compatibility.", "HTTPS backend health can fail on certificate or host semantics even when plaintext HTTP responds.", "Enable Floating IP on Application Gateway.|Floating IP is an Azure Load Balancer rule option, not an Application Gateway certificate fix.", "Create an NSG ASG containing the certificate.|Certificates are not network-interface group members.", "Change Traffic Manager routing to Geographic.|Global DNS method cannot repair regional backend TLS validation."),
    Q("traffic-manager", "Traffic Manager fails over in Azure health status, but many clients keep using the old endpoint for several minutes. What explains this?", "Recursive resolvers and clients can cache the prior DNS answer until its TTL and local caching behavior expire.", "DNS-based steering does not terminate existing connections or instantly replace cached answers.", "Traffic Manager must flush every operating system DNS cache remotely.|It has no control over all downstream caches.", "The secondary requires the primary's private IP.|Public DNS endpoints do not share a private frontend address.", "A Load Balancer probe threshold is overriding DNS TTL.|The services use separate health and caching mechanisms."),
    Q("front-door-overview", "Front Door reports an origin unhealthy even though browsing the origin URL directly succeeds. What should be compared?", "Compare the configured probe path, protocol, expected status, origin host header, certificate name, origin firewall admission, and probe frequency.", "The direct browser request may use a different host, path, TLS name, or source than the Front Door health probe.", "Add the origin to an ASG in Front Door's edge VNet.|Front Door origins are not members of a customer ASG at the edge.", "Enable Azure Bastion tunneling to the origin.|Administrative tunnels do not carry Front Door health probes.", "Create an outbound Load Balancer rule for client traffic.|Front Door origin health depends on its proxy-to-origin path, not backend VM SNAT by default."),
    Q("waf-overview", "WAF Prevention begins blocking a legitimate JSON upload with one managed rule ID. What is the safe response?", "Use WAF logs to confirm the matched rule and field, then add the narrowest justified exclusion or custom adjustment and retest malicious coverage.", "A scoped evidence-based change preserves the rest of the managed ruleset.", "Disable the entire managed ruleset permanently.|That removes unrelated protections to fix one false positive.", "Add an Internet-wide NSG allow for the uploader.|Network admission cannot override an application-layer WAF rule.", "Switch the backend Load Balancer to source-IP affinity.|Session distribution does not change WAF request inspection."),
    Q("firewall-overview", "Traffic reaches an internal Load Balancer frontend but fails after a UDR sends return traffic through Azure Firewall. What effective state should be investigated?", "Investigate asymmetric routing and firewall session state by comparing forward and return effective routes through the intended Azure Firewall path.", "A stateful firewall requires a coherent bidirectional route, while the Load Balancer rule alone does not guarantee symmetry.", "Increase the Load Balancer probe interval because probes define return routes.|Probe timing does not program UDR next hops.", "Add a public DNS alias to the internal frontend.|DNS naming cannot correct an asymmetric IP path.", "Enable WAF Prevention on Azure Firewall.|Azure Firewall policy and Application Gateway WAF are distinct controls, and WAF mode does not fix route symmetry."),
    Q("lb-probe-cli", "Which Azure CLI command should inspect or update the exact protocol, port, and path of an Azure Load Balancer probe?", "Use `az network lb probe show` to inspect it and `az network lb probe update` for a reviewed change.", "The probe command group is authoritative for the regional Load Balancer health contract.", "Use `az network traffic-manager endpoint show` for a regional LB probe.|Traffic Manager endpoints have separate monitoring configuration.", "Use `az network front-door probe update` for a Standard Load Balancer.|Front Door health settings do not modify the Load Balancer resource.", "Use `az network nsg rule show --health-probe` to change the path.|NSG rules have ports and protocols but no HTTP probe-path property."),
    Q("app-gateway-overview", "What is the boundary among Azure Load Balancer, Application Gateway, Traffic Manager, and Front Door?", "Load Balancer is regional layer four, Application Gateway is regional layer seven, Traffic Manager is global DNS-based selection, and Front Door is a global layer-seven reverse proxy.", "Selecting the service requires matching network layer, geographic scope, proxy behavior, and application features.", "All four terminate HTTP and inspect URL paths.|Load Balancer and Traffic Manager do not provide that layer-seven proxy behavior.", "Traffic Manager and Front Door are regional private-IP balancers.|Both are global services with different DNS-versus-proxy models.", "Application Gateway is the only service that can health-check endpoints.|Each service has health concepts, but at different layers and scopes."),
  ],
};

const curriculum = JSON.parse(await readFile(curriculumPath, "utf8"));
const slotOrdinal = (slot) => Number(slot.slotId.match(/-(\d+)$/)?.[1] ?? 0);
const expectedBlocks = curriculum.blockPlans.filter((block) => block.nodeId === NODE);
const expectedMixByOperation = { decision_selection: "scenarioDecision", configuration_sequence: "configurationSequence", decision_diagnosis: "troubleshootingEffectiveState", tool_selection: "toolOrIacCommand", boundary_analysis: "boundaryOrContrast" };
const exactToolSourceByCard = new Map(Object.entries({
  "AZ104-N08-B01-17": "nsg-rule-cli",
  "AZ104-N08-B01-18": "nsg-rule-cli",
  "AZ104-N08-B02-15": "asg-cli",
  "AZ104-N08-B02-16": "asg-cli",
  "AZ104-N08-B03-10": "bastion-cli",
  "AZ104-N08-B04-13": "subnet-cli",
  "AZ104-N08-B05-15": "private-endpoint-cli",
  "AZ104-N08-B05-16": "private-dns-zone-group-cli",
  "AZ104-N08-B06-10": "private-endpoint-cli",
  "AZ104-N08-B07-17": "lb-probe-cli",
  "AZ104-N08-B07-18": "lb-rule-cli",
  "AZ104-N08-B08-13": "lb-probe-cli",
}));
const seenPrompts = new Set();
const seenOptions = new Set();
const seenWrongTuples = new Set();

if (expectedBlocks.length !== 8 || Object.keys(blocks).length !== 8) throw new Error("Expected exactly 8 N08 blocks.");
let totalCards = 0;
for (const block of expectedBlocks) {
  const cards = blocks[block.blockId];
  const slots = curriculum.slots.filter((slot) => slot.blockId === block.blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  if (!cards || cards.length !== slots.length) throw new Error(`${block.blockId}: ${cards?.length ?? 0} cards for ${slots.length} slots.`);
  const actualMix = {};
  for (const slot of slots) actualMix[expectedMixByOperation[slot.learningOperation]] = (actualMix[expectedMixByOperation[slot.learningOperation]] ?? 0) + 1;
  for (const [key, count] of Object.entries(block.questionMix)) if (actualMix[key] !== count) throw new Error(`${block.blockId}: ${key} ${actualMix[key]} != ${count}.`);
  cards.forEach((card, index) => {
    const spec = sourceById.get(card.source);
    if (!spec) throw new Error(`${block.blockId}-${index + 1}: unknown source ${card.source}.`);
    const toolKey = `${block.blockId}-${index + 1}`;
    if (slots[index].learningOperation === "tool_selection" && exactToolSourceByCard.get(toolKey) !== card.source) throw new Error(`${toolKey}: tool source ${card.source} is not the reviewed exact source ${exactToolSourceByCard.get(toolKey)}.`);
    if (!card.prompt || !card.correct || !card.reason || card.wrongs.length !== 3) throw new Error(`${block.blockId}-${index + 1}: incomplete card.`);
    if (seenPrompts.has(card.prompt)) throw new Error(`Duplicate prompt: ${card.prompt}`);
    seenPrompts.add(card.prompt);
    const options = [card.correct, ...card.wrongs.map((wrong) => wrong.text)];
    if (new Set(options).size !== 4) throw new Error(`${block.blockId}-${index + 1}: duplicate local option.`);
    for (const option of options) {
      if (seenOptions.has(option)) throw new Error(`${block.blockId}-${index + 1}: duplicate global option: ${option}`);
      seenOptions.add(option);
    }
    const tuple = card.wrongs.map((wrong) => wrong.text).join("\n");
    if (seenWrongTuples.has(tuple)) throw new Error(`${block.blockId}-${index + 1}: duplicate distractor tuple.`);
    seenWrongTuples.add(tuple);
    if (card.wrongs.some((wrong) => !wrong.text || !wrong.why || /all of the above|none of the above|obviously wrong|placeholder|temporary|future|draft|seed|baseline/i.test(`${wrong.text} ${wrong.why}`))) throw new Error(`${block.blockId}-${index + 1}: generic or incomplete distractor.`);
    const relevantText = `${card.prompt} ${card.correct}`.toLowerCase();
    if (!spec.topics.some((topic) => relevantText.includes(topic.toLowerCase()))) throw new Error(`${block.blockId}-${index + 1}: source ${card.source} lacks exact topical relevance.`);
  });
  totalCards += cards.length;
}
if (totalCards !== 128 || seenPrompts.size !== 128 || seenOptions.size !== 512 || seenWrongTuples.size !== 128) throw new Error(`N08 global uniqueness failed: ${totalCards}/${seenPrompts.size}/${seenOptions.size}/${seenWrongTuples.size}.`);

if (process.argv.includes("--check")) {
  console.log(`AZ104-N08 slot-order, exact-mix, topical-source, prompt, option, and explicit-distractor QA passed for ${totalCards} cases and ${seenOptions.size} globally unique options across ${expectedBlocks.length} blocks.`);
  process.exit(0);
}

const registrySources = sourceSpecs.map(({ boundary, transfer, topics, ...source }) => source);
const sourceRecords = new Map(curriculum.sourceRecords.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = sourceRecords.get(source.sourceId);
  sourceRecords.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
curriculum.sourceRecords = [...sourceRecords.values()].sort((a, b) => a.sourceId.localeCompare(b.sourceId));

for (const [blockId, cards] of Object.entries(blocks)) {
  const slots = curriculum.slots.filter((slot) => slot.blockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  slots.forEach((slot, index) => {
    const requirement = slot.sourceRequirements.directFirstPartyDocumentation[0];
    requirement.testedProperties = [`${blockId.toLowerCase()}-${slot.learningOperation}-${String(index + 1).padStart(2, "0")}`];
    requirement.sourceRefs = [cards[index].source];
    requirement.resolutionState = "resolved_exact_direct";
  });
  const target = curriculum.targetPlans.find((entry) => entry.blockId === blockId);
  if (!target) throw new Error(`${blockId}: missing target plan.`);
  target.sourceRefs = [...new Set(cards.map((card) => card.source))].sort();
}

const activeSourceIds = new Set();
for (const slot of curriculum.slots) {
  for (const ref of slot.sourceRequirements.officialObjective?.sourceRefs ?? []) activeSourceIds.add(ref);
  for (const requirement of slot.sourceRequirements.directFirstPartyDocumentation ?? []) for (const ref of requirement.sourceRefs ?? []) activeSourceIds.add(ref);
}
const collectBlueprintSources = (value) => {
  if (Array.isArray(value)) return value.forEach(collectBlueprintSources);
  if (!value || typeof value !== "object") return;
  for (const [key, child] of Object.entries(value)) key === "sourceRefs" && Array.isArray(child) ? child.forEach((ref) => activeSourceIds.add(ref)) : collectBlueprintSources(child);
};
collectBlueprintSources(curriculum.examSimulationBlueprint);
curriculum.sourceRecords = curriculum.sourceRecords.filter((source) => activeSourceIds.has(source.sourceId));
const { promotionProvenance, contentFingerprint, ...fingerprintPayload } = curriculum;
curriculum.contentFingerprint = createHash("sha256").update(JSON.stringify(fingerprintPayload)).digest("hex");
await writeFile(curriculumPath, `${JSON.stringify(curriculum, null, 2)}\n`);

const anchorRegistry = JSON.parse(await readFile(anchorsPath, "utf8"));
const anchorSources = new Map(anchorRegistry.anchors.map((source) => [source.sourceId, source]));
for (const source of registrySources) {
  const existing = anchorSources.get(source.sourceId);
  anchorSources.set(source.sourceId, existing ? { ...existing, ...source, authoritativeFor: [...new Set([...(existing.authoritativeFor ?? []), ...source.authoritativeFor])] } : source);
}
const retainedSourceIds = new Set(curriculum.sourceRecords.map((source) => source.sourceId));
anchorRegistry.anchors = [...anchorSources.values()].filter((source) => retainedSourceIds.has(source.sourceId)).sort((a, b) => a.sourceId.localeCompare(b.sourceId));
await writeFile(anchorsPath, `${JSON.stringify(anchorRegistry, null, 2)}\n`);

const { manifest, model } = await buildManifest(ROOT);
const manifestTrack = manifest.tracks.find((entry) => entry.trackId === TRACK);
const canonicalTrack = model.curricula.get(TRACK);
const normalizedById = new Map(canonicalTrack.normalized.slots.map((slot) => [slot.slotId, slot]));
const optionIds = ["A", "B", "C", "D"];

for (const [blockId, cards] of Object.entries(blocks)) {
  const batchId = `az104-2026-08-16-${blockId}`;
  const provenance = { authoringMethod: "manual", approvalStatus: "unapproved", author: "Codex", createdAt: CREATED_AT, contentBatchId: batchId };
  const slots = manifestTrack.slots.filter((slot) => slot.learningBlockId === blockId).sort((a, b) => slotOrdinal(a) - slotOrdinal(b));
  const items = slots.map((slot, index) => {
    const card = cards[index];
    const normalized = normalizedById.get(slot.slotId);
    if (!slot.authoringAdmitted || !slot.sourceBinding || !slot.sourceBinding.sourceRefs.includes(card.source)) throw new Error(`${slot.slotId}: invalid source binding.`);
    const spec = sourceById.get(card.source);
    const raw = [{ text: card.correct, why: card.reason, correct: true }, ...card.wrongs.map((wrong) => ({ ...wrong, correct: false }))];
    const shift = index % 4;
    const arranged = [...raw.slice(shift), ...raw.slice(0, shift)];
    const answer = arranged.findIndex((option) => option.correct);
    return {
      itemId: `az104-${blockId}-${String(index + 1).padStart(3, "0")}`,
      slotId: slot.slotId,
      nodeId: NODE,
      learningBlockId: blockId,
      taxonomy: { examDomainId: normalized.raw.primarySimulationDomainId, competencyAreaId: NODE, topicId: blockId, skillAtomId: normalized.raw.directSkillOrDecisionAtomId },
      prompt: card.prompt,
      constraints: [spec.boundary, spec.transfer],
      interaction: { type: "choice", selectionMode: "single", options: arranged.map((option, i) => ({ optionId: optionIds[i], text: option.text })), acceptedOptionIds: [optionIds[answer]] },
      scoringContract: { type: "choice", resultSemantics: "exact_selected_set_with_partial_v1", selectionMode: "single" },
      feedback: {
        Reason: card.reason,
        Details: { mechanismOrProperty: card.reason, scenarioApplication: card.correct, errorCorrection: arranged.filter((option) => !option.correct).map((option) => option.why).join(" "), boundaryOrTradeoff: spec.boundary, transfer: spec.transfer, url: spec.url },
        wrongOptionExplanationsByOptionId: Object.fromEntries(arranged.map((option, i) => [optionIds[i], option.why]).filter((_, i) => i !== answer)),
        omittedCorrectElementExplanationsByOptionId: {},
      },
      modeEligibility: slot.modeEligibility,
      sourceBinding: slot.sourceBinding,
      authoringProvenance: provenance,
    };
  });
  const payload = { schemaVersion: "certification-manual-source-v2", batchId, trackId: TRACK, familyId: "certification", contentVersion: "microsoft-azure-administrator-associate-az-104-authoring-v2026.08.15", taxonomyVersion: "2026.08.15", nodeId: NODE, learningBlockId: blockId, slotIds: items.map((item) => item.slotId), items, authoringProvenance: provenance };
  const output = join(ROOT, "manual/source", TRACK, NODE, `${blockId}.json`);
  await mkdir(join(output, ".."), { recursive: true });
  await writeFile(output, `${JSON.stringify(payload, null, 2)}\n`);
  console.log(`Authored ${items.length} reviewed items at ${output}.`);
}
